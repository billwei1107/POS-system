---
alwaysApply: true
---
# AI Agent 操作規範

> [!IMPORTANT]
> 以下規範適用於所有 AI Agent（Cursor、Antigravity、Copilot 等）在本專案中的操作行為。

## 1. 模塊化開發 (Modular Development)

- **嚴格模塊化**：撰寫程式碼時，必須以模塊化的方式進行開發。
  - 每個功能獨立為一個模塊（函數、類、組件），**禁止**將多個功能混寫在同一個作用域中。
  - 前端：按功能拆分組件，共用邏輯抽為 hooks / utils / services。
  - 後端：嚴格遵守 Controller → Service → Repository 分層，每層只做該層的事。
  - 公共邏輯（日期格式化、驗證、API 呼叫等）必須抽離為獨立的工具模塊，**禁止**重複實現。
- **檔案職責單一**：一個檔案只負責一個功能模塊，過長的檔案（建議超過 300 行）必須拆分。
- **接口清晰**：模塊之間通過明確定義的接口（Props、函數參數、API Contract）互動，減少耦合。

## 2. 功能測試驗證 (Testing After Implementation)

- **完成即測試**：每完成一個功能模塊後，**必須**立即進行測試驗證，確認功能正確後才能繼續下一個功能。
  - 前端：在瀏覽器中實際操作驗證 UI 與互動邏輯。
  - 後端：使用 API 測試工具（curl / Postman / 測試腳本）驗證 API 回應正確。
  - 資料庫：確認資料正確寫入/讀取，驗證約束與索引生效。
- **測試範圍**：
  - ✅ 正常流程 (Happy Path)：功能是否如預期運作
  - ✅ 異常流程 (Error Path)：錯誤輸入是否被正確攔截
  - ✅ 邊界條件：空值、極長字串、特殊字元等
- **測試結果記錄**：測試結果必須記錄在當日 devlog 中，若發現問題則記錄在 `troubleshooting.md`。
- **禁止跳過測試**：**絕對禁止**一次開發多個功能後才一起測試，必須逐個功能驗證。

## 3. 主動詢問 (Ask Before Assuming)

- **不確定就問**：遇到任何不確定的需求、設計決策或技術選型時，**必須先詢問用戶意見**，禁止自行假設或猜測。
- **需要詢問的場景**：
  - 需求文檔中描述模糊或有多種理解方式時
  - 有多種技術實現方案，且各有優劣時
  - 需要變更原先已確認的設計或架構時
  - 遇到可能影響效能、安全性或用戶體驗的重大決策時
  - 需要安裝新的依賴或引入新的第三方服務時
  - 發現需求文檔與設計圖之間有矛盾時
- **詢問格式**：提問時必須提供足夠的上下文，包括：
  1. 當前遇到的情況
  2. 可供選擇的方案（至少兩個）
  3. 每個方案的優缺點
  4. 你建議的方案及理由
- **禁止行為**：
  - ❌ 自行決定重大架構變更而不告知用戶
  - ❌ 遇到需求不明確時自行腦補功能
  - ❌ 刪除或大幅重構現有代碼而不事先說明

---

## 4. 開發階段與 Skill 系統 (Development Phases & Skills)

本專案在 `.agent/skills/` 目錄下提供了三個標準化 Skill，AI 必須按照以下順序在對應階段觸發：

```
用戶提供需求
  ↓
階段 1 ─ 需求確認    → 觸發 Skill「需求文檔撰寫」
  ↓                    產出：需求/需求文檔.md
階段 2 ─ 設計規劃    → 觸發 Skill「開發流程設計」
  ↓                    產出：頁面結構圖、流程圖、架構圖、ER圖、API設計、技術棧
階段 3 ─ 開始開發    → 按需求文檔與設計圖逐模塊開發 + 測試
  ↓
階段 4 ─ 測試驗收    → 觸發 Skill「客戶測試流程」
                       產出：測試計畫、測試案例、測試反饋表
```

**觸發規則**：
- 新專案 **必須** 從階段 1 開始，**禁止**跳過需求確認直接寫 code
- 每個 Skill 的詳細操作步驟請閱讀 `.agent/skills/[Skill名稱]/SKILL.md`
- 若用戶明確表示某階段已完成（如已有需求文檔），可跳過對應 Skill

---

# 代碼規範

## 日誌系統規範

### 開發日誌類型

1. **主開發日誌 (devlog)**
   - 文件名：`devlog.md`
   - 位置：`devlog/` 目錄
   - 用途：記錄整個項目的開發規劃、整體進度和重要里程碑
   - **開頭格式要求**：
     - 必須在文件開頭明確列出以下信息：
       - 項目名稱
       - 開發時間
       - 開發期限
       - 部署方式
       - 需使用的技術棧
       - 項目完整結構樹

2. **日常開發日誌 (Daily Development Log)**
   - 文件名格式：`YYYY-MM-DD-devlog.md`（例如：`2026-04-08-devlog.md`）
   - 位置：`devlog/` 目錄
   - 用途：記錄每次完成事項的詳細記錄

3. **問題與解決日誌 (Issues and Solutions Log)**
   - 文件名：`troubleshooting.md`
   - 位置：`devlog/` 目錄
   - 用途：專門記錄遇到的問題和對應的解決方式

### 日誌記錄規則

1. **自動記錄時機**
   - 任務完成時
   - 代碼撰寫完成時
   - 代碼執行完成時
   - 遇到問題時
   - 問題解決時

2. **時間標記格式**
   - 格式：`HH:MM`（例如：`14:30`）
   - 記錄前必須先使用 `date` 指令確認當前時間

3. **問題處理流程**
   - 遇到問題時，先查閱 `troubleshooting.md` 文件
   - 查找是否有類似問題的記錄和解決方案
   - 優先使用已有的解決方案

4. **日誌存放位置**
   - 所有 devlog 相關文件一律存放在 `devlog/` 目錄中
5. **日誌文件格式**
   - 所有 devlog 日誌文件一律使用 `.md` 擴展名
6. **日誌職責分工**
   - `主devlog` 僅負責記錄所有功能模塊的開發規劃，`日期-devlog` 專責記錄開發過程與進度信息

## 中文使用規範

- 具體使用的語言規範（例如：是否使用簡體中文、繁體中文或其他語言），由當前項目的 `projectrule.mdc` 決定

## 任務管理規範

- 每次接收到任務時，必須先建立任務清單
- 任務清單必須使用中文編寫

## 環境與路徑規範

- 所有腳本、配置與代碼訪問路徑必須通過環境變量傳入，禁止硬編碼絕對路徑
- 訪問文件系統時一律使用相對路徑（以項目根或當前工作目錄為基準），如需絕對路徑必須由環境變量提供

## 需求管理規範

- 所有需求必須統一記錄在 `需求文檔` 文件中
- 確認需求時僅能從 `需求文檔` 獲取信息，不得引用其他來源
- 閱讀需求後需要先整理理解摘要並向用戶報告，得到確認後方可實際更新 `需求文檔`

## 需求與流程圖規範

- 所有需求文檔一律存放在「需求目錄」中（例如專案根目錄下的 `需求/`），禁止將需求內容分散在其他任意位置
- 每個項目必須準備一個專用的「流程圖目錄」（例如 `需求/流程圖/`），專門存放與需求對應的流程圖文件與匯出圖片
- 在閱讀完需求目錄中的需求文檔後，必須依照需求內容為每一個功能模塊建立對應的流程圖
- 流程圖應以 Markdown Preview 友善的方式撰寫（例如使用 Mermaid 語法），並以文字清楚標示各節點與流程關係
- 流程圖的背景顏色一律設置為白色，確保匯出圖片在各種介面上都有良好可讀性
- 流程圖中的文字語言必須遵守當前專案的 `projectrule.mdc` 中對語言的規定
- 完成流程圖撰寫後，必須將流程圖匯出為 PNG 圖片，並存放在 `需求/流程圖/` 目錄

### 頁面結構規範

- 如果項目有頁面結構需求，必須使用樹狀圖的方式撰寫頁面結構
- 頁面結構文檔一律存放在 `需求/頁面結構/` 目錄下
- 頁面結構應以 Mermaid Flowchart (`graph LR`) 語法繪製為左到右 (LR) 的樹狀展開
- 每個節點必須添加層級樣式標記 `:::levelN`
- 頁面結構的背景顏色一律設置為白色
- 完成頁面結構撰寫後，匯出為 PNG 圖片存放在 `需求/頁面結構/` 目錄

### 頁面結構樣式配色

```
classDef level1 fill:#f97316,stroke:none,color:white;
classDef level2 fill:#0d9488,stroke:none,color:white;
classDef level3 fill:#8b5cf6,stroke:none,color:white;
classDef level4 fill:#ec4899,stroke:none,color:white;
```

### 圖表匯出規範

- 所有流程圖和頁面結構的 Markdown 文件完成後，必須匯出為 PNG 圖片
- PNG 圖片生成參數統一使用：`-b white -w 7680 -H 4320 -s 4`
- PNG 文件必須與對應的 Markdown 文件放在同一目錄下

## 代碼註釋規範

- 以每一個功能模塊為單位撰寫註釋，清楚說明該部分的用途與設計思路
- 註釋使用哪種語言、是否需要中英雙語，由當前專案的 `projectrule.mdc` 決定

## 代碼撰寫規範

- 每個文件首行必須寫明該文件用途的註釋
- 嚴禁將所有代碼堆疊在同一作用域，須按功能模塊拆分函數、類或組件
- 公共邏輯必須提取為獨立模塊或工具，避免重複實現
- 每個模塊需遵循單一職責並保持接口清晰
- 新增模塊需同步更新文檔與依賴圖

## 依賴管理規範

- 安裝依賴時，在無特殊需求或相容性限制的情況下，一律安裝可用的最新版依賴
- 如因專案需求需要鎖定版本，必須在 `projectrule.mdc` 中明確說明

## Git 提交規範

1. **Git 初始化檢查**
   - 每次提交前必須先確認 Git 倉庫是否已初始化
   - 如果未初始化，需要先執行 `git init`

2. **自動提交流程**
   - 每次任務完成時，必須自行執行 Git 提交
   - 提交前需要確認當前時間（使用 `date` 指令）

3. **提交消息規範**
   - 格式: `type(scope): subject`
   - 提交消息需要包含中英文，確保清楚記錄變更內容

4. **提交時間記錄**
   - 提交完成後，必須在對應日期的日誌文件中記錄提交時間

## 開發日誌維護規範

- 標註已完成的部分，保留開發歷史記錄
- 新增未完成的開發進度，不能隨意刪除舊記錄
- 如果有需要補充的內容，可以主動補充

## 開發期限管理規範

1. **期限檢查機制**
   - 每次撰寫代碼前，必須先確認當前時間並對照開發期限
   - 計算剩餘開發時間與計劃進度，評估是否按計劃進行
   - 如發現進度延遲風險，必須立即向用戶報告

2. **進度調整建議**
   - 當進度延遲超過計劃時間的 20% 時，建議重新評估任務優先級
   - 必須在每次代碼提交時評估進度，並在日誌中記錄進度狀態

## Docker 開發環境規範

### 1. 統一網關方案避免端口衝突

**問題背景**:
- 開發機可能同時運行多個應用項目
- 每個項目都需要佔用端口（前端 3000、後端 8080、數據庫 3306 等）
- 直接暴露所有服務端口會導致嚴重的端口衝突

**解決方案**:
採用統一網關方案，通過單一入口端口和路徑前綴區分不同項目：

```
開發機架構:
┌─────────────────────────────────────────┐
│         開發機 (Development Machine)      │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │   統一網關 (Unified Gateway)       │ │
│  │   Nginx - Port 80                 │ │
│  │   ├─ /project-a  → 項目A          │ │
│  │   ├─ /project-b  → 項目B          │ │
│  │   └─ /project-c  → 項目C          │ │
│  └───────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### 2. Docker 內部網絡配置

**核心原則**:
1. **僅網關暴露端口**: 統一網關容器暴露 80 端口到主機，所有應用項目服務僅在 Docker 內部網絡通信
2. **獨立項目網絡**: 每個項目創建獨立的 Docker 網絡
3. **路徑前綴路由**: 通過 Nginx location 配置路徑前綴

### 3. 網關服務使用規範

**重要原則**:
- 是否需要在本專案中使用開發網關，由當前專案的 `projectrule.mdc` 中的設定決定
- 網關實際目錄一律放在專案目錄之外（例如上層的 `../dev-gateway`），專案內不可直接硬編碼絕對路徑
- 若需要使用網關服務，必須通過項目根目錄下的軟鏈接 `dev-gateway` 或相對路徑 `../dev-gateway` 訪問網關配置
