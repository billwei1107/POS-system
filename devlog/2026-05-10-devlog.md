# 2026-05-10 開發日誌

## 開發規劃 (Planning)

- [x] 將新版專案初始模板的 AI 協作流程接入既有 POS 專案
- [x] 新增 Claude 規劃與 Codex 執行交接文件
- [x] 新增模塊化組件 reference 自動初始化腳本
- [x] 驗證 reference 腳本與文件結構

## 專案結構 (Project Structure)

```text
POS/
├── AGENTS.md                         # AI Agent 第一入口
├── CLAUDE.md                         # Claude 本地入口
├── ai-project-start.md               # AI 專案啟動與協作流程
├── docs/
│   └── project-map.md                # POS 專案資料夾總覽
├── scripts/
│   └── setup-module-reference.sh     # 自動抓取 reference/模塊化組件
├── reference/
│   └── README.md                     # reference 目錄說明
├── 需求/
│   ├── README.md                     # 需求區資料結構
│   ├── AI協作交接規範.md              # Claude → Codex 分工規範
│   ├── 需求文檔.md                    # Claude 補齊需求與業務規則
│   ├── 系統規格表.md                  # Claude 補齊技術棧與系統規格
│   ├── 開發流程規劃.md                # Claude 補齊開發階段、模塊與風險
│   ├── 功能檢驗流程書.md              # Claude 補齊驗收與測試案例
│   ├── 流程圖/
│   └── 頁面結構/
├── backend/
├── frontend-web/
├── docker/
├── env/
└── devlog/
```

## 進度 (Progress)

- 已將 `AGENTS.md` 從全局 symlink 改為 POS 專案本地入口，避免 Claude / Codex 接手時看不到本專案的協作流程。
- 已新增 `CLAUDE.md`，讓 Claude 先補齊需求與規劃文件，不直接綁死實作細節。
- 已新增 `ai-project-start.md`，定義 reference 初始化、Claude 規劃與 Codex 執行順序。
- 已新增 `scripts/setup-module-reference.sh`，預設抓取 `billwei1107/module` 的 `module-v2026.05.10.2`。
- 已新增 `需求/` 下四份核心規劃模板：需求文檔、系統規格表、開發流程規劃、功能檢驗流程書。
- 已更新 `README.md` 與 `.gitignore`，讓新 reference 流程與忽略規則生效。
- 已將誤放在 POS 內的舊模板資料夾整理併入正式位置：`.agent/`、`.cursor/`、`需求/`、`devlog/troubleshooting.md`。
- 已移除舊模板資料夾，避免後續 AI 將它誤判為專案根目錄。
- 已把舊模板內較完整的 POS 需求、技術規格、UIUX 設計提示詞與開發流程規劃升級為 `需求/` 下的正式文件。
- 已修正模塊同步文件中的母體路徑，統一指向真正母體 `/Users/wei/Desktop/code/模塊化組件/`，`reference/模塊化組件/` 僅保留為參考來源。
- 已新增 `docs/project-map.md`，補齊整個 POS 資料夾的結構、後端模組、前端 feature、Docker/env、reference 與忽略產物說明。
- 已補全 `需求/系統規格表.md`，改為目前實際技術棧、服務埠、模組策略、Flyway、安全與非功能規格。
- 已補全 `需求/功能檢驗流程書.md`，建立 POS 功能驗收清單、標準測試流程、邊界情境與回歸測試基準。
- 已更新 `README.md`、`AGENTS.md`、`ai-project-start.md`、`需求/README.md`，讓 AI 啟動順序包含 `docs/project-map.md`。
- 已清理本地忽略檔 `.DS_Store`。
- 已實際使用瀏覽器自動化檢查目前 POS 前端功能，涵蓋後台登入、請假頁 API、POS 收銀、POS Orders、POS Inventory、手機版購物車與 WebSocket 連線。
- 已修正前端 API 路徑重複 `/api/api` 問題，統一 feature API 使用 axios base `/api` 後的 `/v1/...` 路徑。
- 已修正通知 WebSocket 預設連到 `localhost:8080` 的問題，改為同源 `/ws/notifications`，並在本地 Nginx 補上 `/ws/` proxy。
- 已新增 POS Orders 路由，並移除 POS 側邊選單中尚未實作的 Dashboard / Settings 入口，避免點擊後被 wildcard 導回收銀頁。
- 已修正 POS 手機版購物車 Drawer 空白問題，讓 RegisterPage 可在 Drawer DOM 出現後重新掛載 Cart portal。
- 已修正 Orders 頁在缺少 `VITE_DEFAULT_STORE_ID` 時仍打 API 造成後端 500 的問題，改為前端提示需設定預設門店。
- 已依 `需求/UIUX-設計提示詞.md` 的 POS 深色高對比風格重設 POS 前端主介面，介面文字先統一為英文。
- 已強化 POS Register 桌機版：新增頁面標題、操作說明、快捷操作、搜尋列、掃碼按鈕、可橫向捲動分類列、商品卡片狀態與備餐時間。
- 已強化 POS Current Order 購物車：新增訂單編號、內用資訊、桌號/人數標籤、Hold/Delete icon action、商品備註標籤、總計與付款 CTA 視覺層級。
- 已將 POS Orders 頁調整為英文管理介面，新增指標卡、搜尋列、英文欄位、英文空狀態與缺少預設門店提示。
- 已改善 Checkout 頁付款方式選取狀態與響應式左右分割，在手機版改為上下堆疊，避免內容擠壓。
- 已強化 POS Layout 桌機導覽與頂部狀態列，補齊門店、班次、終端與操作員狀態資訊。
- 已依使用者修正方向，將 POS Register、Current Order、Orders、Checkout 與 POS Layout 顯示文字改回繁體中文優先。
- 已補上 RegisterPage、Cart、PosLayout 的中英雙語檔頭註釋，並整理本次觸及區塊的中英雙語註釋。
- 已為手機版 POS 頂部導覽與購物車按鈕補上中文 `aria-label`，提升可及性並讓瀏覽器自動化測試能穩定定位操作目標。
- 已依開發規劃繼續接入商品與分類管理功能，新增 `/pos/products` 與 `/pos/categories` 路由及 POS 側邊導覽入口。
- 已將商品與分類管理頁改用既有共用模塊 `PageHeader`、`DataTable`、`StatusChip`、`ConfirmDialog` 與 `formatMoney`，降低後續專案 UI 替換時的耦合。
- 已為商品與分類管理頁補上未登入防護：未登入時不呼叫商品/分類 API，顯示登入提示並停用新增操作，避免瀏覽器測試時產生不必要的 403 錯誤。
- 已清理前端 lint 基線中 `any`、React Hooks dependency、set-state-in-effect、refs 與 purity 相關問題，讓 `npm run lint` 可作為後續前端開發 gate。
- 已修正後端健康檢查端點：`SecurityConfig` 放行 `/actuator/health`，並在 `app` 模組補入 `spring-boot-starter-actuator`。
- 已將可共用的前端型別/lint 修正、WebSocket hook 修正、workflow 頁面 hook 修正、`DataTable` 泛型修正與 health endpoint 修正同步回母體 `/Users/wei/Desktop/code/模塊化組件/`。
- 已將 POS reference 初始化預設版本更新為 `module-v2026.05.10.3`，讓後續新專案或重新初始化 reference 時可取得本輪母體修正。
- 已將 POS 收銀台 `RegisterPage` 從 mock 商品改為讀取 `pos-products` API，支援分類篩選與搜尋條件。
- 已新增 `features/pos-orders/store/cartStore.ts`，讓收銀台、購物車與 checkout 共用同一份本地購物車狀態。
- 已將購物車數量加減、移除、清空、5% 稅額與應收金額改為由實際商品資料計算。
- 已修正前端 `ApiResponse` 正規化：axios interceptor 會為後端 `code/message/data` 補上 `success`，並修正 `loginApi` 取內層登入資料。
- 已修正 `productApi`、`orderApi` 與 axios interceptor 的回應型別不一致問題，避免登入後頁面拿到錯誤資料層。
- 已修正 Zustand selector 直接回傳新 totals 物件造成 React production runtime 最大更新深度錯誤的問題，改由純函式與 `useMemo` 計算。
- 已將可共用的 `axiosInstance`、`ApiResponse`、`authApi` 修正同步回母體 `/Users/wei/Desktop/code/模塊化組件/`。
- 已將 POS reference 初始化預設版本更新為 `module-v2026.05.10.4`，讓後續新專案取得 ApiResponse 正規化修正。

## 驗證 (Verification)

- `bash -n scripts/setup-module-reference.sh`：通過
- `/tmp/pos-reference-smoke` 空專案 reference 初始化：通過，成功 checkout `module-v2026.05.10.2`
- POS 專案內執行 `bash scripts/setup-module-reference.sh`：通過，已建立 `reference/模塊化組件/ai-handoff.md`
- `git -C reference/模塊化組件 describe --tags --exact-match`：輸出 `module-v2026.05.10.2`
- `git diff --check`：通過
- `reference/模塊化組件/` 已由 `.gitignore` 忽略，避免將母體 clone 進 POS 版控
- 舊模板資料夾存在性檢查：通過，舊模板資料夾已不存在
- `rg` 檢查目前入口文件與需求文件：已無要求 AI 從舊模板資料夾作為根目錄讀取的路徑
- `git diff --check`：通過，文件無尾隨空白
- `npm run build`（frontend-web）：通過；保留 Vite chunk size > 500 kB 警告
- `docker compose up -d --build frontend`（docker/local）：通過；frontend 容器已重建並啟動
- 瀏覽器回歸測試：
  - 後台登入 `admin / 123456`：通過
  - POS 收銀桌機版：通過，可看到商品、Current Order 與 PAY NOW
  - POS Orders：通過，未設定 `VITE_DEFAULT_STORE_ID` 時顯示前端提示，不再出現 API 500
  - POS Inventory：通過
  - POS 手機版購物車：通過，Drawer 內可看到 Current Order 與 PAY NOW
  - API 重複路徑檢查：通過，未再出現 `/api/api`
  - WebSocket 目標檢查：通過，未再連到 `localhost:8080`
  - Console / request failed 檢查：通過，未捕捉到非圖片錯誤
- `npm run build`（frontend-web，POS UI 設計調整後）：通過；保留 Vite chunk size > 500 kB 警告
- `docker compose up -d --build frontend`（POS UI 設計調整後）：通過；frontend 容器已重建並啟動
- 瀏覽器 UI 回歸測試：
  - Desktop Register UI：通過，可看到 Register、Current Order、PAY NOW 與英文商品操作介面
  - Checkout flow：通過，可從 PAY NOW 進入 Select Payment，付款方式與 Confirm Selection 顯示正常
  - Orders UI：通過，可看到英文 Orders 頁、缺少預設門店提示與 No orders yet 空狀態
  - Mobile cart UI：通過，手機版購物車 Drawer 可看到 Current Order 與 PAY NOW
  - Console / request failed：通過，未捕捉到 console/page error 或非圖片 request failure
- `npm run build`（frontend-web，POS 中文優先修正後）：通過；保留 Vite chunk size > 500 kB 警告
- `git diff --check`（POS 中文優先修正後）：通過
- `docker compose up -d --build frontend`（POS 中文優先修正後）：通過；frontend 容器已重建並啟動
- 瀏覽器中文 UI 回歸測試：
  - Desktop Register UI：通過，可看到收銀台、目前訂單、立即結帳、全部商品、中文商品名稱與信義旗艦店狀態列
  - Checkout flow：通過，可從立即結帳進入結帳頁，看到訂單摘要、選擇付款方式、確認付款方式與中文品項
  - Orders UI：通過，可看到訂單、訂單狀態、目前沒有訂單與尚未設定預設門店提示
  - Mobile cart UI：通過，手機版可用「開啟購物車」操作抽屜，看到目前訂單與立即結帳
  - Console / request failed：通過，未捕捉到 console/page error 或非圖片 request failure
- `npm run lint`（frontend-web，商品/分類管理接入後）：通過
- `npm run build`（frontend-web，商品/分類管理接入後）：通過；保留 Vite chunk size > 500 kB 警告
- `mvn -pl module-pos-product -am test`（backend）：通過
- `mvn -pl module-common -am test`（backend）：通過
- `mvn -pl app -am test -DskipTests`（backend）：通過
- `docker compose up -d --build backend frontend`（docker/local）：通過；backend/frontend 容器已重建並啟動
- `curl -fsS http://localhost:38080/actuator/health`：通過，回傳 `{"status":"UP"}`
- 瀏覽器商品/分類管理 smoke test：
  - `/pos/products`：通過，可看到「商品管理」、未登入提示與停用的「新增商品」
  - `/pos/categories`：通過，可看到「商品分類管理」、未登入提示與停用的「新增分類」
  - POS 導覽：通過，從收銀頁點擊「商品」與「分類」可進入對應頁面
  - Console / API 4xx 檢查：通過，未捕捉到商品/分類頁未登入自動 API 403
- 母體 `/Users/wei/Desktop/code/模塊化組件/` 同步驗證：
  - `npm run lint`（module/frontend-web）：通過
  - `npm run build`（module/frontend-web）：通過；保留 Vite chunk size > 500 kB 警告
  - `mvn -pl module-common -am test`（module/backend）：通過
  - `mvn -pl app -am test -DskipTests`（module/backend）：通過
- Reference 版本檢查：通過，`scripts/setup-module-reference.sh`、`README.md`、`docs/project-map.md`、`需求/系統規格表.md`、`需求/功能檢驗流程書.md` 已指向 `module-v2026.05.10.3`
- `npm run lint`（frontend-web，收銀台接入商品 API 與 cart store 後）：通過
- `npm run build`（frontend-web，收銀台接入商品 API 與 cart store 後）：通過；保留 Vite chunk size > 500 kB 警告
- `docker compose up -d --build frontend`（docker/local）：通過；frontend 容器已重建並啟動
- 瀏覽器收銀台互動 smoke test：
  - 透過 API 建立測試分類與測試商品後登入狀態開啟 `/pos/register`：通過，可看到真實商品資料
  - 點選商品加入購物車、加減數量、進入 `/pos/checkout`：通過，checkout 顯示同一筆商品與 `$126` 應收金額
  - 未登入狀態開啟 `/pos/register`：通過，顯示登入提示且未捕捉到商品 API 403
- 母體 `/Users/wei/Desktop/code/模塊化組件/` 同步驗證：
  - `npm run lint`（module/frontend-web，ApiResponse 正規化同步後）：通過
  - `npm run build`（module/frontend-web，ApiResponse 正規化同步後）：通過；保留 Vite chunk size > 500 kB 警告
- Reference 版本檢查：通過，`scripts/setup-module-reference.sh`、`README.md`、`docs/project-map.md`、`需求/系統規格表.md`、`需求/功能檢驗流程書.md` 已指向 `module-v2026.05.10.4`

## Git 提交記錄

- 2026-05-10 05:01:51 CST
  - Hash：`904dc25`
  - 訊息：`docs(ai): add Claude Codex handoff workflow`
  - 變更統計：17 files changed, 821 insertions(+), 4 deletions(-)
- 2026-05-10 05:01:59 CST
  - Hash：`f518d7f`
  - 訊息：`docs(devlog): record AI handoff update`
  - 變更統計：1 file changed, 4 insertions(+), 1 deletion(-)
- 2026-05-10 17:24:00 CST
  - Hash：`44d20fc`
  - 訊息：`feat(pos): add product management UI and polish register flow`
  - 變更統計：37 files changed, 1086 insertions(+), 561 deletions(-)
  - 用途：提交 POS UI 中文化、商品/分類管理、API 路徑修正、WebSocket proxy、health endpoint 與 reference baseline 更新

> 維護說明：本次補記日誌後產生的純日誌提交不再另開一筆日誌提交追記，避免 Git 提交記錄形成無限補記循環；最終提交狀態以 `git log` 與遠端 `feature` 分支為準。
