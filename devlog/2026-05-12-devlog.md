# 2026-05-12 Devlog

## Planning

- 依使用者回饋整理 POS 側邊欄資訊架構，減少主選單項目數。
- 將關聯性高的功能合併為主功能群組，保留子功能入口。

## Project Structure

- `frontend-web/src/layouts/PosLayout.tsx`
  - POS 主版面、側邊欄、手機/平板底部導覽與購物車掛載區。
- `devlog/2026-05-12-devlog.md`
  - 今日開發紀錄。

## Progress

- `frontend-web/src/layouts/PosLayout.tsx`
  - 側邊欄主項目整理為：收銀台、訂單、商品、庫存、發票、班次、對帳。
  - `訂單` 群組合併「訂單列表」與「退款處理」。
  - `商品` 群組合併「商品管理」與「分類管理」。
  - 只有目前所在群組或已點開的群組會顯示子項，避免側邊欄長期攤開過多功能。
  - 手機底部快速導覽縮成：收銀台、訂單、商品、庫存。
  - 平板底部導覽改用群組 active 判斷，退款頁會歸屬在訂單群組，分類頁會歸屬在商品群組。

## Verification

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `npm test -- --run`（frontend-web）：通過，5 files / 10 tests passed。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 導覽驗證：
  - 桌面 `/pos/register` 側邊欄只顯示主項目，未攤開退款與分類。
  - 桌面 `/pos/orders`、`/pos/refunds` 會展開「訂單列表 / 退款處理」。
  - 桌面 `/pos/products`、`/pos/categories` 會展開「商品管理 / 分類管理」。
  - 桌面 `/pos/shifts` 回到精簡主項目。
  - 手機底部快速導覽顯示「收銀台 / 訂單 / 商品 / 庫存」，不再單獨顯示退款。
  - 桌面與手機皆無橫向溢出。
  - 截圖輸出：`/tmp/sidebar-grouped-desktop.png`、`/tmp/sidebar-grouped-mobile.png`。

## Update 2026-05-12 02:37

- Bug fix：修正側邊欄群組打開後無法手動收合。
- `frontend-web/src/layouts/PosLayout.tsx`
  - 群組展開狀態改為「目前所在群組預設展開，但使用者手動點擊後可覆蓋」。
  - 點擊已展開且目前 active 的群組主項時，只收合子列表，不重新導頁。
  - 再次點擊同一主項可重新展開。

## Update Verification 2026-05-12 02:37

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `npm test -- --run`（frontend-web）：通過，5 files / 10 tests passed。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 側邊欄互動驗證：
  - `/pos/products` 初始可見「商品管理 / 分類管理」。
  - 點擊「商品」後子項收合，畫面不再出現「商品管理 / 分類管理」。
  - 再點擊「商品」後子項重新展開。
  - `/pos/orders` 初始可見「訂單列表 / 退款處理」。
  - 點擊「訂單」後子項收合，畫面不再出現「訂單列表 / 退款處理」。
  - 再點擊「訂單」後子項重新展開。
  - 截圖輸出：`/tmp/sidebar-collapse-fixed.png`。

## Update 2026-05-12 03:05

- 目標：補完整 POS 庫存功能，讓門店可支援「進貨前點收」與「定期盤點單」。
- `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/controller/StockController.java`
  - 新增 `POST /api/v1/inventory/stock/receive` 進貨驗收入庫端點。
- `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/dto/request/ReceiveStockRequest.java`
  - 新增批次驗收入庫 request DTO，支援多品項實收數量。
- `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/service/StockDeductionService.java`
  - 新增 `receiveBatch`，同一張驗收單以同一個 referenceId 批次寫入 `RECEIVING` 庫存異動。
  - 入庫後重新檢查庫存警示狀態。
- `frontend-web/src/features/pos-inventory/pages/StockOverviewPage.tsx`
  - 將庫存總覽改為操作中樞，加入「進貨驗收」「盤點單」「手動調整」入口。
  - 庫存列表顯示商品名稱與 SKU，不再只顯示 UUID。
  - 補上庫存品項、總庫存量、低庫存、缺貨摘要。
- `frontend-web/src/features/pos-inventory/pages/ReceivingPage.tsx`
  - 新增進貨驗收頁，支援進貨單號、供應商、點收品項、進貨單數量、實收數量、差異摘要與驗收備註。
- `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
  - 盤點頁改為盤點單工作區，顯示帳面庫存、實盤數量、差異、進度、未盤/已盤/差異摘要。
  - 完成盤點前若仍有未盤品項會提示未盤品項不會調整庫存。
- `frontend-web/src/App.tsx`
  - 補上 `/pos/inventory/receiving` 與 `/pos/inventory/stock-takes` 路由。
- `frontend-web/src/layouts/PosLayout.tsx`
  - 「庫存」改為群組，包含「庫存總覽 / 進貨驗收 / 盤點單」。
- `frontend-web/src/features/pos-inventory/api/inventoryApi.ts`
  - 修正 transfer / stockTake API response 型別，新增 `stockApi.receive`。

## Update Verification 2026-05-12 03:05

- `npx tsc -b`（frontend-web）：通過。
- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `mvn -pl module-pos-inventory -am -Dtest=StockControllerTest -Dsurefire.failIfNoSpecifiedTests=false test`：通過，3 tests passed。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build backend frontend`：通過，backend/frontend 容器重建完成。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：Hook warning 修正後再次重建 frontend，通過。
- `curl http://localhost:38080/actuator/health`：通過，回傳 `{"status":"UP"}`。
- 瀏覽器互動驗證（Chrome CDP，實測 Docker `http://127.0.0.1:38082`）：
  - 桌面 `/pos/inventory` 可看到「庫存總覽 / 進貨驗收 / 盤點單 / 手動調整」，且 API 正常載入庫存資料。
  - 桌面 `/pos/inventory/receiving` 可看到「進貨驗收 / 進貨單號 / 供應商 / 點收品項 / 完成驗收入庫 / 驗收摘要」。
  - 桌面 `/pos/inventory/stock-takes` 可看到「盤點單 / 建立盤點單 / 帳面數 / 實盤數」。
  - 手機尺寸 `/pos/inventory` 顯示庫存操作入口與底部快速導覽，無水平溢出。
  - 手機尺寸 `/pos/inventory/receiving` 表單可垂直滾動，無水平溢出。
  - 截圖輸出：`/tmp/pos-docker-inventory-desktop.png`、`/tmp/pos-docker-receiving-desktop.png`、`/tmp/pos-docker-stocktake-desktop.png`、`/tmp/pos-docker-inventory-mobile.png`、`/tmp/pos-docker-receiving-mobile.png`。

## Update 2026-05-12 03:25

- Bug fix：修正點擊「建立盤點單」後畫面仍顯示「尚無盤點單」的問題。
- `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/entity/StockTakeItem.java`
  - 在 `stockTake` 父層關聯加上 `@JsonIgnore`，避免 `StockTake -> items -> stockTake` JSON 無限遞迴造成 API 500。
- `backend/module-pos-inventory/src/test/java/com/enterprise/inventory/controller/StockTakeControllerTest.java`
  - 新增盤點單列表序列化測試，確認明細不會回傳父層 `stockTake`。
- `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
  - 載入與建立盤點單失敗時顯示後端實際錯誤訊息。
  - 建立失敗後重新載入盤點列表，若已存在進行中盤點單會立即顯示在畫面上。

## Update Verification 2026-05-12 03:25

- `mvn -pl module-pos-inventory -am test -Dtest=StockControllerTest,StockTakeControllerTest -Dsurefire.failIfNoSpecifiedTests=false`：通過，4 tests passed。
- `npm run lint`（frontend-web）：通過。
- `npx tsc -b`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml up -d --build backend frontend`：通過，backend/frontend 容器重建完成。
- `curl GET /api/v1/inventory/stock-takes/stores/{storeId}`：通過，既有 `IN_PROGRESS` 盤點單正常回傳 4 個品項，不再出現 `StackOverflowError`。
- Chrome CDP 瀏覽器互動驗證：
  - `/pos/inventory/stock-takes` 顯示「已有進行中盤點」與「盤點基準」。
  - 頁面顯示 4 個實盤數量輸入欄與「登記」按鈕。
  - 未再顯示「尚無盤點單」或載入錯誤。
  - 截圖輸出：`/tmp/pos-stocktake-fixed.png`。
