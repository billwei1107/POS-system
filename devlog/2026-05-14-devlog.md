# 2026-05-14 Devlog

## Planning

- 進入後端正式化階段，優先完成 POS 權限底座，避免退款、庫存盤點、調撥與庫存調整等敏感操作仍可被任意登入使用者呼叫。
- 補齊角色 / 權限關聯查詢、JWT 角色解析、`@RequirePermission` AOP 守衛與 `@Auditable` 稽核紀錄。

## Project Structure

- `backend/module-common/`
  - JWT 產生、驗證、角色 claim 解析與 SecurityContext 工具。
- `backend/module-auth/`
  - 使用者、角色、權限、PIN 登入與 POS 角色權限種子資料。
  - 新增 `aspect/`：權限與稽核 AOP。
  - 新增 `repository/UserRoleRepository.java`、`repository/RolePermissionRepository.java`：RBAC 關聯查詢。
  - 新增 `service/RoleResolverService.java`、`service/PermissionGuardService.java`：登入角色解析與權限守衛。
- `backend/module-pos-core/`
  - POS 訂單與退款後端；退款 API 已套用權限與稽核註解。
- `backend/module-pos-inventory/`
  - POS 庫存、進貨驗收、盤點單與調撥後端；庫存寫入類 API 已套用權限與稽核註解。
- `backend/app/`
  - 本地 POS demo seed，已補 demo 操作員角色指派。
- `devlog/troubleshooting.md`
  - 編譯問題與修復紀錄。

## Progress

- `JwtTokenProvider` 新增 `getRoleFromJWT()`；`JwtAuthenticationFilter` 會將 JWT role 寫入 authorities。
- `SecurityUtils` 新增 `getCurrentRole()`，供權限與稽核 AOP 讀取目前角色。
- `AuthServiceImpl` 與 `PosAuthServiceImpl` 改為依使用者角色關聯產生 JWT role，不再固定寫死 `USER` / `CASHIER`。
- 修正 `PosAuthServiceImpl.managerOverride()` 原本只檢查角色存在、不檢查使用者是否擁有該角色的問題。
- `DataSeeder` 固定建立 `SUPER_ADMIN` 與 admin 角色指派，不再受既有 POS 角色影響。
- 本地 demo 操作員同時指派 `CASHIER` 與 `STORE_MANAGER`，讓瀏覽器測試可操作退款、庫存、盤點與調撥，不會被正式權限守衛擋住。
- `PosDataSeeder` 補齊 POS 權限與角色權限關聯：
  - `pos:inventory:receive`
  - `pos:inventory:stock-take`
  - `pos:inventory:transfer`
- 敏感操作已掛上 `@RequirePermission` 與 `@Auditable`：
  - 退款建立 / 完成
  - 庫存調整 / 進貨驗收 / 警示確認
  - 盤點單開始 / 登記 / 完成 / 取消
  - 調撥建立 / 核准 / 出貨 / 收貨 / 取消
- 新增 `PermissionGuardServiceTest`，覆蓋允許、拒絕與 SUPER_ADMIN 繞過權限表三種情境。

## Verification

- `mvn test -pl module-common,module-auth -am`：通過。
- `mvn test -pl module-pos-core,module-pos-inventory -am`：通過。
- `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組編譯 / 測試成功。
- Docker / OrbStack：
  - 原本 `38080/38082` 仍由 `financial-accounting-backend/frontend` 佔用，POS 沿用替代端口 `38180/38182`。
  - 使用 Docker Compose override 重建並啟動 `pos-backend`、`pos-frontend`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
- 權限 API 驗證：
  - `POST /api/v1/pos/auth/pin-login`：200，demo 操作員 role 回 `STORE_MANAGER`。
  - `GET /api/v1/inventory/stores/{storeId}/stock`：200，取得 4 筆 demo 庫存。
  - `POST /api/v1/inventory/stock-takes/start`：200，受 `pos:inventory:stock-take` 保護的盤點建立成功。
  - 測試盤點單已立即呼叫 cancel API 取消。

## Backend Entry Fix

- 問題：直接開啟 `http://127.0.0.1:38180/` 會得到 403，容易誤判為後端無法進入。
- 變更：
  - `backend/module-common/src/main/java/com/enterprise/common/config/SecurityConfig.java`
    - 將 `/` 與 `/api` 設為公開可讀。
  - `backend/app/src/main/java/com/enterprise/controller/BackendInfoController.java`
    - 新增後端入口資訊，回傳 service、status、health、apiBase、frontend 與 +8 timestamp。
- 驗證：
  - `curl -i http://127.0.0.1:38180/`：200，回 `Titanium POS Backend` 與 `status=UP`。
  - `curl -i http://127.0.0.1:38180/api`：200，回 API base 與健康檢查路徑。
  - `curl -i http://127.0.0.1:38180/actuator/health`：200，回 `{"status":"UP"}`。

## API Timestamp Timezone Fix

- 問題：後端入口 `/api` 的 `data.timestamp` 使用 `+08:00`，但全域 `ApiResponse.timestamp` 仍是容器 UTC / 無 offset 字串，畫面上會同時出現 `2026-05-14T01:xx+08:00` 與 `2026-05-13T17:xx`。
- 變更：
  - `backend/module-common/src/main/java/com/enterprise/common/dto/ApiResponse.java`
    - 將全域 timestamp 型別改為 `OffsetDateTime`，並固定使用 `Asia/Taipei`。
  - `backend/module-common/src/test/java/com/enterprise/common/dto/ApiResponseTest.java`
    - 補上 `+08:00` offset 斷言。
  - `backend/app/src/main/java/com/enterprise/controller/BackendInfoController.java`
    - 入口資訊改為 `serverTime` 與 `timezone`，避免內外層都叫 `timestamp`。
- 驗證：
  - `mvn test -pl module-common,app -am`：通過。
  - Docker / OrbStack 重建 `pos-backend`：成功。
  - `curl -s http://127.0.0.1:38180/api`：回 `serverTime=2026-05-14T01:57:07...+08:00`，外層 `timestamp=2026-05-14T01:57:07...+08:00`。

## Admin Console Routing

- 目標：將 POS 收銀台與後台管理入口拆開，避免使用者誤以為後端 API JSON 就是後台介面。
- 變更：
  - `frontend-web/src/layouts/AdminLayout.tsx`
    - 新增後台管理殼層，包含側邊導覽、通知、收銀台入口與登出。
  - `frontend-web/src/features/admin/pages/AdminDashboardPage.tsx`
    - 新增 `/admin/dashboard` 後台首頁與常用入口。
  - `frontend-web/src/App.tsx`
    - 新增 `/admin/*` 後台路由群組。
    - 保留舊路由轉址，例如 `/role` 轉至 `/admin/access/roles`。
    - POS 路由維持 `/pos/*`，不影響收銀流程。
- 驗證：
  - `npm run build`：通過。
  - Docker / OrbStack 重建 `pos-frontend`：成功。
  - `curl -I http://127.0.0.1:38182/admin`：200，SPA fallback 正常。
  - Playwright + Chrome 實測：
    - `/admin` 未登入導向 `/login?redirect=%2Fadmin`。
    - 使用 `admin / 123456` 登入後進入 `/admin/dashboard`。
    - `/role` 轉址至 `/admin/access/roles`。
    - `/pos/register` 仍可進入收銀台。

## Admin POS Management Routes

- 目標：讓後台介面可以集中管理 POS 相關功能，而不是只提供組織、請假與流程模組。
- 變更：
  - `frontend-web/src/layouts/AdminLayout.tsx`
    - 後台側邊欄新增 POS 管理、庫存與營運群組。
  - `frontend-web/src/features/admin/pages/AdminDashboardPage.tsx`
    - 後台首頁新增 POS、庫存、營運常用入口。
  - `frontend-web/src/App.tsx`
    - 新增商品、分類、訂單、退款、庫存、進貨、盤點、調撥、發票、稅別、班次、Z 報表、對帳、支付方式與金流設定的 `/admin/*` 路由。
    - 補齊舊 POS 管理路由轉址到新的後台路徑。
  - `frontend-web/src/features/pos-inventory/pages/TransferPage.tsx`
    - 改用共用 `DEFAULT_STORE_ID`，避免環境變數缺省時請求 `/stores/undefined`。
  - `frontend-web/src/features/pos-payment/pages/PayMethodSettingsPage.tsx`
    - 改用共用 `DEFAULT_STORE_ID`，避免支付方式 API 因缺少門店 ID 回 500。
- 驗證：
  - `npm run build`：通過，僅有既有 bundle size warning。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-frontend`：成功。
  - Playwright + Chrome 實測：
    - `/admin` 未登入導向 `/login?redirect=%2Fadmin`。
    - 使用 `admin / 123456` 登入後進入 `/admin/dashboard`。
    - `/admin/pos/products`、`/admin/inventory/transfers`、`/admin/operations/pay-methods` 與 `/pos/register` 均可正常開啟。
    - 瀏覽器網路監聽：無 4xx / 5xx。
    - Console / page error：無錯誤。

## Persistent Audit Log API

- 目標：讓 `@Auditable` 敏感操作不只輸出應用程式 log，也能寫入資料庫並透過受保護 API 查詢。
- 變更：
  - `backend/module-auth/src/main/java/com/enterprise/auth/entity/AuditLog.java`
    - 新增 `auth_audit_logs` 對應 entity，記錄 module、action、method、userId、role、status、error、duration 與 occurredAt。
  - `backend/module-auth/src/main/resources/db/migration/V20260514130500__create_auth_audit_logs.sql`
    - 新增稽核資料表與 module/action、user/time、status/time 索引。
    - 使用時間戳版本號，避免在既有資料庫中被 Flyway 視為 out-of-order migration。
  - `backend/module-auth/src/main/java/com/enterprise/auth/service/AuditLogService.java`
    - 新增 REQUIRES_NEW 寫入，避免稽核寫入失敗破壞原本業務交易。
    - 新增分頁搜尋，可依 module、action、status、userId、from、to 過濾。
  - `backend/module-auth/src/main/java/com/enterprise/auth/aspect/AuditAspect.java`
    - 成功與失敗都寫入稽核表，仍保留原本 structured application log。
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/AuditLogController.java`
    - 新增 `GET /api/v1/audit-logs`，並使用 `@RequirePermission("system:audit:read")` 保護。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:audit:read` 權限，並指派給 `AREA_MANAGER`；`SUPER_ADMIN` 仍由權限守衛 bypass。
  - `backend/module-auth/src/test/java/com/enterprise/auth/service/AuditLogServiceTest.java`
    - 補欄位裁切、時間與 duration 正規化測試。
- 驗證：
  - `mvn test -pl module-auth -am`：通過。
  - `mvn test -pl app -am`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - `curl http://127.0.0.1:38180/api`：回 `serverTime` 與外層 `timestamp` 均為 `+08:00`。
  - API 實測：
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `POST /api/v1/inventory/stock-takes/start`：200，觸發 `inventory-stock-take:start` 稽核。
    - `GET /api/v1/audit-logs?module=inventory-stock-take&page=0&size=5`：200，查到 `start:SUCCESS`。
    - 測試盤點單已呼叫 cancel，查詢稽核可看到 `cancel:SUCCESS,start:SUCCESS`。
  - `git diff --check`：通過。

## Admin Audit Log Page

- 目標：將後端稽核紀錄查詢能力接入管理後台，讓管理者可以直接檢視敏感操作軌跡。
- 變更：
  - `frontend-web/src/features/admin/api/auditApi.ts`
    - 新增 `auditApi.search()`，對應 `GET /api/v1/audit-logs`。
  - `frontend-web/src/features/admin/types/index.ts`
    - 新增 `AuditLog`、`AuditLogQuery` 與後台分頁回應型別。
  - `frontend-web/src/features/admin/pages/AuditLogPage.tsx`
    - 新增稽核紀錄頁，支援模組、狀態、動作代碼篩選與分頁。
    - 顯示符合筆數、本頁成功、本頁失敗、平均耗時與表格明細。
  - `frontend-web/src/layouts/AdminLayout.tsx`
    - 在「權限與流程」群組新增「稽核紀錄」入口。
  - `frontend-web/src/features/admin/pages/AdminDashboardPage.tsx`
    - 後台首頁新增稽核紀錄常用入口。
  - `frontend-web/src/App.tsx`
    - 新增 `/admin/access/audit-logs` 路由與 `/audit-logs` 舊路由轉址。
- 驗證：
  - `npm run build`：通過，僅有既有 bundle size warning。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-frontend`：成功。
  - Playwright + Chrome 實測：
    - `/admin/access/audit-logs` 未登入時導向 `/login?redirect=%2Fadmin%2Faccess%2Faudit-logs`。
    - 使用 `admin / 123456` 登入後回到 `/admin/access/audit-logs`。
    - 稽核 API 回 `200`，頁面顯示符合筆數 2。
    - 表格可見 `inventory-stock-take` 的 `cancel:成功` 與 `start:成功`。
    - 瀏覽器網路監聽：無 4xx / 5xx。
    - Console / page error：無錯誤。

## Admin RBAC Permission Summary

- 時間：2026-05-14 14:21:46 CST
- 目標：補完整後台角色權限檢視，讓管理者能確認角色實際擁有哪些權限，而不是只看到角色列表。
- 變更：
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/PermissionResponse.java`
    - 新增權限回應 DTO，輸出 name、code、type、resource、action。
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/RolePermissionSummaryResponse.java`
    - 新增角色權限摘要 DTO，整合角色基本資料與已指派權限清單。
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/RoleController.java`
    - 新增 `GET /api/v1/roles/permission-summaries`，並以 `system:rbac:read` 保護。
  - `backend/module-auth/src/main/java/com/enterprise/auth/service/impl/RoleServiceImpl.java`
    - 依角色查詢已指派權限，並依 role code 與 permission code 排序輸出。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 補齊 POS 庫存、稽核與 RBAC 權限 seed，並建立常用角色權限對應。
  - `frontend-web/src/features/auth/api/authApi.ts`
    - 新增 `roleApi.getPermissionSummaries()`，對接新後端端點。
  - `frontend-web/src/features/auth/types/index.ts`
    - 新增 `RolePermissionSummary` 與 `Permission.resource` 型別。
  - `frontend-web/src/features/auth/pages/RoleListPage.tsx`
    - 調整後台深色介面標題與說明文字，避免低對比文字。
  - `frontend-web/src/features/auth/components/RolePermissionTree.tsx`
    - 重做角色權限摘要 UI，新增統計卡、角色卡、權限群組 chips。
    - `SUPER_ADMIN` 改顯示「全部權限」，避免因後端 bypass 機制造成 0 權限誤解。
- 驗證：
  - `mvn test -pl module-auth -am`：通過。
  - `mvn test -pl app -am`：通過。
  - `npm run build`：通過，僅有既有 bundle size warning。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend` 與 `pos-frontend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API 實測：
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/roles/permission-summaries`：200，回傳角色與權限摘要。
  - Playwright + Chrome 實測：
    - `/admin/access/roles` 未登入時導向登入頁。
    - 使用 `admin / 123456` 登入後回到 `/admin/access/roles`。
    - 頁面顯示角色總數、可用權限、角色卡與 `system:rbac:read`。
    - `SUPER_ADMIN` 顯示「全部權限」。
    - 瀏覽器網路監聽：無 4xx / 5xx。
    - Console / page error：無錯誤。

## Admin RBAC Permission Editing

- 時間：2026-05-14 15:46:46 CST
- 目標：讓後台角色權限頁從唯讀摘要升級為可編輯管理，管理者能直接調整角色完整權限清單。
- 變更：
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/UpdateRolePermissionsRequest.java`
    - 新增角色權限更新請求 DTO，承載完整 permission id 清單。
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/RoleController.java`
    - 新增 `PUT /api/v1/roles/{id}/permissions`，使用 `system:rbac:manage` 保護，並以 `@Auditable` 記錄變更。
  - `backend/module-auth/src/main/java/com/enterprise/auth/service/impl/RoleServiceImpl.java`
    - 新增角色權限替換流程：驗證角色、禁止直接修改 `SUPER_ADMIN`、驗證權限 ID 存在、刪除舊關聯後寫入新關聯。
  - `backend/module-auth/src/main/java/com/enterprise/auth/repository/RolePermissionRepository.java`
    - 新增依角色刪除權限關聯方法。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:rbac:manage` 權限 seed，保留 `SUPER_ADMIN` 以系統策略 bypass 管理。
  - `backend/module-auth/src/test/java/com/enterprise/auth/service/impl/RoleServiceImplTest.java`
    - 補角色權限替換、缺失權限 ID 回滾保護與 `SUPER_ADMIN` 禁止修改測試。
  - `frontend-web/src/features/auth/api/authApi.ts`
    - 新增取得所有權限與更新角色權限 API。
  - `frontend-web/src/features/auth/types/index.ts`
    - 補齊 `Permission.resource`、角色權限摘要與更新請求型別。
  - `frontend-web/src/features/auth/components/RolePermissionTree.tsx`
    - 角色卡新增「編輯權限」入口。
    - 新增權限編輯對話框，依權限類型分組顯示 checkbox，儲存時覆蓋該角色完整權限集合。
    - `SUPER_ADMIN` 編輯按鈕停用，避免與系統全權限 bypass 規則衝突。
- 驗證：
  - `mvn test -pl module-auth -am`：通過。
  - `mvn test -pl app -am`：通過。
  - `npm run build`：通過，僅有既有 bundle size warning。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend` 與 `pos-frontend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API 實測：
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/roles/permission-summaries`：200。
    - `GET /api/v1/permissions`：200。
    - `PUT /api/v1/roles/{cashierId}/permissions`：可新增 `pos:order:void`，再次呼叫可恢復原權限。
  - Playwright + Chrome 實測：
    - `/admin/access/roles` 可登入進入。
    - 點擊 `CASHIER` 的「編輯權限」可開啟權限對話框。
    - 勾選 `pos:order:void` 後儲存，畫面顯示成功訊息且角色卡出現該權限。
    - 重新開啟對話框可看到 checkbox 已保持勾選。
    - 取消勾選並儲存後，角色卡恢復為原權限集合。
    - 瀏覽器網路監聽：無 4xx / 5xx。
    - Console / page error：無錯誤。

## Admin User Role Management

- 時間：2026-05-14 15:56:52 CST
- 目標：補齊後台帳號角色管理能力，讓 RBAC 不只可管理角色權限，也能管理使用者擁有哪些角色。
- 變更：
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/UserResponse.java`
    - 新增安全使用者回應 DTO，不回傳 `passwordHash`。
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/RoleResponse.java`
    - 新增安全角色回應 DTO。
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/UserRoleSummaryResponse.java`
    - 新增使用者與角色指派摘要 DTO。
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/UpdateUserRolesRequest.java`
    - 新增使用者角色覆蓋更新請求 DTO。
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/UserController.java`
    - 新增 `GET /api/v1/users`、`GET /api/v1/users/{id}` 與 `PUT /api/v1/users/{id}/roles`。
    - 使用 `system:user:read` / `system:user:manage` 保護讀取與管理操作。
    - 建立使用者與更新角色會寫入 audit log。
  - `backend/module-auth/src/main/java/com/enterprise/auth/service/impl/UserServiceImpl.java`
    - 新增使用者角色摘要查詢與角色覆蓋更新。
    - 更新角色時驗證 role ID 存在。
    - 防止移除最後一位 `SUPER_ADMIN` 使用者，避免後台鎖死。
  - `backend/module-auth/src/main/java/com/enterprise/auth/repository/UserRoleRepository.java`
    - 新增依角色查詢、依使用者刪除與角色計數方法。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:user:read` 與 `system:user:manage` 權限 seed。
  - `frontend-web/src/features/auth/api/authApi.ts`
    - 新增 `userApi` 與角色列表 API。
  - `frontend-web/src/features/auth/components/UserTable.tsx`
    - 改接新使用者角色摘要 API。
    - 新增角色 chips、帳號狀態中文顯示與角色編輯對話框。
    - 調整深色 UI 對比與按鈕尺寸，避免文字斷行。
  - `frontend-web/src/features/auth/pages/UserListPage.tsx`
    - 重整為後台深色帳號管理頁。
  - `frontend-web/src/App.tsx`、`frontend-web/src/layouts/AdminLayout.tsx`
    - 新增 `/admin/access/users` 路由與側邊欄入口。
- 驗證：
  - `mvn test -pl module-auth -am`：通過。
  - `mvn test -pl app -am`：通過。
  - `npm run build`：通過，僅有既有 bundle size warning。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend` 與 `pos-frontend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API 實測：
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/users`：200，回傳帳號角色摘要。
    - `GET /api/v1/roles`：200。
    - `PUT /api/v1/users/{adminId}/roles` 使用原 `SUPER_ADMIN` 角色集合：200。
    - 使用者清單 JSON 不含 `passwordHash`。
  - Playwright + Chrome 實測：
    - `/admin/access/users` 未登入時可經登入後回到帳號管理頁。
    - 頁面顯示 `admin`、`cashier` 與角色 chips。
    - 點擊「編輯角色」可開啟角色對話框。
    - 對話框可看到 `SUPER_ADMIN` 角色。
    - 按鈕文字不再斷行。
    - 瀏覽器網路監聽：無 4xx / 5xx。
    - Console / page error：無錯誤。

## Admin Account Lifecycle Management

- 時間：2026-05-14 16:48:11 CST
- 目標：將後台帳號管理從角色指派補齊為完整帳號生命週期管理，包含建立帳號、停用 / 啟用與重設密碼。
- 變更：
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/CreateUserRequest.java`
    - 新增後台建立帳號請求 DTO，支援帳號、密碼、Email、手機與初始角色。
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/UpdateUserStatusRequest.java`
    - 新增帳號狀態更新 DTO，限制後台只可切換 `ACTIVE` / `INACTIVE`。
  - `backend/module-auth/src/main/java/com/enterprise/auth/dto/ResetUserPasswordRequest.java`
    - 新增重設密碼 DTO。
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/UserController.java`
    - 新增 `POST /api/v1/users`、`PUT /api/v1/users/{id}/status`、`PUT /api/v1/users/{id}/password`。
    - 管理操作使用 `system:user:manage` 保護，並以 `@Auditable` 記錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/service/impl/UserServiceImpl.java`
    - 新增後台建立帳號、停用 / 啟用與重設密碼流程。
    - 停用帳號會防止關閉最後一位啟用中的 `SUPER_ADMIN`。
    - 啟用與重設密碼會清除鎖定時間與登入失敗次數。
  - `backend/module-auth/src/test/java/com/enterprise/auth/service/impl/UserServiceImplTest.java`
    - 補建立帳號、停用最後一位系統管理員防護、重設密碼清除鎖定狀態測試。
  - `frontend-web/src/features/auth/api/authApi.ts`、`frontend-web/src/features/auth/types/index.ts`
    - 新增帳號建立、狀態更新與重設密碼 API 型別與呼叫方法。
  - `frontend-web/src/features/auth/components/UserTable.tsx`
    - 新增「新增帳號」對話框、初始角色選取、重設密碼對話框與帳號啟用 / 停用操作。
    - 操作欄改為高對比 icon buttons，並縮短 UUID 顯示，避免表格在 1440px 後台寬度下被裁切。
- 驗證：
  - `mvn test -pl module-auth -am`：通過。
  - `mvn test -pl app -am`：通過。
  - `npm run build`：通過，僅有既有 bundle size warning。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend` 與 `pos-frontend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API 實測：
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `POST /api/v1/users`：200，建立測試帳號並指派 `CASHIER`。
    - `PUT /api/v1/users/{id}/password`：200，重設密碼成功。
    - `PUT /api/v1/users/{id}/status` 停用：200。
    - 停用帳號登入：403，符合預期。
    - 啟用後使用新密碼登入：200。
    - 測試帳號最後已停用收尾。
  - Chrome CDP 瀏覽器實測：
    - `/admin/access/users` 未登入時可經登入後回到帳號管理頁。
    - 新增帳號、選取初始角色、重設密碼、停用、啟用、再停用，全流程成功。
    - 瀏覽器網路監聽：無非預期 4xx / 5xx。
    - 版面檢查：`document.documentElement.scrollWidth === clientWidth`，表格不再水平溢出，操作欄完整可見。

## Admin User Pagination And Filters

- 時間：2026-05-14 20:10:12 CST
- 目標：避免後台帳號管理一次載入全部帳號，補齊後端分頁、關鍵字搜尋、狀態篩選與角色篩選，讓正式資料量增加後仍可穩定操作。
- 變更：
  - `backend/module-auth/src/main/java/com/enterprise/auth/repository/UserRepository.java`
    - 擴充 `JpaSpecificationExecutor<User>`，支援動態條件查詢。
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/UserController.java`
    - `GET /api/v1/users` 改為分頁回應，支援 `keyword`、`status`、`roleId`、`page`、`size` 查詢參數。
  - `backend/module-auth/src/main/java/com/enterprise/auth/service/impl/UserServiceImpl.java`
    - 新增帳號搜尋規格，支援帳號 / Email / 手機關鍵字、狀態與角色子查詢。
    - 限制分頁大小在安全範圍內，避免異常參數造成過大查詢。
  - `backend/module-auth/src/test/java/com/enterprise/auth/service/impl/UserServiceImplTest.java`
    - 補分頁帳號角色摘要查詢測試。
  - `frontend-web/src/features/auth/api/authApi.ts`、`frontend-web/src/features/auth/types/index.ts`
    - 新增帳號查詢參數與分頁回應型別。
  - `frontend-web/src/features/auth/components/UserTable.tsx`
    - 新增帳號搜尋、帳號狀態篩選、角色篩選、清除篩選、每頁筆數與分頁切換。
    - 建立帳號、角色更新、狀態切換與密碼重設後改為重新抓取目前條件下的分頁資料。
- 驗證：
  - `mvn test -pl module-auth -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組編譯 / 測試成功。
  - `npx tsc -b`：通過。
  - `npm run build`：通過，僅有既有 bundle size warning。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend` 與 `pos-frontend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API 實測：
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/users?page=0&size=2`：200，回傳分頁 metadata。
    - `GET /api/v1/users?status=ACTIVE&page=0&size=20`：200，所有資料皆為啟用帳號。
    - `GET /api/v1/users?keyword=admin&page=0&size=10`：200，可查到 admin。
    - `GET /api/v1/users?roleId={cashierId}&page=0&size=20`：200，所有資料皆包含 CASHIER 角色。
  - Chrome CDP 瀏覽器實測：
    - `/admin/access/users` 可登入並載入帳號管理頁。
    - 狀態下拉可選取「停用」。
    - 前端確實送出 `/api/v1/users?...status=INACTIVE...`，回應 200。
    - 表格顯示 4 筆帳號且狀態全為「停用」。
    - 瀏覽器網路監聽：無非預期 4xx / 5xx。

## Organization Backend Permission Hardening

- 時間：2026-05-14 20:47:42 CST
- 目標：補齊組織管理後端正式化所需的權限控管、操作稽核、軟刪除過濾與列表 API 行為。
- 變更：
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/CompanyController.java`
    - 公司讀取 API 加上 `system:organization:read` 權限，建立 / 更新 / 刪除加上 `system:organization:manage` 與稽核紀錄。
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/DepartmentController.java`
    - 部門讀取 API 加上 `system:organization:read` 權限，建立 / 更新 / 刪除加上 `system:organization:manage` 與稽核紀錄。
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/PositionController.java`
    - 職位讀取 API 加上 `system:organization:read` 權限，建立 / 更新 / 刪除加上 `system:organization:manage` 與稽核紀錄。
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/EmployeeController.java`
    - 員工讀取 API 加上 `system:organization:read` 權限，建立 / 更新 / 離職 / 刪除加上 `system:organization:manage` 與稽核紀錄。
    - `GET /api/v1/employees` 未帶 `companyId` 時改為回傳全部可見員工，避免後台初始化無法取得列表。
  - `backend/module-organization/src/main/java/com/enterprise/organization/entity/Company.java`
  - `backend/module-organization/src/main/java/com/enterprise/organization/entity/Department.java`
  - `backend/module-organization/src/main/java/com/enterprise/organization/entity/Position.java`
  - `backend/module-organization/src/main/java/com/enterprise/organization/entity/Employee.java`
    - 新增 Hibernate 軟刪除與 `deleted_at IS NULL` 查詢限制，刪除後不再出現在一般列表。
  - `backend/module-organization/src/main/java/com/enterprise/organization/service/EmployeeService.java`
  - `backend/module-organization/src/main/java/com/enterprise/organization/service/impl/EmployeeServiceImpl.java`
    - 新增 `listAll()`，供後台無公司篩選時取得全部可見員工。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:organization:read` 與 `system:organization:manage` 權限 seed，並指派給 `AREA_MANAGER`。
  - `backend/module-organization/src/test/java/com/enterprise/organization/service/impl/EmployeeServiceImplTest.java`
    - 補 `listAll()` repository 呼叫測試。
  - `backend/module-organization/src/test/java/com/enterprise/organization/controller/OrganizationControllerSecurityTest.java`
    - 新增反射測試，確認組織控制器權限與稽核 annotation 存在。
- 驗證：
  - `mvn test -pl module-organization -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/companies`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/companies`：200。
    - `GET /api/v1/employees` 未帶 `companyId`：200，回傳 1 筆員工。
    - `POST /api/v1/positions` 建立測試職位：200。
    - `GET /api/v1/positions` 可查到測試職位。
    - `DELETE /api/v1/positions/{id}`：200。
    - 刪除後 `GET /api/v1/positions` 不再出現該測試職位，確認軟刪除查詢過濾生效。
    - `GET /api/v1/audit-logs?module=organization-position&page=0&size=10`：200，可查到 `create` 與 `delete` 稽核動作。

## Product Backend Permission Hardening

- 時間：2026-05-14 20:52:56 CST
- 目標：補齊商品與分類後端正式化所需的權限控管與操作稽核，避免商品主檔可被任意登入使用者改動。
- 變更：
  - `backend/module-pos-product/src/main/java/com/enterprise/product/controller/ProductItemController.java`
    - 商品列表、詳情與 SKU 查詢加上 `pos:product:read` 權限。
    - 商品建立 / 更新 / 刪除加上 `pos:product:manage` 權限與 `pos-product` 稽核紀錄。
  - `backend/module-pos-product/src/main/java/com/enterprise/product/controller/ProductCategoryController.java`
    - 分類列表、根分類、子分類與詳情查詢加上 `pos:product:read` 權限。
    - 分類建立 / 更新 / 刪除加上 `pos:product:manage` 權限與 `pos-product-category` 稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `pos:product:read` 與 `pos:product:manage` 權限 seed。
    - `CASHIER`、`SHIFT_MANAGER`、`STORE_MANAGER`、`AREA_MANAGER` 指派商品讀取權限，避免收銀台商品載入被權限守衛擋住。
    - `STORE_MANAGER`、`AREA_MANAGER` 指派商品管理權限。
  - `backend/module-pos-product/src/test/java/com/enterprise/product/controller/ProductControllerSecurityTest.java`
    - 新增商品與分類 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-pos-product -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/pos/products?page=0&size=1`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/pos/products?page=0&size=5`：200。
    - `GET /api/v1/pos/categories`：200。
    - `POST /api/v1/pos/products` 建立測試商品：201。
    - `GET /api/v1/pos/products/by-sku/{sku}` 可查到測試商品。
    - `DELETE /api/v1/pos/products/{id}`：200。
    - 刪除後 `GET /api/v1/pos/products/by-sku/{sku}`：404，確認軟刪除查詢過濾生效。
    - `GET /api/v1/audit-logs?module=pos-product&page=0&size=10`：200，可查到 `create` 與 `delete` 稽核動作。

## Payment Backend Permission Hardening

- 時間：2026-05-14 21:05:47 CST
- 目標：補齊付款、錢櫃與對帳後端正式化所需的權限控管與操作稽核，避免付款設定與營運金流資料可被未授權存取或異動。
- 變更：
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/controller/PaymentController.java`
    - 付款處理加上 `pos:payment:process` 權限與 `pos-payment` 稽核紀錄。
    - 付款紀錄與付款方式查詢加上 `pos:payment:read` 權限。
    - 付款方式建立 / 停用加上 `pos:payment:method-manage` 權限與 `pos-pay-method` 稽核紀錄。
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/controller/CashDrawerController.java`
    - 錢櫃查詢加上 `pos:cash-drawer:read` 權限。
    - 開櫃 / 關櫃加上 `pos:cash-drawer:manage` 權限與 `pos-cash-drawer` 稽核紀錄。
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/controller/ReconciliationController.java`
    - 對帳查詢加上 `pos:reconciliation:read` 權限。
    - 對帳產生 / 確認加上 `pos:reconciliation:manage` 權限與 `pos-reconciliation` 稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增付款、錢櫃與對帳相關權限 seed。
    - 依角色職責分配：收銀員可處理 / 查詢付款與查詢錢櫃；班主管可管理錢櫃並查看對帳；店長與區域主管可管理付款方式與對帳。
  - `backend/module-pos-payment/src/test/java/com/enterprise/payment/controller/PaymentControllerSecurityTest.java`
    - 新增付款、錢櫃與對帳 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-pos-payment -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/pos/payments/methods?storeId={storeId}`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/pos/payments/methods?storeId={storeId}`：200。
    - `POST /api/v1/pos/payments/methods` 建立測試付款方式：200。
    - `DELETE /api/v1/pos/payments/methods/{id}` 停用測試付款方式：200。
    - 停用後再次查詢啟用付款方式列表，已不再出現該付款方式。
    - `GET /api/v1/pos/reconciliation?storeId={storeId}&date=2026-05-14`：200。
    - `GET /api/v1/audit-logs?module=pos-pay-method&page=0&size=10`：200，可查到 `create` 與 `deactivate` 稽核動作。

## Order Backend Permission Hardening

- 時間：2026-05-14 21:11:13 CST
- 目標：補齊訂單、結帳完成、作廢與掛單後端正式化所需的權限控管與操作稽核，避免訂單資料與掛單狀態被未授權存取或異動。
- 變更：
  - `backend/module-pos-core/src/main/java/com/enterprise/core/controller/OrderController.java`
    - 建立訂單加上 `pos:order:create` 權限與 `pos-order:create` 稽核紀錄。
    - 單筆訂單與訂單列表查詢加上 `pos:order:read` 權限。
    - 完成結帳加上 `pos:payment:process` 權限與 `pos-order:complete` 稽核紀錄。
    - 作廢訂單加上 `pos:order:void` 權限與 `pos-order:void` 稽核紀錄。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/controller/HeldOrderController.java`
    - 建立掛單與刪除掛單加上 `pos:order:create` 權限與 `pos-held-order` 稽核紀錄。
    - 掛單列表查詢加上 `pos:order:read` 權限。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `pos:order:read` 權限 seed。
    - 將訂單讀取權限指派給 `CASHIER`、`SHIFT_MANAGER`、`STORE_MANAGER`、`AREA_MANAGER`，確保收銀台與後台訂單列表可正常載入。
  - `backend/module-pos-core/src/test/java/com/enterprise/core/controller/OrderControllerSecurityTest.java`
    - 新增訂單、掛單與退款 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-pos-core -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/pos/orders?storeId={storeId}&page=0&size=1`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `POST /api/v1/pos/orders` 建立測試訂單：200。
    - `GET /api/v1/pos/orders/{id}`：200，可查到測試訂單。
    - `GET /api/v1/pos/orders?storeId={storeId}&page=0&size=5`：200，可在列表查到測試訂單。
    - `POST /api/v1/pos/held-orders` 建立測試掛單：200。
    - `GET /api/v1/pos/held-orders?storeId={storeId}&terminalId={terminalId}`：200，可查到測試掛單。
    - `DELETE /api/v1/pos/held-orders/{id}`：200。
    - `GET /api/v1/audit-logs?module=pos-order&page=0&size=10`：200，可查到 `create` 稽核動作。
    - `GET /api/v1/audit-logs?module=pos-held-order&page=0&size=10`：200，可查到 `create` 與 `delete` 稽核動作。

## Tax Backend Permission Hardening

- 時間：2026-05-14 21:16:49 CST
- 目標：補齊稅別、發票與字軌後端正式化所需的權限控管與操作稽核，將一般發票開立與高風險稅務設定 / 字軌管理拆成不同權限。
- 變更：
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/controller/InvoiceController.java`
    - 發票開立加上 `pos:invoice:issue` 權限與 `pos-invoice:issue` 稽核紀錄。
    - 發票查詢加上 `pos:invoice:read` 權限。
    - 發票作廢加上 `pos:invoice:void` 權限與 `pos-invoice:void` 稽核紀錄。
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/controller/InvoiceTrackController.java`
    - 字軌查詢加上 `pos:tax:read` 權限。
    - 字軌新增加上 `pos:invoice-track:manage` 權限與 `pos-invoice-track:add` 稽核紀錄。
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/controller/TaxClassController.java`
    - 稅別查詢加上 `pos:tax:read` 權限。
    - 稅別建立 / 停用加上 `pos:tax:manage` 權限與 `pos-tax-class` 稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `pos:invoice:read`、`pos:invoice:issue`、`pos:invoice:void`、`pos:tax:read`、`pos:tax:manage`、`pos:invoice-track:manage` 權限 seed。
    - 收銀員可查詢 / 開立發票；班主管可作廢發票並查詢稅務設定；店長與區域主管可管理稅別與字軌。
  - `backend/module-pos-tax/src/test/java/com/enterprise/tax/controller/TaxControllerSecurityTest.java`
    - 新增發票、字軌與稅別 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-pos-tax -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/pos/tax-classes?storeId={storeId}`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/pos/tax-classes?storeId={storeId}`：200。
    - `POST /api/v1/pos/tax-classes` 建立測試稅別：200。
    - `DELETE /api/v1/pos/tax-classes/{id}` 停用測試稅別：200。
    - `POST /api/v1/pos/invoice-tracks` 新增測試字軌：200。
    - `GET /api/v1/pos/invoice-tracks?storeId={storeId}`：200，可查到測試字軌。
    - `GET /api/v1/audit-logs?module=pos-tax-class&page=0&size=10`：200，可查到 `create` 與 `deactivate` 稽核動作。
    - `GET /api/v1/audit-logs?module=pos-invoice-track&page=0&size=10`：200，可查到 `add` 稽核動作。
- 問題紀錄：
  - API smoke 曾使用過長的字軌 `period` 測試值造成 PostgreSQL `varchar(10)` 錯誤，已改用不超過 10 字元的期別並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-invoice-track-period-length.md`。

## Organization Store Terminal Permission Hardening

- 時間：2026-05-14 21:23:46 CST
- 目標：補齊店鋪、終端機、區域與店鋪員工關聯後端正式化所需的權限控管與操作稽核，並修正店鋪列表未帶篩選條件時回傳空資料的問題。
- 變更：
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/StoreController.java`
    - 店鋪建立 / 更新 / 狀態更新 / 刪除加上 `system:organization:manage` 權限與 `organization-store` 稽核紀錄。
    - 店鋪詳情、店鋪代碼查詢與列表查詢加上 `system:organization:read` 權限。
    - `GET /api/v1/stores` 未帶 `companyId` 或 `regionId` 時改為回傳全部店鋪，避免後台與 smoke test 無法取得 demo 店鋪。
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/TerminalController.java`
    - 終端註冊 / 更新 / 狀態更新 / 刪除加上 `system:organization:manage` 權限與 `organization-terminal` 稽核紀錄。
    - 終端詳情、代碼查詢、店鋪終端列表與離線終端查詢加上 `system:organization:read` 權限。
    - 終端 heartbeat 加上 `system:organization:manage` 權限，暫不加稽核，避免心跳操作產生大量低價值紀錄。
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/RegionController.java`
    - 區域建立 / 更新 / 刪除加上 `system:organization:manage` 權限與 `organization-region` 稽核紀錄。
    - 區域詳情與公司區域列表加上 `system:organization:read` 權限。
  - `backend/module-organization/src/main/java/com/enterprise/organization/controller/StoreEmployeeController.java`
    - 店鋪員工指派 / 解除指派 / 設定主要店鋪加上 `system:organization:manage` 權限與 `organization-store-employee` 稽核紀錄。
    - 依店鋪、依員工與主要店鋪查詢加上 `system:organization:read` 權限。
  - `backend/module-organization/src/main/java/com/enterprise/organization/service/StoreService.java`
    - 新增 `listAll()` 介面。
  - `backend/module-organization/src/main/java/com/enterprise/organization/service/impl/StoreServiceImpl.java`
    - 實作 `listAll()`，支援無篩選店鋪列表。
  - `backend/module-organization/src/test/java/com/enterprise/organization/controller/OrganizationControllerSecurityTest.java`
    - 擴充店鋪、終端、區域與店鋪員工關聯 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-organization -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/stores`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/stores`：200，可查到 demo 店鋪 `XINYI-001`。
    - `GET /api/v1/stores/code/XINYI-001`：200。
    - `PATCH /api/v1/stores/{storeId}/status?status=ACTIVE`：200，產生 `organization-store:update-status` 稽核紀錄。
    - `GET /api/v1/terminals/store/{storeId}`：200，可查到 demo 終端 `DEMO-T-001`。
    - `GET /api/v1/terminals/code/DEMO-T-001`：200。
    - `POST /api/v1/terminals/{terminalId}/heartbeat?ipAddress=127.0.0.1&appVersion=api-smoke`：200。
    - `GET /api/v1/audit-logs?module=organization-store&page=0&size=20`：200，可查到 `update-status` 成功稽核紀錄。
- 問題紀錄：
  - API smoke 初次沿用舊測試密碼 `admin123` 造成登入 401，確認目前本地 seed 的管理員密碼為 `admin / 123456`，已改正 smoke 流程並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-pos-admin-smoke-password.md`。

## Staff Backend Permission Hardening

- 時間：2026-05-14 21:27:54 CST
- 目標：補齊班次、打卡、交接班與 X/Z 報表後端正式化所需的權限控管與操作稽核，避免營運班次與報表資料裸露或異動無紀錄。
- 變更：
  - `backend/module-pos-staff/src/main/java/com/enterprise/staff/controller/ShiftController.java`
    - 開放班次列表加上 `pos:shift:read` 權限。
    - 開班、一般關班與打卡加上 `pos:shift:operate` 權限與 `pos-shift` 稽核紀錄。
    - 盲點結算與交接班加上 `pos:shift:manage` 權限與 `pos-shift` 稽核紀錄。
  - `backend/module-pos-staff/src/main/java/com/enterprise/staff/controller/ReportController.java`
    - X Report 產生 / 查詢加上 `pos:report:view` 權限，產生動作加上 `pos-report-x:generate` 稽核紀錄。
    - Z Report 產生 / 查詢加上 `pos:report:view-all` 權限，產生動作加上 `pos-report-z:generate` 稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `pos:shift:read`、`pos:shift:operate`、`pos:shift:manage` 權限 seed。
    - 收銀員可查詢與操作班次；班主管、店長與區域主管可管理班次。
  - `backend/module-pos-staff/src/test/java/com/enterprise/staff/controller/StaffControllerSecurityTest.java`
    - 新增班次與報表 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-pos-staff -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/staff/shifts?storeId={storeId}`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/staff/shifts?storeId={storeId}`：200。
    - 若目前無開放班次，使用 demo 員工與 demo 終端建立測試班次：200。
    - `POST /api/v1/staff/reports/x/{shiftId}`：200，成功產生 X Report。
    - `GET /api/v1/staff/reports/x?shiftId={shiftId}`：200，可查到 X Report。
    - `GET /api/v1/audit-logs?module=pos-report-x&page=0&size=20`：200，可查到 `generate` 成功稽核紀錄。

## Inventory Backend Permission Hardening

- 時間：2026-05-14 21:33:05 CST
- 目標：補齊庫存查詢、盤點單查詢與調撥單查詢的讀取權限，讓庫存異動、盤點與調撥流程在正式後端中都具備基本存取控管。
- 變更：
  - `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/controller/StockController.java`
    - 庫存列表、單品庫存、庫存異動紀錄與未確認警示查詢加上 `pos:inventory:read` 權限。
    - 保留庫存調整 `pos:inventory:adjust`、進貨驗收 `pos:inventory:receive` 與警示確認稽核。
  - `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/controller/StockTakeController.java`
    - 盤點單列表與盤點單詳情加上 `pos:inventory:read` 權限。
    - 保留建立、登記數量、完成與取消盤點的 `pos:inventory:stock-take` 權限與稽核。
  - `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/controller/TransferController.java`
    - 調撥單列表與詳情加上 `pos:inventory:read` 權限。
    - 保留建立、核准、出貨、收貨與取消調撥的 `pos:inventory:transfer` 權限與稽核。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `pos:inventory:read` 權限 seed。
    - 收銀員、班主管、店長與區域主管都可讀取庫存；進貨、調整、盤點與調撥仍維持更高權限。
  - `backend/module-pos-inventory/src/test/java/com/enterprise/inventory/controller/InventoryControllerSecurityTest.java`
    - 新增庫存、盤點單與調撥 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-pos-inventory -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，沿用原本 POS 端口，不另開服務。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/inventory/stores/{storeId}/stock`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/inventory/stores/{storeId}/stock`：200，可查到 demo 庫存商品。
    - `GET /api/v1/inventory/stores/{storeId}/stock/{itemId}`：200。
    - `GET /api/v1/inventory/stores/{storeId}/items/{itemId}/movements`：200。
    - `GET /api/v1/inventory/stores/{storeId}/alerts`：200。
    - `GET /api/v1/inventory/stock-takes/stores/{storeId}`：200。
    - `GET /api/v1/inventory/transfers/stores/{storeId}`：200。
- 問題紀錄：
  - API smoke 腳本初次使用 `status` 變數名，與 zsh 唯讀特殊參數衝突，已改用 `http_status` 並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-zsh-status-readonly-variable.md`。

## RBAC Backend Permission Hardening

- 時間：2026-05-14 21:38:05 CST
- 目標：補齊角色與權限管理讀取端點的權限控管，避免後台 RBAC 設定資料未登入即可存取。
- 變更：
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/RoleController.java`
    - 角色列表與角色詳情加上 `system:rbac:read` 權限。
    - 保留角色權限摘要 `system:rbac:read`，角色權限更新 `system:rbac:manage` 與 `system-rbac:update-role-permissions` 稽核。
  - `backend/module-auth/src/main/java/com/enterprise/auth/controller/PermissionController.java`
    - 權限列表與權限詳情加上 `system:rbac:read` 權限。
  - `backend/module-auth/src/test/java/com/enterprise/auth/controller/RbacControllerSecurityTest.java`
    - 新增角色與權限 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-auth -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，沿用原本 POS 端口。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/roles`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/roles`：200，可查到角色。
    - `GET /api/v1/roles/permission-summaries`：200。
    - `GET /api/v1/roles/{roleId}`：200。
    - `GET /api/v1/permissions`：200，可查到權限。
    - `GET /api/v1/permissions/{permissionId}`：200。

## Attendance Backend Permission Hardening

- 時間：2026-05-14 21:45:31 CST
- 目標：補齊出勤、打卡、補卡、地理圍欄、假日與出勤班別設定的權限控管與稽核紀錄，避免人事出勤資料與設定端點未受正式權限保護。
- 變更：
  - `backend/module-attendance/src/main/java/com/enterprise/attendance/controller/AttendanceController.java`
    - 打卡與補卡申請加上 `system:attendance:operate` 權限與稽核紀錄。
    - 今日打卡紀錄與月度打卡紀錄查詢加上 `system:attendance:read` 權限。
  - `backend/module-attendance/src/main/java/com/enterprise/attendance/controller/GeofenceController.java`
    - 地理圍欄查詢加上 `system:attendance:read` 權限。
    - 地理圍欄新增、更新與刪除加上 `system:attendance:manage` 權限與稽核紀錄。
  - `backend/module-attendance/src/main/java/com/enterprise/attendance/controller/HolidayController.java`
    - 假日查詢加上 `system:attendance:read` 權限。
    - 假日新增、更新與刪除加上 `system:attendance:manage` 權限與稽核紀錄。
  - `backend/module-attendance/src/main/java/com/enterprise/attendance/controller/ShiftController.java`
    - 出勤班別查詢加上 `system:attendance:read` 權限。
    - 班別新增、更新、刪除與排班加上 `system:attendance:manage` 權限與稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:attendance:read`、`system:attendance:operate`、`system:attendance:manage` 權限 seed。
    - 區域主管取得出勤查詢、操作與設定管理權限；`SUPER_ADMIN` 仍維持系統 bypass 策略。
  - `backend/module-attendance/pom.xml`
    - 補上 `spring-boot-starter-test` 測試依賴，讓模組可執行 controller annotation 測試。
  - `backend/module-attendance/src/test/java/com/enterprise/attendance/controller/AttendanceControllerSecurityTest.java`
    - 新增出勤、地理圍欄、假日與班別 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-attendance -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，沿用原本 POS 端口。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/attendance/holidays?year=2026`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/attendance/holidays?year=2026`：200。
    - `GET /api/v1/attendance/geofences`：200。
    - `GET /api/v1/attendance/shifts`：200。
    - `GET /api/v1/attendance/records?year=2026&month=5`：200。
- 問題紀錄：
  - 新增測試後發現 `module-attendance` 缺少 JUnit 測試依賴，已補上 `spring-boot-starter-test` 並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-maven-module-missing-test-dependency.md`。

## Leave Backend Permission Hardening

- 時間：2026-05-14 21:49:49 CST
- 目標：補齊請假申請、銷假、請假日曆、餘假與假別管理的權限控管，並讓請假與假別異動可被後台稽核追蹤。
- 變更：
  - `backend/module-leave/src/main/java/com/enterprise/leave/controller/LeaveRequestController.java`
    - 請假送出與銷假加上 `system:leave:request` 權限與 `leave-request` 稽核紀錄。
    - 員工請假列表與請假日曆查詢加上 `system:leave:read` 權限。
  - `backend/module-leave/src/main/java/com/enterprise/leave/controller/LeaveBalanceController.java`
    - 員工年度餘假查詢加上 `system:leave:read` 權限。
  - `backend/module-leave/src/main/java/com/enterprise/leave/controller/LeaveTypeController.java`
    - 假別查詢加上 `system:leave:read` 權限。
    - 假別新增、更新與刪除加上 `system:leave:manage` 權限與 `leave-type` 稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:leave:read`、`system:leave:request`、`system:leave:manage` 權限 seed。
    - 收銀員與班主管可查詢 / 申請請假；店長與區域主管可管理假別設定。
  - `backend/module-leave/src/test/java/com/enterprise/leave/controller/LeaveControllerSecurityTest.java`
    - 新增請假、餘假與假別 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-leave -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，沿用原本 POS 端口。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/leave/types`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/leave/types`：200。
    - `GET /api/v1/leave/requests?employeeId=00000000-0000-0000-0000-000000000201`：200。
    - `GET /api/v1/leave/requests/calendar?startDate=2026-05-01&endDate=2026-05-31`：200。
    - `GET /api/v1/leave/balances?employeeId=00000000-0000-0000-0000-000000000201&year=2026`：200。

## Notification Backend Permission Hardening

- 時間：2026-05-14 21:53:23 CST
- 目標：補齊通知查詢、未讀數、已讀狀態與測試推播的權限控管，避免個人通知資料與推播測試端點未受正式權限保護。
- 變更：
  - `backend/module-notification/src/main/java/com/enterprise/notification/controller/NotificationController.java`
    - 補上中英文檔頭註釋。
    - 未讀通知列表與未讀數查詢加上 `system:notification:read` 權限。
    - 單筆已讀與全部已讀加上 `system:notification:operate` 權限與 `notification` 稽核紀錄。
    - 測試推播加上 `system:notification:manage` 權限與 `notification:test-push` 稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:notification:read`、`system:notification:operate`、`system:notification:manage` 權限 seed。
    - 所有 POS 角色可讀取與操作自己的通知已讀狀態；店長與區域主管可執行通知測試 / 推播管理。
  - `backend/module-notification/pom.xml`
    - 補上 `spring-boot-starter-test` 測試依賴。
  - `backend/module-notification/src/test/java/com/enterprise/notification/controller/NotificationControllerSecurityTest.java`
    - 新增通知 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-notification -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，沿用原本 POS 端口。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/notifications/count`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/notifications/count`：200。
    - `GET /api/v1/notifications?page=0&size=5`：200。
    - `PUT /api/v1/notifications/read-all`：200，產生 `notification:mark-all-read` 稽核紀錄。
    - `POST /api/v1/notifications/test-push`：200，產生 `notification:test-push` 稽核紀錄。
    - `GET /api/v1/audit-logs?module=notification&page=0&size=10`：200。
- 剩餘缺口：
  - 公開 / 登入入口：`AuthController`、`PosAuthController`、`BackendInfoController` 保持公開或特殊登入流程，不直接加一般權限。
  - 待處理正式化模組：`module-workflow` 的流程定義、流程啟動與任務審批 controller。

## Workflow Backend Permission Hardening

- 時間：2026-05-14 21:58:38 CST
- 目標：補齊流程定義查詢、流程啟動與待辦任務處理的權限控管，讓後台工作流進入可正式驗收的 RBAC 與稽核基線。
- 變更：
  - `backend/module-workflow/src/main/java/com/enterprise/workflow/controller/DefinitionController.java`
    - 補上中英文檔頭註釋。
    - 流程定義列表查詢加上 `system:workflow:read` 權限。
  - `backend/module-workflow/src/main/java/com/enterprise/workflow/controller/WorkflowController.java`
    - 補上中英文檔頭註釋。
    - 流程啟動加上 `system:workflow:start` 權限與 `workflow-instance:start` 稽核紀錄。
  - `backend/module-workflow/src/main/java/com/enterprise/workflow/controller/TaskController.java`
    - 補上中英文檔頭註釋。
    - 個人待辦查詢加上 `system:workflow:read` 權限。
    - 核准、駁回與轉派加上 `system:workflow:approve` 權限與 `workflow-task` 稽核紀錄。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `system:workflow:read`、`system:workflow:start`、`system:workflow:approve` 權限 seed。
    - 班主管可查詢與審批工作流；店長與區域主管可查詢、啟動與審批工作流。
  - `backend/module-workflow/src/test/java/com/enterprise/workflow/controller/WorkflowControllerSecurityTest.java`
    - 新增工作流 controller 權限 / 稽核 annotation 反射測試。
- 驗證：
  - `mvn test -pl module-workflow -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 仍由 financial-accounting 容器使用，沿用原本 POS 端口。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/workflow/definitions`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/workflow/definitions`：200。
    - `GET /api/v1/workflow/tasks/my-pending?operatorId=00000000-0000-0000-0000-000000000201`：200。
    - 未登入 `POST /api/v1/workflow/start`：403，符合保護預期。
- Controller 權限掃描：
  - 已掃描 `backend/module-*` 與 `backend/app` 的所有 controller mapping。
  - 剩餘未標註 `@RequirePermission` 的 controller 僅有公開 / 登入入口：
    - `backend/module-auth/src/main/java/com/enterprise/auth/controller/AuthController.java`
    - `backend/module-auth/src/main/java/com/enterprise/auth/controller/PosAuthController.java`
    - `backend/app/src/main/java/com/enterprise/controller/BackendInfoController.java`
  - 判斷：符合預期，保留為登入、POS 終端驗證與後端資訊入口，不套一般 RBAC 權限。

## Employee Data Scope Hardening

- 時間：2026-05-14 22:06:58 CST
- 目標：在 RBAC 權限控管之外，補上高風險人事資料的資料範圍守門，避免一般操作員用其他人的 `employeeId`、`operatorId` 或通知 ID 存取 / 修改資料。
- 變更：
  - `backend/module-organization/src/main/java/com/enterprise/organization/service/EmployeeAccessService.java`
    - 新增員工資料範圍守衛服務。
    - 目前登入者可透過 `auth_users.id -> org_employees.user_id` 解析自己的員工 ID。
    - 一般員工只能操作自己的員工資料。
    - `SUPER_ADMIN` 可操作員工範圍資料；`AREA_MANAGER`、`STORE_MANAGER`、`SHIFT_MANAGER` 可讀取人事範圍資料。
  - `backend/module-attendance/pom.xml`
    - 新增 `module-organization` 依賴，讓出勤模組能解析登入者對應員工。
  - `backend/module-attendance/src/main/java/com/enterprise/attendance/controller/AttendanceController.java`
    - 打卡、下班、今日紀錄與補卡預設使用目前登入者的員工 ID，不再誤用 user ID。
    - 月度出勤查詢若指定 `employeeId`，會先檢查是否具備讀取該員工資料的範圍。
  - `backend/module-leave/src/main/java/com/enterprise/leave/service/LeaveRequestService.java`
    - 請假提交與銷假前檢查操作員工範圍。
    - 員工請假列表查詢前檢查讀取範圍。
    - 請假日曆限具備人事範圍角色使用。
  - `backend/module-workflow/src/main/java/com/enterprise/workflow/controller/WorkflowController.java`
    - 啟動流程前檢查 `initiatorId` 操作範圍。
  - `backend/module-workflow/src/main/java/com/enterprise/workflow/controller/TaskController.java`
    - 待辦查詢與核准 / 駁回 / 轉派前檢查 `operatorId` 操作範圍。
  - `backend/module-notification/src/main/java/com/enterprise/notification/service/NotificationService.java`
    - 單筆通知已讀操作改為帶入目前使用者 ID。
  - `backend/module-notification/src/main/java/com/enterprise/notification/service/impl/NotificationServiceImpl.java`
    - 單筆通知已讀前檢查通知歸屬，禁止改其他使用者通知。
  - `backend/module-organization/src/test/java/com/enterprise/organization/service/EmployeeAccessServiceTest.java`
    - 新增一般員工不可操作他人資料、可操作自己資料、店長可讀人事範圍、超級管理員可 bypass 的測試。
  - `backend/module-notification/src/test/java/com/enterprise/notification/service/NotificationServiceImplTest.java`
    - 新增通知已讀資料範圍測試。
  - `backend/module-leave/src/test/java/com/enterprise/leave/LeaveRequestServiceTest.java`
    - 補上 `EmployeeAccessService` mock 與請假 / 銷假範圍驗證。
- 驗證：
  - `mvn test -pl module-organization -am`：通過。
  - `mvn test -pl module-notification -am`：通過。
  - `mvn test -pl module-attendance -am`：通過。
  - `mvn test -pl module-workflow -am`：通過。
  - `mvn test -pl module-leave -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 仍由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功，映像 `sha256:1012be322d7b4ece122a9cd49a3926877ee1b90925d7defe276143f362084985`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 未登入 `GET /api/v1/attendance/records?employeeId=00000000-0000-0000-0000-000000000201&year=2026&month=5`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `admin / 123456`：200，取得 JWT。
    - `GET /api/v1/attendance/records?employeeId=00000000-0000-0000-0000-000000000201&year=2026&month=5`：200。
    - `GET /api/v1/leave/requests?employeeId=00000000-0000-0000-0000-000000000201`：200。
    - `GET /api/v1/workflow/tasks/my-pending?operatorId=00000000-0000-0000-0000-000000000201`：200。
    - `GET /api/v1/notifications/count`：200。
- 問題紀錄：
  - 平行執行相依重疊的 Maven reactor 測試時，`module-leave -am` 與 `module-workflow -am` 同時操作 workflow 測試 fork，偶發 `Unable to create test class`；已改為單獨重跑驗證，並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-parallel-maven-surefire-fork-test-class.md`。
  - API smoke 腳本使用 `path` 作為 zsh 迴圈變數，覆蓋 zsh 特殊變數並導致 `curl` 找不到；已改用 `api_path` 重跑通過，並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-zsh-path-special-variable.md`。

## Store Data Scope Hardening

- 時間：2026-05-14 22:46:09 CST
- 目標：補上 POS core / inventory 的門店資料範圍守門，避免收銀員用其他門店 `storeId` 讀取或操作訂單、暫存單、退款與庫存資料。
- 變更：
  - `backend/module-organization/src/main/java/com/enterprise/organization/service/StoreAccessService.java`
    - 新增門店資料範圍守衛服務。
    - `SUPER_ADMIN`、`AREA_MANAGER` 可跨店；一般門店角色需有 `org_store_employees` active assignment。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/controller/OrderController.java`
    - 新增訂單建立、查詢、完成付款、作廢的門店範圍檢查。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/controller/HeldOrderController.java`
    - 新增暫存單建立、查詢、刪除的門店範圍檢查。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/controller/RefundController.java`
    - 新增退款建立與完成前的訂單 / 退款門店範圍檢查。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/service/OrderService.java`
    - 新增 `findStoreId`，讓 controller 可先查訂單門店再做範圍判定。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/service/HeldOrderService.java`
    - 新增 `findStoreId` 並整理共用查詢方法。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/service/RefundService.java`
    - 新增 `findOrderStoreId`、`findStoreId` 並整理共用查詢方法。
  - `backend/module-pos-core/src/test/java/com/enterprise/core/controller/PosCoreStoreScopeTest.java`
    - 新增 POS core controller 門店範圍反射 / mock MVC 測試。
  - `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/controller/StockController.java`
  - `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/controller/StockTakeController.java`
  - `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/controller/TransferController.java`
    - 新增庫存、盤點、調撥 API 的門店讀取 / 操作範圍檢查。
  - `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/service/StockAlertService.java`
    - 新增 `findById`，支援 alert 操作前做門店判定。
  - `backend/app/src/main/java/com/enterprise/config/PosDemoDataSeeder.java`
    - 修復本地 demo data：使用 JDBC 顯式建立固定 UUID 的 store / terminal / employee。
    - 將舊版隨機 UUID demo 資料改成 `*-OLD-<id>`，並移除舊 cashier employee 的 `user_id`，避免登入後員工解析出現多筆。
- 驗證：
  - `mvn test -pl module-organization -am`：通過。
  - `mvn test -pl module-pos-inventory -am`：通過。
  - `mvn test -pl module-pos-core -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功，映像 `sha256:2ed2b0edbeafa583ee9c90e4276d573faf5bcb7bfc7dda37a06644b34e415d4b`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - DB smoke：
    - `org_stores` 已有固定 `00000000-0000-0000-0000-000000000001 / XINYI-001`。
    - `org_terminals` 已有固定 `00000000-0000-0000-0000-000000000101 / DEMO-T-001`。
    - `org_employees` 已有固定 `00000000-0000-0000-0000-000000000201 / EMP-POS-001`，舊 demo employee 已解除 `user_id`。
  - API smoke test：
    - 未登入 `GET /api/v1/pos/orders?storeId=00000000-0000-0000-0000-000000000001&page=0&size=1`：403，符合保護預期。
    - `POST /api/v1/auth/login` 使用 `cashier / 123456`：200，取得 JWT。
    - cashier 查詢自己門店訂單：200。
    - cashier 查詢自己門店暫存單：200。
    - cashier 查詢自己門店庫存：200。
    - cashier 查詢其他門店訂單：403，符合資料範圍預期。
- 問題紀錄：
  - JPA `@GeneratedValue(strategy = GenerationType.UUID)` 會忽略 demo seeder 對 `BaseEntity.id` 的手動指派，導致前端固定 storeId 和資料庫實際 UUID 不一致；已改用 JDBC 顯式插入固定 UUID，並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-pos-demo-data-store-scope-denied.md`。

## Store Data Scope Hardening - Payment / Tax / Staff

- 時間：2026-05-14 22:55:26 CST
- 目標：將門店資料範圍守門延伸到支付、稅務與 POS 班次報表模組，避免收銀員透過 `storeId`、支付方式 ID、發票 / 稅率 ID 或班次 / 報表 API 存取其他門店資料。
- 變更：
  - `backend/module-pos-payment/pom.xml`
    - 新增 `module-organization` 依賴。
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/controller/PaymentController.java`
    - 支付處理、訂單付款查詢、支付方式查詢 / 建立 / 停用前，加入 `StoreAccessService` 門店讀取 / 操作範圍檢查。
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/controller/CashDrawerController.java`
    - 開錢櫃、關錢櫃與查詢開啟中錢櫃加入門店範圍檢查。
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/controller/ReconciliationController.java`
    - 對帳產生、查詢、確認加入門店範圍檢查。
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/service/PayMethodService.java`
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/service/CashDrawerService.java`
  - `backend/module-pos-payment/src/main/java/com/enterprise/payment/service/ReconciliationService.java`
    - 新增 `findStoreId` 類查詢方法，支援 controller 在變更前先做資料範圍判定。
  - `backend/module-pos-tax/pom.xml`
    - 新增 `module-organization` 依賴。
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/controller/InvoiceController.java`
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/controller/InvoiceTrackController.java`
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/controller/TaxClassController.java`
    - 發票、發票字軌、稅率類別 API 加入門店讀取 / 操作範圍檢查。
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/service/InvoiceService.java`
  - `backend/module-pos-tax/src/main/java/com/enterprise/tax/service/TaxClassService.java`
    - 新增 `findStoreId`，支援單筆操作前先判斷資料歸屬。
  - `backend/module-pos-staff/pom.xml`
    - 新增 `module-organization` 依賴。
  - `backend/module-pos-staff/src/main/java/com/enterprise/staff/controller/ShiftController.java`
    - 開班、關班、盲點結算、交接班、打卡與開放班次查詢加入門店範圍檢查。
  - `backend/module-pos-staff/src/main/java/com/enterprise/staff/controller/ReportController.java`
    - X / Z Report 查詢與產生加入門店範圍檢查，Z Report 產生需具備可操作門店。
  - `backend/module-pos-staff/src/main/java/com/enterprise/staff/service/StaffShiftService.java`
    - 新增 `findStoreId`，支援班次相關 API 先查歸屬門店。
  - 測試更新：
    - `PaymentControllerTest.java`
    - `ReconciliationControllerTest.java`
    - `InvoiceControllerTest.java`
    - `ShiftControllerTest.java`
    - 補齊新增 constructor dependency 的 mock 與既有 controller 測試。
- 驗證：
  - `mvn test -pl module-pos-payment -am`：通過。
  - `mvn test -pl module-pos-tax -am`：通過。
  - `mvn test -pl module-pos-staff -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用，`38080/38082` 由 financial-accounting 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功，映像 `sha256:58868bdd13b3fe220e998b5d5803f8ef964b5758716d9e9d8fb394ae19121949`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test 使用 `cashier / 123456`：
    - payment methods 自己門店：200，其他門店：403。
    - reconciliation 自己門店：200，其他門店：403。
    - invoice tracks 自己門店：200，其他門店：403。
    - tax classes 自己門店：200，其他門店：403。
    - staff shifts 自己門店：200，其他門店：403。
    - Z reports 自己門店：200，其他門店：403。
- 問題紀錄：
  - `ReportController` 加入 `StaffShiftService` 欄位後漏 import，導致 `mvn test -pl module-pos-staff -am` 編譯失敗；已補 import 並寫入 `~/Desktop/obsidian/raw/coding/errors/2026-05-14-java-missing-service-import-after-controller-dependency.md`。

## Product Price Rule API Formalization

- 時間：2026-05-14 23:04:59 CST
- 目標：將既有 `pos_prod_price_rules` 價格規則資料表正式補齊為可由後台 / POS 管理使用的後端 API，支援會員價、歡樂時光、批量價與門店範圍價格。
- 變更：
  - `backend/module-pos-product/pom.xml`
    - 新增 `module-organization` 依賴，讓價格規則 API 可使用門店資料範圍守門。
  - `backend/module-pos-product/src/main/java/com/enterprise/product/dto/PriceRuleRequest.java`
    - 新增價格規則建立 / 修改請求 DTO。
  - `backend/module-pos-product/src/main/java/com/enterprise/product/dto/PriceRuleResponse.java`
    - 新增價格規則回應 DTO。
  - `backend/module-pos-product/src/main/java/com/enterprise/product/repository/PriceRuleRepository.java`
    - 新增全域價格規則查詢方法，避免未帶 `storeId` 時讀到所有門店規則。
  - `backend/module-pos-product/src/main/java/com/enterprise/product/service/PriceRuleService.java`
    - 新增價格規則查詢、建立、修改、停用與 `findStoreId` 商業邏輯。
    - 驗證商品存在、有效期間順序與預設 `minQty / active`。
  - `backend/module-pos-product/src/main/java/com/enterprise/product/controller/PriceRuleController.java`
    - 新增 `GET /api/v1/pos/price-rules`、`POST /api/v1/pos/price-rules`、`PUT /api/v1/pos/price-rules/{id}`、`DELETE /api/v1/pos/price-rules/{id}`。
    - 讀取指定門店規則時檢查 `requireReadableStore`；建立 / 修改 / 停用指定門店規則時檢查 `requireOperableStore`。
  - 測試更新：
    - `backend/module-pos-product/src/test/java/com/enterprise/product/controller/ProductControllerSecurityTest.java`
    - `backend/module-pos-product/src/test/java/com/enterprise/product/controller/PriceRuleControllerTest.java`
    - `backend/module-pos-product/src/test/java/com/enterprise/product/service/PriceRuleServiceTest.java`
- 驗證：
  - `mvn test -pl module-pos-product -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - 啟動前檢查端口：`38180/38182` 由 POS 容器使用。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功，映像 `sha256:667728894712052b072c0f9b7029ffa68e39e96406f66c1944f574a9afb5ff36`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - `POST /api/v1/auth/login` 使用 `cashier / 123456`：200，取得 JWT。
    - `GET /api/v1/pos/products?page=0&size=1`：200，取得測試商品。
    - `GET /api/v1/pos/price-rules?itemId=<itemId>`：200，只查全域價格規則。
    - `GET /api/v1/pos/price-rules?itemId=<itemId>&storeId=00000000-0000-0000-0000-000000000001`：200。
    - `GET /api/v1/pos/price-rules?itemId=<itemId>&storeId=00000000-0000-0000-0000-000000000099`：403，符合門店資料範圍預期。
    - `POST /api/v1/pos/price-rules` 使用 `admin / 123456` 建立會員價規則：200。
    - `DELETE /api/v1/pos/price-rules/{id}` 使用 `admin / 123456` 停用剛建立的規則：200。

## Order Price Rule Application

- 時間：2026-05-14 23:11:23 CST
- 目標：讓訂單建立流程實際套用商品主檔與價格規則，避免交易金額完全信任前端傳入的 `unitPrice`，並補上促銷 / 會員價 / 批量價進入結帳計算的後端基線。
- 變更：
  - `backend/module-pos-core/src/main/java/com/enterprise/core/service/PriceRuleResolver.java`
    - 新增訂單價格規則解析器。
    - 依商品主檔重新帶入 `itemNameSnapshot`、`skuSnapshot` 與基準價格。
    - 同時查詢全域價格規則與指定門店價格規則。
    - 依有效期間、最低數量、會員身份與規則類型挑選可套用的最低單價。
    - `STAFF` 價格規則暫不自動套用，避免尚未建立員工優惠上下文前誤用。
  - `backend/module-pos-core/src/main/java/com/enterprise/core/service/OrderService.java`
    - 建立訂單前先呼叫 `PriceRuleResolver.resolveOrderItems`。
    - 使用重新定價後的明細計算小計、折扣、稅額與總額。
    - 儲存訂單明細時使用後端解析出的商品名稱、SKU 與單價。
    - 修正 line total，使 modifier price adjustment 也會進入明細金額。
  - 測試更新：
    - `backend/module-pos-core/src/test/java/com/enterprise/core/service/OrderServiceTest.java`
      - 新增建立訂單前會先重新定價並儲存後端解析單價的測試。
    - `backend/module-pos-core/src/test/java/com/enterprise/core/service/PriceRuleResolverTest.java`
      - 覆蓋會員 / 批量規則、非會員忽略會員與員工價、不可售商品拒絕建立訂單。
- 驗證：
  - `mvn test -pl module-pos-core -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - Docker / OrbStack 使用 POS 端口 override 重建 `pos-backend`：成功，映像 `sha256:a97e1e126ea83e13bcc4e9693fb7e9a126b8e437ec2b0197cf209c8ca7403f44`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test：
    - 使用 `admin / 123456` 建立商品門店 `BASE` 價格規則，價格為 `77`：200。
    - 使用 `cashier / 123456` 建立訂單，請求中故意傳入 `unitPrice=999`。
    - 後端回傳訂單明細 `unitPrice=77.00`、`lineTotal=77.00`、`subtotal=77.00`、`taxTotal=3.85`、`grandTotal=81.00`。
    - 後端回傳 `itemNameSnapshot=API Debug Item`、`skuSnapshot=DBG-1778416231`，確認已使用商品主檔快照而非前端偽造文字。
    - 測試用價格規則已透過 `DELETE /api/v1/pos/price-rules/{id}` 停用：200。

## POS CRM Member MVP

- 時間：2026-05-14 23:26:53 CST
- 目標：補上 POS CRM 會員與點數後端基線，讓前端會員折扣 / 點數流程之後可接正式 API，而不是只依賴 demo 資料。
- 變更：
  - `backend/pom.xml`
    - 新增 `module-pos-crm` 子模組。
  - `backend/app/pom.xml`
    - 新增 `module-pos-crm` 依賴，讓 app 打包 CRM 模組。
  - `backend/app/src/main/resources/application.yml`
    - Flyway migration locations 加入 `classpath:db/migration/pos/crm`。
    - modules 開關加入 `pos-crm: true`。
  - `backend/module-pos-crm/src/main/resources/db/migration/pos/crm/V9001__create_pos_crm_members.sql`
    - 新增 `pos_crm_members` 與 `pos_crm_point_ledgers`。
    - 建立會員編號、手機、點數流水與訂單累點防重複索引。
    - 補入三筆本地 demo 會員資料，對齊前端可測 UUID。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/config/CrmModuleConfig.java`
    - 新增 CRM 模組自動配置，啟用 component scan、entity scan 與 JPA repository scan。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/entity/Member.java`
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/entity/PointLedger.java`
    - 新增會員與點數流水 entity，重要商業資料採 `deleted_at` 軟刪除。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/repository/MemberRepository.java`
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/repository/PointLedgerRepository.java`
    - 新增會員搜尋、手機 / 會員編號唯一性檢查、點數流水查詢與訂單累點防重查詢。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/service/MemberService.java`
    - 新增會員搜尋、單筆查詢、建立會員、點數流水查詢。
    - 監聽 `OrderCompletedEvent`，若訂單有 `memberId`，以 `grandTotal / 10` 向下取整累點，並寫入 `ORDER_EARN` 流水。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/controller/MemberController.java`
    - 新增 `GET /api/v1/pos/members/search`。
    - 新增 `GET /api/v1/pos/members/{id}`。
    - 新增 `GET /api/v1/pos/members/{id}/points`。
    - 新增 `POST /api/v1/pos/members`。
  - `backend/module-auth/src/main/java/com/enterprise/auth/seeder/PosDataSeeder.java`
    - 新增 `pos:member:read`、`pos:member:operate`、`pos:member:manage` 權限。
    - `CASHIER`、`SHIFT_MANAGER` 給 read / operate；`STORE_MANAGER`、`AREA_MANAGER` 額外給 manage。
  - `docker/local/docker-compose.yml`
  - `env/.env.example`
  - `env/local/.env`
  - `docker/local/README.md`
    - 將 POS 本地 backend / frontend port 正式改為環境變數 `BACKEND_PORT=38180`、`FRONTEND_PORT=38182`，避免再靠臨時 compose override，也避免與財務系統 `38080/38082` 衝突。
- 驗證：
  - 任務開始前已讀取 `~/ai-kb/AGENTS.md`、`~/ai-kb/memory/MEMORY.md`，並查閱 Spring/Flyway/Docker 相關錯誤 SOP。
  - `mvn test -pl module-pos-crm -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - `git diff --check`：通過。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml config`：確認 backend `38180`、frontend `38182`。
  - Docker / OrbStack 重建 `pos-backend`：成功，最終映像 `sha256:df64b9b0dd2a3035fc45fdfe24b35e16e24ef432fbbe810c0818007e71ba9b87`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test 使用 `cashier / 123456`：
    - `GET /api/v1/pos/members/search?query=M-000801`：200，取得 demo 會員「林依晨」。
    - `GET /api/v1/pos/members/00000000-0000-0000-0000-000000000801`：200，點數初始為 1280。
    - `POST /api/v1/pos/members`：200，可建立 smoke 會員。
    - 建立訂單時故意傳 `unitPrice=999`，後端仍以商品主檔回 `unitPrice=120.00`。
    - 完成該會員訂單後，`GET /api/v1/pos/members/{id}/points` 回新增 `ORDER_EARN` 流水，點數增加 12，餘額由 1280 增至 1292。
- 問題紀錄：
  - CRM 模組初次進 Docker 啟動時缺少 `CrmModuleConfig`，Spring 未掃描 `MemberRepository`，導致 `pos-backend` 重啟循環；已補 module config，並寫入 troubleshooting 與 Obsidian raw。
  - Docker Hub metadata 曾出現 TLS handshake timeout；重試成功，已寫入 troubleshooting 與 Obsidian raw。

## POS CRM Point Mutations

- 時間：2026-05-14 23:32:58 CST
- 目標：補齊會員點數兌換與店長手動調整 API，讓 CRM 後端不只可查詢 / 累點，也能支援結帳折抵與營運補扣點。
- 變更：
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/dto/PointRedemptionRequest.java`
    - 新增會員兌點請求 DTO，要求兌換點數必須大於 0，可帶入訂單 ID 與備註。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/dto/PointAdjustmentRequest.java`
    - 新增店長手動點數調整 DTO，支援正負異動與備註。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/service/MemberService.java`
    - 新增 `redeemPoints` 與 `adjustPoints`。
    - 所有點數異動都寫入 `pos_crm_point_ledgers`，並防止餘額扣成負數。
    - 手動調整禁止 0 點 no-op，停用會員禁止異動。
  - `backend/module-pos-crm/src/main/java/com/enterprise/crm/controller/MemberController.java`
    - 新增 `POST /api/v1/pos/members/{id}/points/redemptions`，權限 `pos:member:operate`，稽核 `pos-member/redeem-points`。
    - 新增 `POST /api/v1/pos/members/{id}/points/adjustments`，權限 `pos:member:manage`，稽核 `pos-member/adjust-points`。
  - `backend/module-pos-crm/src/test/java/com/enterprise/crm/service/MemberServiceTest.java`
    - 覆蓋兌點成功、點數不足拒絕、手動補點成功、0 點調整拒絕。
  - `backend/module-pos-crm/src/test/java/com/enterprise/crm/controller/MemberControllerSecurityTest.java`
    - 補齊兌點 / 調整點數端點的權限與稽核註解測試。
- 驗證：
  - `mvn test -pl module-pos-crm -am`：通過。
  - `mvn test -pl app -am`：通過，整個後端 app 與所有相依模組測試成功。
  - 重建前確認 POS 端口：`38180`、`38182` 皆由 OrbStack POS 容器占用，符合預期。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build backend`：通過，後端映像 `sha256:4930a12cef301fa2f114809a1a83e45c4c40dd9fbc28b4754f583e7384fb3858`。
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - API smoke test 使用 `cashier / 123456`：
    - 建立 smoke 會員「Smoke 點數 233404」：200，初始點數 0。
    - `POST /api/v1/pos/members/{id}/points/adjustments` 補點 10：200，流水 `MANUAL_ADJUST`，餘額 10。
    - `POST /api/v1/pos/members/{id}/points/redemptions` 兌點 3：200，流水 `REDEEM`，餘額 7。
    - `GET /api/v1/pos/members/{id}`：200，會員點數餘額為 7。
