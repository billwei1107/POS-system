# 2026-05-19 Devlog

## POS Order Table Compact Layout Polish

- 時間：2026-05-19 10:36:59 CST。
- 目標：修正淺色模式訂單列表水平滑動後，訂單編號與後續欄位視覺重疊，並讓右側操作欄更容易完整呈現。

## Planning - POS Order Table Compact Layout Polish

- 桌面訂單表格：
  - 將訂單編號欄改為固定寬度，長編號改在欄位內水平捲動。
  - 使用 `tableLayout: fixed` 與 `colgroup` 明確控制欄寬。
  - 壓縮整體表格最小寬度，減少水平滑動距離。
  - 保留右側操作欄按鈕完整可見。
- 測試：
  - 補強訂單表格測試，確認桌面表格為 fixed layout 且訂單編號 cell 可捲動。
  - 使用瀏覽器實際驗證右滑後操作欄完整呈現。

## Project Structure - POS Order Table Compact Layout Polish

```text
POS/
├── frontend-web/
│   ├── src/features/pos-orders/pages/OrderListPage.tsx
│   └── test/pos-orders/OrderListPage.test.tsx
└── devlog/
    ├── 2026-05-19-devlog.md
    └── troubleshooting.md
```

## Progress - POS Order Table Compact Layout Polish

- `OrderListPage`：
  - 將桌面訂單表格最小寬度由 `1280px` 收斂至 `1180px`。
  - 新增訂單編號欄固定寬度 `224px`，欄內使用水平 overflow。
  - 新增 `colgroup` 欄寬配置，讓狀態、類型、付款、發票、折扣、合計、建立時間與操作欄有穩定寬度。
  - 訂單編號 sticky cell 加上 `overflow: hidden`，避免右滑時長字串壓到狀態欄。
  - 折扣 Chip 加上最大寬度與 ellipsis，避免窄欄位時撐開表格。
- `OrderListPage.test.tsx`：
  - 更新桌面表格測試，確認 `minWidth: 1180px`、`tableLayout: fixed` 與訂單編號欄內可水平捲動。

## Verification - POS Order Table Compact Layout Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，6 tests。
  - `npx tsc -b`：通過。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，16 files / 39 tests。
  - `git diff --check`：通過。
- Docker：
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml build frontend`：通過。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過，`pos-frontend` 已重啟。
- Browser：
  - 開啟 `http://localhost:38182/pos/orders`。
  - 確認表格實際寬度為 `1180px`、container 可水平捲動。
  - 確認訂單編號 cell 寬度為 `224px`，欄內 scroller 為 `overflow-x: auto`。
  - 右滑至最大位移後，操作欄三個按鈕右緣皆在表格容器內。

## Notes - POS Order Table Compact Layout Polish

- 38080 / 38082 仍由 `financial-accounting` 容器占用；POS 目前使用 `38180 / 38182`。
- 本輪只重建與重啟 `frontend`，未重啟後端與資料庫。

## Git 提交記錄

- 時間：2026-05-19 10:39:15 CST
- Hash：`f983730`
- 訊息：`fix(pos): compact order table columns`
- 變更統計：4 files changed, 177 insertions(+), 18 deletions(-)

## POS Order Number Column Width Follow-up

- 時間：2026-05-19 10:45:12 CST。
- 目標：依照畫面檢視結果，進一步縮小訂單編號欄位，並把釋出的寬度分配給其他資訊欄位。

## Progress - POS Order Number Column Width Follow-up

- `OrderListPage`：
  - 訂單編號欄由 `280px` 調整為 `224px`。
  - 將空間分配到狀態、類型、品項、付款狀態、發票狀態、折扣、合計與建立時間欄。
  - 維持表格總寬 `1180px` 與右側操作欄寬度，避免重新增加水平捲動距離。

## Verification - POS Order Number Column Width Follow-up

- `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，6 tests。
- `npx tsc -b`：通過。
- `npm run lint`：通過。
- `npm test -- --run`：通過，16 files / 39 tests。
- `npm run build`：通過，保留既有 Vite chunk size warning。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過，`pos-frontend` 已重啟。
- Browser DOM 幾何驗證：
  - 訂單編號欄：`224px`。
  - 狀態：`90px`；類型：`94px`；品項：`60px`。
  - 付款狀態：`100px`；發票狀態：`100px`；折扣：`142px`。
  - 合計：`84px`；建立時間：`142px`；操作：`144px`。

## Git 提交記錄 - POS Order Number Column Width Follow-up

- 時間：2026-05-19 10:46:03 CST
- Hash：`734e8e5`
- 訊息：`style(pos): rebalance order table columns`
- 變更統計：2 files changed, 42 insertions(+), 11 deletions(-)

## POS Order Status Column Readability Follow-up

- 時間：2026-05-19 10:58:21 CST。
- 目標：修正狀態欄 chip 顯示成省略文字的問題，狀態屬於關鍵資訊，必須完整顯示。

## Progress - POS Order Status Column Readability Follow-up

- `OrderListPage`：
  - 訂單編號欄由 `224px` 進一步收斂為 `190px`。
  - 狀態欄由 `90px` 擴大為 `124px`。
  - 新增狀態 chip 專用樣式，避免 label 被 MUI 預設 ellipsis 裁切。
  - 表格總寬仍維持 `1180px`。

## Verification - POS Order Status Column Readability Follow-up

- `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，6 tests。
- `npx tsc -b`：通過。
- `npm run lint`：通過。
- `npm test -- --run`：通過，16 files / 39 tests。
- `npm run build`：通過，保留既有 Vite chunk size warning。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過，`pos-frontend` 已重啟。
- Browser DOM 幾何驗證：
  - 訂單編號欄：`190px`，欄內 scroller max width 為 `158px`。
  - 狀態欄：`124px`。
  - 狀態 chip：`78px`。
  - 狀態 label 顯示 `已完成`，`scrollWidth <= clientWidth`，文字完整未省略。

## Git 提交記錄 - POS Order Status Column Readability Follow-up

- 時間：2026-05-19 10:59:08 CST
- Hash：`24b2fae`
- 訊息：`style(pos): keep order status labels visible`
- 變更統計：3 files changed, 74 insertions(+), 3 deletions(-)

## POS Order Detail Dialog Title Contrast

- 時間：2026-05-19 16:23:15 CST。
- 目標：修正淺色模式下訂單詳情彈窗左上標題文字幾乎看不清楚的問題。

## Progress - POS Order Detail Dialog Title Contrast

- `OrderListPage`：
  - 訂單詳情 Dialog paper 明確指定 `background.paper`、`text.primary` 與移除背景圖。
  - 將 Dialog 標題拆成兩行：主標 `訂單詳情` 與次要資訊 `orderNo`。
  - 主標使用 `text.primary`、`h6` 與 `fontWeight: 900`，提升淺色模式可讀性。
  - 訂單編號使用 monospace、`text.secondary` 與 `overflowWrap: anywhere`，避免長編號壓縮或溢出。
  - 補上輔助文字，保留 Dialog 的可辨識標題。
- `OrderListPage.test.tsx`：
  - 更新訂單詳情測試，確認 Dialog 開啟後可見標題與訂單編號存在。

## Verification - POS Order Detail Dialog Title Contrast

- `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，6 tests。
- `npx tsc -b`：通過。
- `npm run lint`：通過。
- `npm test -- --run`：通過，16 files / 39 tests。
- `npm run build`：通過，保留既有 Vite chunk size warning。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過，`pos-frontend` 已更新。
- Browser DOM style 驗證：
  - Dialog paper 背景：`rgb(255, 255, 255)`。
  - 標題文字：`rgb(17, 24, 39)`、`fontWeight: 900`。
  - 訂單編號：`rgb(95, 102, 117)`、`fontWeight: 800`。

## Git 提交記錄 - POS Order Detail Dialog Title Contrast

- 時間：2026-05-19 16:32:17 CST
- Hash：`a1fc603`
- 訊息：`style(pos): improve order detail title contrast`
- 變更統計：4 files changed, 128 insertions(+), 4 deletions(-)
