# 2026-05-11 Devlog

## 開發規劃 (Planning)

- 完成 POS 收銀台折扣功能，讓購物車的「折扣」按鈕可實際套用固定金額與百分比折扣。
- 確保折扣會同步到結帳頁、建立訂單 API、付款完成流程與資料庫訂單金額。
- 修正並驗證 TWD 現金結帳金額四捨五入後，不會產生不合理找零。

## 專案結構 (Project Structure)

```text
POS/
├── backend/
│   ├── app/                         # Spring Boot 啟動模組與全域配置
│   ├── module-auth/                 # 登入、JWT、POS PIN 驗證
│   ├── module-common/               # 共用 DTO、例外與基礎實體
│   ├── module-organization/         # 公司、門市、終端機、組織資料
│   ├── module-pos-core/             # POS 訂單、定價、付款完成核心流程
│   ├── module-pos-product/          # 商品與分類
│   ├── module-pos-inventory/        # 庫存與盤點
│   ├── module-pos-payment/          # 支付交易資料
│   ├── module-pos-tax/              # 發票與稅務事件
│   └── module-pos-staff/            # POS 員工資料
├── frontend-web/
│   └── src/
│       ├── features/pos-auth/       # POS PIN 登入與終端登入 API
│       ├── features/pos-orders/     # 收銀台、購物車、結帳、退款、訂單列表
│       ├── features/pos-products/   # 商品與分類管理
│       ├── layouts/                 # POS 與後台 Layout
│       └── shared/                  # API、auth store、工具函式與共用元件
├── docker/local/                    # 本地 Docker Compose、前後端 Dockerfile、nginx
├── env/local/                       # 本地環境變數
├── devlog/                          # 開發日誌與 troubleshooting
├── 需求/                            # 需求、流程、頁面結構與規格文件
└── 我的開發模板/                    # 專案模板與多 AI 接手文件
```

## 進度 (Progress)

- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `discountAmount` 狀態、`setDiscountAmount`、`clearDiscount`。
  - `calculateCartTotals` 支援折扣上限保護，折扣不可小於 0 且不可超過小計。
  - 總計改為 TWD 整數金額，與後端 `rounding_adj` / `grand_total` 對齊。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 「折扣」按鈕改為可操作功能。
  - 新增折扣 Dialog，支援 5%、10%、20% 快捷折扣、自訂金額、清除折扣與預覽折扣後小計。
  - 購物車摘要會顯示實際折扣與折扣後總計。
- `frontend-web/src/features/pos-orders/pages/CheckoutPage.tsx`
  - 結帳頁讀取購物車折扣狀態並帶入訂單建立 payload。
  - 現金付款送出的金額與畫面顯示的 TWD 整數總計一致。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/PricingEngine.java`
  - 後端定價引擎對負數折扣與超過小計的折扣做保護。
- `backend/module-pos-core/src/test/java/com/enterprise/core/service/PricingEngineTest.java`
  - 新增折扣超過小計、負數折扣的單元測試。

## 驗證 (Verification)

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `mvn -pl module-pos-core -am test`（backend）：通過。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」後總計 `$126`。
  - 套用 10% 折扣後購物車顯示折扣 `-$12`、結帳按鈕 `$113`。
  - `/pos/checkout` 顯示訂單摘要、折扣 `-$12`、應收 `$113`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`discount_total=12.00`、`tax_total=5.40`、`rounding_adj=-0.40`、`grand_total=113.00`、`paid_total=113.00`、`change_given=0.00`。

## 問題紀錄 (Issues)

- 第一次 Docker 重建使用錯誤 compose 路徑，導致前端容器仍是舊 bundle，付款時送出 `113.40` 並產生 `change_given=0.40`。
- 正確流程是只重建並重啟 frontend：`docker compose -f docker/local/docker-compose.yml build frontend`，再 `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`。
- `docker compose ... up -d --build frontend` 會因 `depends_on: backend` 嘗試重建 backend；若 Docker 內 Maven DNS 暫時無法解析 `repo.maven.apache.org`，會讓 frontend 更新被 backend build 失敗擋住。

## Update 2026-05-11 00:53

- `frontend-web/src/features/pos-orders/config.ts`
  - 新增本地 demo 會員資料，包含會員 ID、會員編號、遮罩手機、等級、點數與會員折扣率。
- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `selectedMember`、`setMember`、`clearMember`。
  - 會員綁定後會依會員折扣率自動計算折扣。
  - 已綁定會員時，新增、增加、減少或移除商品會重新計算會員折扣。
  - 手動折扣與會員折扣暫時互斥，避免尚未接促銷引擎前出現折扣疊加規則不清。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 「會員」按鈕改為可操作，支援空購物車先綁會員。
  - 新增會員 Dialog，可依姓名、手機或會員編號搜尋 demo 會員。
  - 綁定會員後購物車顯示會員姓名、等級與自動折扣。
- `frontend-web/src/features/pos-orders/pages/CheckoutPage.tsx`
  - 結帳頁顯示會員摘要與會員折扣 chip。
  - 建立訂單 payload 帶入 `memberId`。

## Update Verification 2026-05-11 00:53

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - 在空購物車狀態先綁定「金卡 林依晨」。
  - 加入「拿鐵 12oz」後自動套用 10% 會員折扣，顯示 `-$12` 與結帳 `$113`。
  - `/pos/checkout` 顯示 `金卡 · 林依晨 · 1,280 點` 與 `10% 會員折扣`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`member_id=00000000-0000-0000-0000-000000000801`、`discount_total=12.00`、`tax_total=5.40`、`rounding_adj=-0.40`、`grand_total=113.00`、`paid_total=113.00`、`change_given=0.00`。

## Update 2026-05-11 01:04

- `frontend-web/src/features/pos-orders/pages/CheckoutPage.tsx`
  - 現金付款新增「現金收款」區塊。
  - 支援收款金額輸入、剛好付款、`+$10`、`+$20`、`+$50` 快捷按鈕。
  - 即時計算找零或不足金額。
  - 現金收款不足時禁止確認付款並顯示錯誤。
  - 呼叫付款完成 API 時，現金付款改送實際 `cashTendered`，非現金付款維持不送 tendered。

## Update Verification 2026-05-11 01:04

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - 空購物車先綁定「金卡 林依晨」。
  - 加入「拿鐵 12oz」後自動套用 10% 會員折扣，結帳金額 `$113`。
  - `/pos/checkout` 現金收款輸入 `120` 後顯示找零 `$7`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`member_id=00000000-0000-0000-0000-000000000801`、`discount_total=12.00`、`tax_total=5.40`、`rounding_adj=-0.40`、`grand_total=113.00`、`paid_total=113.00`、`change_given=7.00`。

## Update 2026-05-11 01:06

- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `HeldOrder` 資料結構與 `heldOrders` 狀態。
  - 新增 `holdCurrentOrder`、`restoreHeldOrder`、`removeHeldOrder`。
  - 掛單資料寫入 `localStorage` 的 `pos-held-orders`，支援瀏覽器刷新後取回。
  - 掛單會保存品項、稅率、折扣、會員、總品項數、總金額與建立時間。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 右上角暫停圖示改為「掛單與取單」入口。
  - 有目前訂單時可一鍵掛起並清空當前購物車。
  - 無目前訂單時可從掛單列表取回。
  - 支援刪除不需要的掛單。

## Update Verification 2026-05-11 01:06

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - 加入「拿鐵 12oz」後進入「掛單與取單」並點擊「掛起」。
  - 購物車清空且 `localStorage.pos-held-orders` 寫入 1 筆掛單，金額 `126`。
  - 刷新 `/pos/register` 後仍可看到掛單並取回。
  - 取回後 `localStorage.pos-held-orders` 清空，購物車恢復 `$126` 訂單。
  - 結帳頁輸入現金收款 `130`，顯示找零 `$4`，付款完成。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`discount_total=0.00`、`tax_total=6.00`、`rounding_adj=0.00`、`grand_total=126.00`、`paid_total=126.00`、`change_given=4.00`。

## Update 2026-05-11 01:08

- 新增 `需求/開發進度對照.md`。
- 將 `開發流程規劃.md` 的 Phase/Sprint 與目前實作狀態對齊。
- 標記已完成、MVP 已完成但需正式化、未開始與下一步建議。
- 明確下一步建議：
  1. 回到 Sprint 1-2 正式化後端掛單 API。
  2. 接 Sprint 1-3 payment transaction / cash drawer / reconciliation。
  3. 接 Sprint 1-4 tax mock invoice happy path。
  4. 補 Phase 0 前端測試基線。

## Update Verification 2026-05-11 01:08

- 本次為文檔整理，未修改執行程式碼。
- `需求/開發進度對照.md` 已建立，可作為後續「繼續開發」前的進度入口。

## Update 2026-05-11 01:23

- `backend/module-pos-core/src/main/java/com/enterprise/core/repository/HeldOrderRepository.java`
  - 新增 `pos_held_orders` Repository，支援依門市與終端機查詢掛單。
- `backend/module-pos-core/src/main/java/com/enterprise/core/dto/request/CreateHeldOrderRequest.java`
  - 新增建立掛單 request，包含 `storeId`、`terminalId`、`label` 與 JSON payload。
- `backend/module-pos-core/src/main/java/com/enterprise/core/dto/response/HeldOrderResponse.java`
  - 新增掛單 response，統一回傳後端掛單 ID、標籤、payload 與時間欄位。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/HeldOrderService.java`
  - 新增建立、查詢、刪除掛單流程。
  - 查詢可依 `terminalId` 過濾；刪除不存在掛單時回傳 `ResourceNotFoundException`。
- `backend/module-pos-core/src/main/java/com/enterprise/core/controller/HeldOrderController.java`
  - 新增 `/api/v1/pos/held-orders`。
  - 支援 `POST` 建立、`GET` 查詢、`DELETE /{id}` 刪除。
- `backend/module-pos-core/src/test/java/com/enterprise/core/service/HeldOrderServiceTest.java`
  - 補建立掛單、依門市與終端機查詢、刪除掛單單元測試。
- `frontend-web/src/features/pos-orders/api/orderApi.ts`
  - 新增 `heldOrderApi.create/list/remove`。
- `frontend-web/src/features/pos-orders/types/index.ts`
  - 新增 `HeldOrderResponse` 與 `CreateHeldOrderRequest` 型別。
- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `replaceHeldOrders`、`replaceHeldOrder`，讓 API 回傳的後端掛單 ID 可取代本地暫存 ID。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 掛單 Dialog 開啟時會從後端載入掛單。
  - 掛起目前訂單時先保留本地 fallback，再呼叫後端建立掛單。
  - 取回或刪除後端掛單時同步呼叫刪除 API。
  - 若 API 暫時失敗，仍保留 localStorage 掛單，避免收銀現場遺失當前訂單。
- `需求/開發進度對照.md`
  - 將掛單狀態由前端 MVP 更新為後端 API 基礎正式化。
  - 下一步改為 Sprint 1-3 payment transaction。

## Update Verification 2026-05-11 01:23

- `mvn -pl module-pos-core -am test`（backend）：通過。
- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build backend frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps backend frontend`：通過。
- `curl http://localhost:38080/actuator/health`：回傳 `{"status":"UP"}`。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」後總計 `$126`。
  - 點擊「掛單與取單」並掛起目前訂單，後端 `pos_held_orders` active 筆數為 1。
  - 重新整理 `/pos/register` 後仍停留收銀台，登入資料有持久化。
  - 再次開啟「掛單與取單」可從後端掛單取回訂單。
  - 取回後後端 `pos_held_orders` active 筆數為 0。
  - `/pos/checkout` 現金收款輸入 `130`，顯示找零 `$4`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - 資料庫最新訂單：`status=COMPLETED`、`subtotal=120.00`、`discount_total=0.00`、`tax_total=6.00`、`grand_total=126.00`、`paid_total=126.00`、`change_given=4.00`。
  - 截圖輸出：`/tmp/pos-held-order-api-after-hold.png`、`/tmp/pos-held-order-api-restored.png`、`/tmp/pos-held-order-api-complete.png`。

## Git 提交記錄

- 2026-05-11 02:17:36 CST
  - Hash: `4ba1b03`
  - 訊息: `feat(pos-payment): record checkout payment transactions`
  - 推送: `origin/feature`
  - 變更統計: 11 files changed, 440 insertions(+), 42 deletions(-)
  - 摘要: Sprint 1-3 payment transaction 串接，訂單完成事件帶付款資訊，payment 模組建立 `pos_payment_transactions`，前端訂單列表可查付款記錄，並補單元測試與驗證文件。

- 2026-05-11 02:36:52 CST
  - Hash: `d1bf603`
  - 訊息: `feat(pos-tax): issue invoices on completed orders`
  - 推送: `origin/feature`
  - 變更統計: 13 files changed, 335 insertions(+), 59 deletions(-)
  - 摘要: Sprint 1-4 tax/invoice happy path 串接，訂單完成事件帶稅額，tax 模組建立 `pos_invoices` mock invoice，前端新增發票查詢入口，並補發票服務測試與 troubleshooting。

- 2026-05-11 02:37 CST
  - Hash: `23897e1`
  - 訊息: `docs(devlog): log tax invoice checkpoint commit`
  - 推送: `origin/feature`
  - 變更統計: 1 file changed, 8 insertions(+), 1 deletion(-)
  - 摘要: 補記 Checkpoint 2 tax/invoice commit 與推送狀態。

- 2026-05-11 01:41:22 CST
  - Hash: `f9e48c6`
  - 訊息: `feat(pos): stabilize checkout terminal flow`
  - 推送: `origin/feature`
  - 變更統計: 35 files changed, 2350 insertions(+), 162 deletions(-)
  - 摘要: 備份 POS PIN 登入、登入持久化、商品載入修正、折扣、會員 MVP、現金收款找零、後端掛單 API、前端掛單串接、進度文件與 devlog。

- 2026-05-11 02:52:40 CST
  - Hash: `a2d983c`
  - 訊息: `feat(pos-inventory): deduct stock on checkout`
  - 推送: `origin/feature`
  - 變更統計: 13 files changed, 335 insertions(+), 72 deletions(-)
  - 摘要: Checkpoint 3 inventory 扣庫存端到端；訂單完成時同步扣減追蹤庫存商品，庫存不足以 409 阻止付款完成，前端庫存頁接真實 API，並補 inventory service/listener 測試與驗證文件。

- 2026-05-11 02:53:25 CST
  - Hash: `986d771`
  - 訊息: `docs(devlog): log inventory checkpoint commit`
  - 推送: `origin/feature`
  - 變更統計: 1 file changed, 7 insertions(+)
  - 摘要: 補記 Checkpoint 3 inventory commit 與推送狀態。

## Update 2026-05-11 02:51

- Checkpoint 3：正式化 inventory 扣庫存端到端。
- `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/service/InventoryEventListener.java`
  - 訂單完成事件改為在核心訂單交易內同步處理。
  - 由 `OrderItemRepository` 查詢訂單品項，並透過 `ProductItemRepository` 只扣減 `trackInventory=true` 商品。
  - 已處理過的訂單會以 `pos_inv_stock_movements.reference_id + SALE` 防重。
- `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/service/StockDeductionService.java`
  - 銷售扣庫不再自動建立庫存紀錄或允許負庫存。
  - 改以悲觀鎖查詢既有庫存，並以 `quantity - reserved_quantity` 判斷可用庫存。
  - 庫存不足時拋出 `BusinessException(409, "庫存不足...")`，讓付款完成交易 rollback。
- `backend/app/src/main/java/com/enterprise/config/PosDemoDataSeeder.java`
  - Demo 商品改為 `trackInventory=true`。
  - 本地 demo 門店會為 demo 商品建立初始庫存 `100.000`；已存在庫存不覆蓋，避免重啟重置測試資料。
- `frontend-web/src/App.tsx`
  - `/pos/inventory` 改為實際 API 驅動的 `StockOverviewPage`。
- `frontend-web/src/features/pos-inventory/api/inventoryApi.ts`
  - 修正 axios interceptor 後的 `ApiResponse<T>` 型別，避免頁面讀取 `.data.data`。
- `frontend-web/src/features/pos-inventory/pages/StockOverviewPage.tsx`
  - 使用 `DEFAULT_STORE_ID` fallback。
  - 庫存與警示資料改讀 `response.data`。
- `frontend-web/src/features/pos-orders/pages/CheckoutPage.tsx`
  - 付款失敗時優先顯示後端 `ApiResponse.message`，庫存不足可在前端看到可理解錯誤。
- 測試：
  - 更新 `StockDeductionServiceTest`，覆蓋正常扣庫、SALE movement、庫存不足攔截、缺少庫存紀錄攔截、reserved quantity 導致可用庫存不足。
  - 新增 `InventoryEventListenerTest`，覆蓋 tracked item 扣庫、untracked item 跳過與訂單防重。

## Update Verification 2026-05-11 02:51

- `mvn -pl module-pos-inventory,module-pos-core,module-pos-product -am test`：通過。
- `mvn -pl app -am test`：通過。
- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml up -d --build backend frontend`：通過。
- `curl http://localhost:38080/actuator/health`：回傳 `{"status":"UP"}`。
- DB 驗證：
  - Demo 商品 `DEMO-*` 皆為 `track_inventory=true`，並有門店庫存。
  - 成功付款後 `DEMO-LATTE-12OZ` 從 `100.000` 扣為 `99.000`。
  - `pos_inv_stock_movements` 有 `SALE`、`quantity_change=-1.000`、`reference_type=pos_orders`。
  - 將 `DEMO-AMERICANO-12OZ` 測試庫存設為 0 後嘗試付款，`pos_payment_transactions` 與 `COMPLETED` 訂單數未增加；測試後已恢復 demo 庫存。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」。
  - `/pos/checkout` 現金收款輸入 `130`，付款前顯示找零 `$4`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - `/pos/inventory` 顯示庫存總覽，扣庫後可看到 `99.00`。
  - 將美式咖啡庫存設為 0 後重新從瀏覽器結帳，畫面顯示「庫存不足」，未顯示付款完成。

## Update 2026-05-11 02:09

- `backend/module-pos-core/src/main/java/com/enterprise/core/event/OrderCompletedEvent.java`
  - 訂單完成事件新增 `payMethod`、`paidAmount`、`tenderedAmount`、`changeGiven`。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/OrderService.java`
  - `completeOrder` 發布事件時帶入實際付款方式、應付金額、實收金額與找零。
- `backend/module-pos-payment/src/main/java/com/enterprise/payment/service/PaymentService.java`
  - `OrderCompletedEvent` after-commit 監聽改為建立正式 `PaymentTransaction`。
  - 現金付款會解析或建立門店 `CASH` 支付方式，並透過 `CashGateway` 產生 gateway reference。
  - 避免同一訂單重複建立 payment transaction。
- `backend/module-pos-payment/src/test/java/com/enterprise/payment/service/PaymentServiceTest.java`
  - 新增現金付款寫入 `methodType`、`amount`、`tendered`、`changeGiven` 的單元測試。
  - 新增既有交易不重複建立的測試。
- `frontend-web/src/features/pos-payment/api/paymentApi.ts`
  - 修正 Axios interceptor 已回傳 `ApiResponse` body 後，payment API 的 TypeScript generic 型別。
- `frontend-web/src/features/pos-payment/pages/PayMethodSettingsPage.tsx`
  - 修正支付方式列表讀取 `res.data`。
- `frontend-web/src/features/pos-payment/pages/ReconciliationPage.tsx`
  - 修正對帳列表與產生對帳時讀取 `res.data`。
- `frontend-web/src/features/pos-orders/pages/OrderListPage.tsx`
  - 已完成訂單新增「付款記錄」按鈕。
  - 對話框可查同一訂單的付款方式、狀態、付款金額、實收、找零、閘道編號與處理時間。
- `需求/開發進度對照.md`
  - 將 Sprint 1-3 payment transaction checkpoint 標記為完成。
  - 保留 cash drawer、reconciliation、多支付方式端到端為後續 checkpoint。

## Update Verification 2026-05-11 02:09

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `mvn -pl module-pos-payment,module-pos-core -am test`（backend）：通過。
- `docker compose -f docker/local/docker-compose.yml build backend frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps backend frontend`：通過。
- `curl http://localhost:38080/actuator/health`：回傳 `{"status":"UP"}`。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」。
  - `/pos/checkout` 現金收款輸入 `130`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - `/pos/orders` 對最新完成訂單開啟「付款記錄」。
  - 前端付款記錄顯示：`CASH`、`SUCCESS`、付款金額 `$126`、實收 `$130`、找零 `$4`、gateway ref `CASH-...`。
  - POS API 回應皆為 200，包含 `/api/v1/pos/payments/orders/{orderId}`。
  - 截圖輸出：`/tmp/pos-payment-complete.png`、`/tmp/pos-payment-transaction-dialog.png`。
- 資料庫驗證：
  - 訂單 `000000-20260510180850-9266`：`status=COMPLETED`、`grand_total=126.00`、`paid_total=126.00`、`change_given=4.00`。
  - `pos_payment_transactions`：`method_type=CASH`、`amount=126.00`、`tendered=130.00`、`change_given=4.00`、`status=SUCCESS`。

## Update 2026-05-11 02:36

- `backend/module-pos-core/src/main/java/com/enterprise/core/event/OrderCompletedEvent.java`
  - 訂單完成事件新增 `taxAmount`，保留既有 constructor 相容 payment 模組。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/OrderService.java`
  - `completeOrder` 發布事件時帶入 `order.getTaxTotal()`，讓 tax 模組以訂單實際稅額開票。
- `backend/module-pos-tax/src/main/java/com/enterprise/tax/service/InvoiceService.java`
  - `OrderCompletedEvent` after-commit 監聽改為用訂單總額與稅額建立 mock invoice。
  - 若沒有發票字軌，會保存 `fullInvoiceNo=null` 的 mock invoice，不再讓字軌缺失造成交易 rollback-only。
  - 發票 `salesAmount + taxAmount = totalAmount`，且 `taxAmount` 與 `pos_orders.tax_total` 一致。
- `backend/module-pos-tax/src/test/java/com/enterprise/tax/service/InvoiceServiceTest.java`
  - 新增訂單完成自動開票金額一致性測試。
  - 新增同一訂單已有發票時不重複建立測試。
- `frontend-web/src/features/pos-tax/api/taxApi.ts`
  - 改用專案共用 `axiosInstance` 與 `ApiResponse` 型別，走 `/api` baseURL 與 JWT interceptor。
- `frontend-web/src/features/pos-tax/pages/InvoicePage.tsx`
  - 修正發票列表讀取 `res.data`。
  - 改用 `DEFAULT_STORE_ID` fallback，避免 Docker build 未注入 `VITE_DEFAULT_STORE_ID` 時查詢缺 `storeId`。
- `frontend-web/src/features/pos-tax/pages/TaxClassSettingsPage.tsx`
  - 修正 tax class API 回應讀取與門店 ID fallback。
- `frontend-web/src/features/pos-tax/pages/InvoiceTrackPage.tsx`
  - 修正 invoice track API 回應讀取與門店 ID fallback。
- `frontend-web/src/App.tsx`
  - 新增 `/pos/invoices` 路由。
- `frontend-web/src/layouts/PosLayout.tsx`
  - POS 側邊導覽新增「發票」入口。
- `需求/開發進度對照.md`
  - 將 Sprint 1-4 tax happy path 標記為本 checkpoint 完成。
  - 下一步改為 Checkpoint 3 inventory 扣庫存端到端。

## Update Verification 2026-05-11 02:36

- `mvn -pl module-pos-tax,module-pos-core -am test`（backend）：通過。
- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml up -d --build backend frontend`：通過；backend/frontend 容器已重建並啟動。
- `curl http://localhost:38080/actuator/health`：HTTP 200。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」。
  - `/pos/checkout` 現金收款輸入 `130`，畫面顯示找零 `$4`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - `/pos/invoices` 點擊查詢後可看到 mock invoice。
  - 前端發票列表顯示：`（未分配字軌）`、`B2C`、稅前 `NT$ 120.00`、稅額 `NT$ 6.00`、含稅總額 `NT$ 126.00`、狀態 `有效`、上傳 `已上傳`。
  - POS API 回應皆為 200，包含 `/api/v1/pos/orders`、`/api/v1/pos/orders/{id}/complete`、`/api/v1/pos/invoices`。
- 資料庫驗證：
  - 訂單 `000000-20260510183637-6093`：`grand_total=126.00`、`tax_total=6.00`、`change_given=4.00`。
  - `pos_invoices`：`sales_amount=120.00`、`tax_amount=6.00`、`total_amount=126.00`、`status=ISSUED`、`upload_status=SUCCESS`。

## Update 2026-05-11 03:08

- Checkpoint 4：正式化 staff shift / cash drawer / reconciliation 現金閉環。
- `backend/module-pos-core/src/main/java/com/enterprise/core/event/OrderCompletedEvent.java`
  - 訂單完成事件新增 `terminalId`、`employeeId`，保留既有 constructor 相容既有測試與模組。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/OrderService.java`
  - `completeOrder` 發布事件時帶入訂單的 terminal / employee，讓支付、班次與錢櫃能對齊同一筆收銀資料。
- `backend/module-pos-payment/src/main/java/com/enterprise/payment/event/PaymentProcessedEvent.java`
  - 新增 `terminalId`、`employeeId`，提供 cash drawer 監聽器定位開啟中的錢櫃與操作員。
- `backend/module-pos-payment/src/main/java/com/enterprise/payment/service/PaymentService.java`
  - 由 `OrderCompletedEvent` 建立 payment transaction 後，同步發布帶 terminal / employee 的 `PaymentProcessedEvent`。
- `backend/module-pos-payment/src/main/java/com/enterprise/payment/service/CashDrawerService.java`
  - 監聽成功 CASH payment event；若同 terminal 有 OPEN cash drawer，寫入 `SALE` event，金額等於付款 transaction amount，orderId 對應完成訂單。
- `backend/module-pos-staff/src/main/java/com/enterprise/staff/service/StaffEventListener.java`
  - 現金訂單完成後累計開放班次的 `total_sales`、`total_tax` 與 `transaction_count`。
- `frontend-web/src/App.tsx`、`frontend-web/src/layouts/PosLayout.tsx`
  - POS 導覽新增「班次」與「對帳」，路由接 `/pos/shifts`、`/pos/reconciliation`。
- `frontend-web/src/features/pos-staff/pages/ShiftPage.tsx`
  - 開班時同步嘗試開啟 cash drawer；關班時同步關閉同 terminal 開放錢櫃。
  - 表單預設使用專案 demo store / terminal / employee ID，避免 Docker build 未注入 env 時無法測試。
- `frontend-web/src/features/pos-payment/pages/ReconciliationPage.tsx`
  - 對帳查詢與產生對帳改用 demo store fallback；確認人員預設 demo employee。
- `frontend-web/src/features/pos-staff/api/staffApi.ts`、`frontend-web/src/features/pos-staff/pages/ZReportPage.tsx`
  - 修正 axios interceptor 已回傳 `ApiResponse` body 後的型別與資料讀取。
- 測試：
  - 更新 `PaymentServiceTest`，確認 payment event 會攜帶 terminal / employee。
  - 新增 `CashDrawerServiceTest`，覆蓋 CASH payment 寫入 SALE event，以及非現金 payment 不寫入錢櫃事件。
- `需求/開發進度對照.md`
  - 將 Checkpoint 4 標記完成。
  - 下一步改為 Checkpoint 5 訂單查詢與退款閉環確認。

## Update Verification 2026-05-11 03:08

- `mvn -pl module-pos-payment,module-pos-staff,module-pos-core -am test`：通過。
- `mvn -pl app -am test`：通過。
- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml up -d --build backend frontend`：通過。
- `curl http://localhost:38080/actuator/health`：回傳 `{"status":"UP"}`。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/shifts` 開班成功，並同步開啟 cash drawer。
  - `/pos/register` 加入「拿鐵 12oz」。
  - `/pos/checkout` 現金收款輸入 `130`，付款前顯示找零 `$4`。
  - 點擊「確認付款方式」後顯示「付款完成」，訂單 `000000-20260510190416-5807` 完成。
  - `/pos/reconciliation` 產生對帳後顯示 `CASH`、交易筆數 `8`、交易金額 `NT$ 1,008.00`。
  - `/pos/shifts` 關班成功。
- DB 驗證：
  - `pos_payment_transactions` CASH 成功交易由 `7` 筆增加為 `8` 筆，合計 `1008.00`。
  - 最新現金交易 `amount=126.00`、`tendered=130.00`、`change_given=4.00`。
  - `pos_cash_drawer_events` 新增 `SALE`，`amount=126.00` 且 `order_id` 對應最新完成訂單。
  - 最新班次 `SH202605110303320000` 已關班：`total_sales=126.00`、`total_tax=6.00`、`transaction_count=1`、`closing_cash=1126.00`、`cash_variance=0.00`。
  - 最新 cash drawer 已關閉：`opening_amount=1000.00`、`closing_amount=1126.00`、`expected_amount=1126.00`、`variance=0.00`。

## Git 提交記錄

- 2026-05-11 03:09 CST
  - Hash: `1488e5e`
  - Message: `feat(pos-staff): connect cash drawer reconciliation flow`
  - Stats: 17 files changed, 324 insertions(+), 45 deletions(-)
  - Scope: Checkpoint 4 staff shift / cash drawer / reconciliation。
- 2026-05-11 03:10 CST
  - Hash: `e301c14`
  - Message: `docs(devlog): log staff cash drawer checkpoint commit`
  - Stats: 1 file changed, 8 insertions(+)
  - Push: `git push origin feature`，遠端更新 `d802e5a..e301c14`。

## Update 2026-05-11 03:13

- Checkpoint 5：訂單查詢與退款閉環確認。
- `frontend-web/src/features/pos-orders/pages/OrderListPage.tsx`
  - 訂單列表新增「付款狀態」與「發票狀態」欄位。
  - 對 `COMPLETED` / `CLOSED` 訂單使用既有 `paymentApi.getByOrder` 與 `invoiceApi.getByOrder` 查詢狀態。
  - 付款狀態顯示 `已付款`、`未付款`、`已退款`、`付款異常`。
  - 發票狀態顯示 `已開立`、`已作廢`、`折讓`、`未開立`。
- `需求/開發進度對照.md`
  - 將 Checkpoint 5 標記完成。
  - 下一步改為 Checkpoint 6 前端與後端測試基線。

## Update Verification 2026-05-11 03:13

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `mvn -pl module-pos-core,module-pos-tax,module-pos-payment -am test`：通過。
- `mvn -pl app -am test`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過；Compose 連帶重建 backend/frontend，容器已啟動。
- `curl http://localhost:38080/actuator/health`：回傳 `{"status":"UP"}`。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/orders` 可看到完成訂單 `000000-20260510190416-5807`，列表欄位含「付款狀態」「發票狀態」。
  - 退款前該訂單顯示 `已付款`、`已開立`。
  - 從訂單列表點「退款」進入 `/pos/refunds`，提交全額退款 `126`，狀態先為 `APPROVED`。
  - 點「確認退款完成」後狀態為 `COMPLETED`。
  - 回到 `/pos/orders`，同一訂單發票狀態顯示 `已作廢`。
- DB 驗證：
  - `pos_order_refunds`：`RF-20260510191222-6508`，`status=COMPLETED`，`refund_amount=126.00`。
  - `pos_invoices`：同一訂單 `status=VOIDED`，`void_reason=全額退款`。
  - backend log 顯示 `InventoryEventListener` 收到退款完成事件並觸發庫存回補。

## Git 提交記錄

- 2026-05-11 03:14 CST
  - Hash: `0e5e441`
  - Message: `feat(pos-orders): show payment invoice status`
  - Stats: 3 files changed, 148 insertions(+), 8 deletions(-)
  - Scope: Checkpoint 5 訂單查詢與退款閉環確認。
