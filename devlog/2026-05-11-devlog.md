# 2026-05-11 Devlog

## 開發規劃 (Planning)

- 完成 POS 收銀台折扣功能，讓購物車的「折扣」按鈕可實際套用固定金額與百分比折扣。
- 確保折扣會同步到結帳頁、建立訂單 API、付款完成流程與資料庫訂單金額。
- 修正並驗證 TWD 現金結帳金額四捨五入後，不會產生不合理找零。

## 專案結構 (Project Structure)

```text
POS/
├── backend/
│   ├── app/                         # Spring Boot 啟動模組與全域配置
│   ├── module-auth/                 # 登入、JWT、POS PIN 驗證
│   ├── module-common/               # 共用 DTO、例外與基礎實體
│   ├── module-organization/         # 公司、門市、終端機、組織資料
│   ├── module-pos-core/             # POS 訂單、定價、付款完成核心流程
│   ├── module-pos-product/          # 商品與分類
│   ├── module-pos-inventory/        # 庫存與盤點
│   ├── module-pos-payment/          # 支付交易資料
│   ├── module-pos-tax/              # 發票與稅務事件
│   └── module-pos-staff/            # POS 員工資料
├── frontend-web/
│   └── src/
│       ├── features/pos-auth/       # POS PIN 登入與終端登入 API
│       ├── features/pos-orders/     # 收銀台、購物車、結帳、退款、訂單列表
│       ├── features/pos-products/   # 商品與分類管理
│       ├── layouts/                 # POS 與後台 Layout
│       └── shared/                  # API、auth store、工具函式與共用元件
├── docker/local/                    # 本地 Docker Compose、前後端 Dockerfile、nginx
├── env/local/                       # 本地環境變數
├── devlog/                          # 開發日誌與 troubleshooting
├── 需求/                            # 需求、流程、頁面結構與規格文件
└── 我的開發模板/                    # 專案模板與多 AI 接手文件
```

## 進度 (Progress)

- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `discountAmount` 狀態、`setDiscountAmount`、`clearDiscount`。
  - `calculateCartTotals` 支援折扣上限保護，折扣不可小於 0 且不可超過小計。
  - 總計改為 TWD 整數金額，與後端 `rounding_adj` / `grand_total` 對齊。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 「折扣」按鈕改為可操作功能。
  - 新增折扣 Dialog，支援 5%、10%、20% 快捷折扣、自訂金額、清除折扣與預覽折扣後小計。
  - 購物車摘要會顯示實際折扣與折扣後總計。
- `frontend-web/src/features/pos-orders/pages/CheckoutPage.tsx`
  - 結帳頁讀取購物車折扣狀態並帶入訂單建立 payload。
  - 現金付款送出的金額與畫面顯示的 TWD 整數總計一致。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/PricingEngine.java`
  - 後端定價引擎對負數折扣與超過小計的折扣做保護。
- `backend/module-pos-core/src/test/java/com/enterprise/core/service/PricingEngineTest.java`
  - 新增折扣超過小計、負數折扣的單元測試。

## 驗證 (Verification)

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `mvn -pl module-pos-core -am test`（backend）：通過。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」後總計 `$126`。
  - 套用 10% 折扣後購物車顯示折扣 `-$12`、結帳按鈕 `$113`。
  - `/pos/checkout` 顯示訂單摘要、折扣 `-$12`、應收 `$113`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`discount_total=12.00`、`tax_total=5.40`、`rounding_adj=-0.40`、`grand_total=113.00`、`paid_total=113.00`、`change_given=0.00`。

## 問題紀錄 (Issues)

- 第一次 Docker 重建使用錯誤 compose 路徑，導致前端容器仍是舊 bundle，付款時送出 `113.40` 並產生 `change_given=0.40`。
- 正確流程是只重建並重啟 frontend：`docker compose -f docker/local/docker-compose.yml build frontend`，再 `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`。
- `docker compose ... up -d --build frontend` 會因 `depends_on: backend` 嘗試重建 backend；若 Docker 內 Maven DNS 暫時無法解析 `repo.maven.apache.org`，會讓 frontend 更新被 backend build 失敗擋住。

## Update 2026-05-11 00:53

- `frontend-web/src/features/pos-orders/config.ts`
  - 新增本地 demo 會員資料，包含會員 ID、會員編號、遮罩手機、等級、點數與會員折扣率。
- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `selectedMember`、`setMember`、`clearMember`。
  - 會員綁定後會依會員折扣率自動計算折扣。
  - 已綁定會員時，新增、增加、減少或移除商品會重新計算會員折扣。
  - 手動折扣與會員折扣暫時互斥，避免尚未接促銷引擎前出現折扣疊加規則不清。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 「會員」按鈕改為可操作，支援空購物車先綁會員。
  - 新增會員 Dialog，可依姓名、手機或會員編號搜尋 demo 會員。
  - 綁定會員後購物車顯示會員姓名、等級與自動折扣。
- `frontend-web/src/features/pos-orders/pages/CheckoutPage.tsx`
  - 結帳頁顯示會員摘要與會員折扣 chip。
  - 建立訂單 payload 帶入 `memberId`。

## Update Verification 2026-05-11 00:53

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - 在空購物車狀態先綁定「金卡 林依晨」。
  - 加入「拿鐵 12oz」後自動套用 10% 會員折扣，顯示 `-$12` 與結帳 `$113`。
  - `/pos/checkout` 顯示 `金卡 · 林依晨 · 1,280 點` 與 `10% 會員折扣`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`member_id=00000000-0000-0000-0000-000000000801`、`discount_total=12.00`、`tax_total=5.40`、`rounding_adj=-0.40`、`grand_total=113.00`、`paid_total=113.00`、`change_given=0.00`。

## Update 2026-05-11 01:04

- `frontend-web/src/features/pos-orders/pages/CheckoutPage.tsx`
  - 現金付款新增「現金收款」區塊。
  - 支援收款金額輸入、剛好付款、`+$10`、`+$20`、`+$50` 快捷按鈕。
  - 即時計算找零或不足金額。
  - 現金收款不足時禁止確認付款並顯示錯誤。
  - 呼叫付款完成 API 時，現金付款改送實際 `cashTendered`，非現金付款維持不送 tendered。

## Update Verification 2026-05-11 01:04

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - 空購物車先綁定「金卡 林依晨」。
  - 加入「拿鐵 12oz」後自動套用 10% 會員折扣，結帳金額 `$113`。
  - `/pos/checkout` 現金收款輸入 `120` 後顯示找零 `$7`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`member_id=00000000-0000-0000-0000-000000000801`、`discount_total=12.00`、`tax_total=5.40`、`rounding_adj=-0.40`、`grand_total=113.00`、`paid_total=113.00`、`change_given=7.00`。

## Update 2026-05-11 01:06

- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `HeldOrder` 資料結構與 `heldOrders` 狀態。
  - 新增 `holdCurrentOrder`、`restoreHeldOrder`、`removeHeldOrder`。
  - 掛單資料寫入 `localStorage` 的 `pos-held-orders`，支援瀏覽器刷新後取回。
  - 掛單會保存品項、稅率、折扣、會員、總品項數、總金額與建立時間。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 右上角暫停圖示改為「掛單與取單」入口。
  - 有目前訂單時可一鍵掛起並清空當前購物車。
  - 無目前訂單時可從掛單列表取回。
  - 支援刪除不需要的掛單。

## Update Verification 2026-05-11 01:06

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - 加入「拿鐵 12oz」後進入「掛單與取單」並點擊「掛起」。
  - 購物車清空且 `localStorage.pos-held-orders` 寫入 1 筆掛單，金額 `126`。
  - 刷新 `/pos/register` 後仍可看到掛單並取回。
  - 取回後 `localStorage.pos-held-orders` 清空，購物車恢復 `$126` 訂單。
  - 結帳頁輸入現金收款 `130`，顯示找零 `$4`，付款完成。
  - POS API 回應皆為 200。
  - 資料庫最新訂單：`discount_total=0.00`、`tax_total=6.00`、`rounding_adj=0.00`、`grand_total=126.00`、`paid_total=126.00`、`change_given=4.00`。

## Update 2026-05-11 01:08

- 新增 `需求/開發進度對照.md`。
- 將 `開發流程規劃.md` 的 Phase/Sprint 與目前實作狀態對齊。
- 標記已完成、MVP 已完成但需正式化、未開始與下一步建議。
- 明確下一步建議：
  1. 回到 Sprint 1-2 正式化後端掛單 API。
  2. 接 Sprint 1-3 payment transaction / cash drawer / reconciliation。
  3. 接 Sprint 1-4 tax mock invoice happy path。
  4. 補 Phase 0 前端測試基線。

## Update Verification 2026-05-11 01:08

- 本次為文檔整理，未修改執行程式碼。
- `需求/開發進度對照.md` 已建立，可作為後續「繼續開發」前的進度入口。

## Update 2026-05-11 01:23

- `backend/module-pos-core/src/main/java/com/enterprise/core/repository/HeldOrderRepository.java`
  - 新增 `pos_held_orders` Repository，支援依門市與終端機查詢掛單。
- `backend/module-pos-core/src/main/java/com/enterprise/core/dto/request/CreateHeldOrderRequest.java`
  - 新增建立掛單 request，包含 `storeId`、`terminalId`、`label` 與 JSON payload。
- `backend/module-pos-core/src/main/java/com/enterprise/core/dto/response/HeldOrderResponse.java`
  - 新增掛單 response，統一回傳後端掛單 ID、標籤、payload 與時間欄位。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/HeldOrderService.java`
  - 新增建立、查詢、刪除掛單流程。
  - 查詢可依 `terminalId` 過濾；刪除不存在掛單時回傳 `ResourceNotFoundException`。
- `backend/module-pos-core/src/main/java/com/enterprise/core/controller/HeldOrderController.java`
  - 新增 `/api/v1/pos/held-orders`。
  - 支援 `POST` 建立、`GET` 查詢、`DELETE /{id}` 刪除。
- `backend/module-pos-core/src/test/java/com/enterprise/core/service/HeldOrderServiceTest.java`
  - 補建立掛單、依門市與終端機查詢、刪除掛單單元測試。
- `frontend-web/src/features/pos-orders/api/orderApi.ts`
  - 新增 `heldOrderApi.create/list/remove`。
- `frontend-web/src/features/pos-orders/types/index.ts`
  - 新增 `HeldOrderResponse` 與 `CreateHeldOrderRequest` 型別。
- `frontend-web/src/features/pos-orders/store/cartStore.ts`
  - 新增 `replaceHeldOrders`、`replaceHeldOrder`，讓 API 回傳的後端掛單 ID 可取代本地暫存 ID。
- `frontend-web/src/features/pos-orders/components/Cart.tsx`
  - 掛單 Dialog 開啟時會從後端載入掛單。
  - 掛起目前訂單時先保留本地 fallback，再呼叫後端建立掛單。
  - 取回或刪除後端掛單時同步呼叫刪除 API。
  - 若 API 暫時失敗，仍保留 localStorage 掛單，避免收銀現場遺失當前訂單。
- `需求/開發進度對照.md`
  - 將掛單狀態由前端 MVP 更新為後端 API 基礎正式化。
  - 下一步改為 Sprint 1-3 payment transaction。

## Update Verification 2026-05-11 01:23

- `mvn -pl module-pos-core -am test`（backend）：通過。
- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml build backend frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps backend frontend`：通過。
- `curl http://localhost:38080/actuator/health`：回傳 `{"status":"UP"}`。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」後總計 `$126`。
  - 點擊「掛單與取單」並掛起目前訂單，後端 `pos_held_orders` active 筆數為 1。
  - 重新整理 `/pos/register` 後仍停留收銀台，登入資料有持久化。
  - 再次開啟「掛單與取單」可從後端掛單取回訂單。
  - 取回後後端 `pos_held_orders` active 筆數為 0。
  - `/pos/checkout` 現金收款輸入 `130`，顯示找零 `$4`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - 資料庫最新訂單：`status=COMPLETED`、`subtotal=120.00`、`discount_total=0.00`、`tax_total=6.00`、`grand_total=126.00`、`paid_total=126.00`、`change_given=4.00`。
  - 截圖輸出：`/tmp/pos-held-order-api-after-hold.png`、`/tmp/pos-held-order-api-restored.png`、`/tmp/pos-held-order-api-complete.png`。

## Git 提交記錄

- 2026-05-11 01:41:22 CST
  - Hash: `f9e48c6`
  - 訊息: `feat(pos): stabilize checkout terminal flow`
  - 推送: `origin/feature`
  - 變更統計: 35 files changed, 2350 insertions(+), 162 deletions(-)
  - 摘要: 備份 POS PIN 登入、登入持久化、商品載入修正、折扣、會員 MVP、現金收款找零、後端掛單 API、前端掛單串接、進度文件與 devlog。

## Update 2026-05-11 02:09

- `backend/module-pos-core/src/main/java/com/enterprise/core/event/OrderCompletedEvent.java`
  - 訂單完成事件新增 `payMethod`、`paidAmount`、`tenderedAmount`、`changeGiven`。
- `backend/module-pos-core/src/main/java/com/enterprise/core/service/OrderService.java`
  - `completeOrder` 發布事件時帶入實際付款方式、應付金額、實收金額與找零。
- `backend/module-pos-payment/src/main/java/com/enterprise/payment/service/PaymentService.java`
  - `OrderCompletedEvent` after-commit 監聽改為建立正式 `PaymentTransaction`。
  - 現金付款會解析或建立門店 `CASH` 支付方式，並透過 `CashGateway` 產生 gateway reference。
  - 避免同一訂單重複建立 payment transaction。
- `backend/module-pos-payment/src/test/java/com/enterprise/payment/service/PaymentServiceTest.java`
  - 新增現金付款寫入 `methodType`、`amount`、`tendered`、`changeGiven` 的單元測試。
  - 新增既有交易不重複建立的測試。
- `frontend-web/src/features/pos-payment/api/paymentApi.ts`
  - 修正 Axios interceptor 已回傳 `ApiResponse` body 後，payment API 的 TypeScript generic 型別。
- `frontend-web/src/features/pos-payment/pages/PayMethodSettingsPage.tsx`
  - 修正支付方式列表讀取 `res.data`。
- `frontend-web/src/features/pos-payment/pages/ReconciliationPage.tsx`
  - 修正對帳列表與產生對帳時讀取 `res.data`。
- `frontend-web/src/features/pos-orders/pages/OrderListPage.tsx`
  - 已完成訂單新增「付款記錄」按鈕。
  - 對話框可查同一訂單的付款方式、狀態、付款金額、實收、找零、閘道編號與處理時間。
- `需求/開發進度對照.md`
  - 將 Sprint 1-3 payment transaction checkpoint 標記為完成。
  - 保留 cash drawer、reconciliation、多支付方式端到端為後續 checkpoint。

## Update Verification 2026-05-11 02:09

- `npm run lint`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `mvn -pl module-pos-payment,module-pos-core -am test`（backend）：通過。
- `docker compose -f docker/local/docker-compose.yml build backend frontend`：通過。
- `docker compose -f docker/local/docker-compose.yml up -d --no-deps backend frontend`：通過。
- `curl http://localhost:38080/actuator/health`：回傳 `{"status":"UP"}`。
- Playwright 瀏覽器實測：
  - `/pos/login` PIN `1234` 登入成功。
  - `/pos/register` 加入「拿鐵 12oz」。
  - `/pos/checkout` 現金收款輸入 `130`。
  - 點擊「確認付款方式」後顯示「付款完成」。
  - `/pos/orders` 對最新完成訂單開啟「付款記錄」。
  - 前端付款記錄顯示：`CASH`、`SUCCESS`、付款金額 `$126`、實收 `$130`、找零 `$4`、gateway ref `CASH-...`。
  - POS API 回應皆為 200，包含 `/api/v1/pos/payments/orders/{orderId}`。
  - 截圖輸出：`/tmp/pos-payment-complete.png`、`/tmp/pos-payment-transaction-dialog.png`。
- 資料庫驗證：
  - 訂單 `000000-20260510180850-9266`：`status=COMPLETED`、`grand_total=126.00`、`paid_total=126.00`、`change_given=4.00`。
  - `pos_payment_transactions`：`method_type=CASH`、`amount=126.00`、`tendered=130.00`、`change_given=4.00`、`status=SUCCESS`。
