# 2026-05-17 Devlog

## POS Checkout Receipt Snapshot Polish

- 時間：2026-05-17 14:38:35 CST。
- 目標：打磨 POS 結帳完成後的收銀員確認體驗，避免付款完成後因購物車清空導致畫面摘要消失或現金找零顯示錯誤。

## Planning

- 檢查收銀、會員、促銷與結帳近期改動後的邊界體驗。
- 修正付款完成後仍需呈現剛完成訂單摘要的 UI 狀態。
- 補測付款完成後購物車已清空但畫面仍保留商品、會員、促銷與找零資訊。
- 重新建置 frontend container，並使用瀏覽器跑實際付款完成流程。

## Project Structure

```text
POS/
├── backend/                         # Spring Boot 多模組後端
├── frontend-web/                    # React/Vite POS Web 前端
│   ├── src/features/pos-orders/     # 收銀台、結帳、訂單、退款與掛單功能
│   └── test/pos-orders/             # POS 訂單與結帳回歸測試
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數
├── 需求/                            # 需求、規格、進度與驗收文件
├── docs/                            # 專案地圖與交接文件
├── devlog/                          # 每日開發日誌與 troubleshooting
├── scripts/                         # 初始化與輔助腳本
├── reference/模塊化組件/             # AI 參考用模組化組件匯出資料
└── tmp/                             # 本地驗證截圖與臨時產物，不進版控
```

## Progress

- 前端：
  - `CheckoutPage` 新增付款完成快照 `CheckoutReceiptSnapshot`。
  - 建立訂單與完成付款成功後，先保存目前品項、金額、會員、折扣來源與促銷資訊，再清空購物車。
  - 訂單摘要、會員 chip、促銷 chip、金額彙總與現金收款區改讀取完成快照，避免付款完成畫面顯示「購物車是空的」。
  - 現金找零改用摘要快照總額計算，避免清空購物車後把應收視為 `$0`，導致找零錯顯為整筆收款額。
- 測試：
  - 擴充 `CheckoutPage.test.tsx`，確認付款完成後仍顯示品項與正確找零，同時購物車 state 已清空。
  - 擴充促銷付款測試，確認付款完成後仍顯示會員資訊與促銷 chip。

## Verification - POS Checkout Receipt Snapshot Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/CheckoutPage.test.tsx`：通過，1 file / 3 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 23 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/register`，加入 `美式咖啡 12oz`。
  - 進入 `http://127.0.0.1:38182/pos/checkout`，現金付款完成。
  - 付款完成畫面顯示訂單號 `000000-20260517144557-06854`、`付款完成`、`美式咖啡 12oz`、`數量：1`、應收 `$95`。
  - 付款完成畫面未顯示 `購物車是空的`。
  - 現金收款區找零顯示 `$0`，確認不再因購物車清空而錯顯 `$95`。
  - in-app browser 截圖管線仍回 `Page.captureScreenshot` timeout；本輪以 DOM snapshot 與實際互動結果完成驗證。

## Git 提交記錄

- 時間：2026-05-17 14:48:02 CST
- Hash：`0f86d69`
- 訊息：`fix(pos): preserve checkout receipt after payment`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 60 insertions(+), 18 deletions(-)

## POS Checkout Completion Action Polish

- 時間：2026-05-17 14:54:33 CST。
- 目標：延續付款完成摘要修正，讓付款完成後的操作狀態更明確，避免收銀員誤以為仍可變更付款方式或收款金額。

## Progress - POS Checkout Completion Action Polish

- 前端：
  - `CheckoutPage` 新增 `paymentCompleted` 狀態衍生。
  - 付款完成後鎖定付款方式按鈕、現金收款輸入與加鈔快捷鍵。
  - 主付款按鈕在完成後改顯示 `付款已完成` 並保持 disabled。
  - 返回按鈕在完成後改顯示 `返回收銀台`，並固定導回 `/pos/register`。
- 測試：
  - 擴充 `CheckoutPage.test.tsx`，覆蓋付款完成後主按鈕 disabled、顯示 `返回收銀台`，且收款金額欄位 disabled。

## Verification - POS Checkout Completion Action Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/CheckoutPage.test.tsx`：通過，1 file / 3 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 23 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/register`，加入 `美式咖啡 12oz` 兩杯，自動套用 `咖啡滿百 9 折`。
  - 進入結帳並完成現金付款。
  - 付款完成畫面顯示訂單號 `000000-20260517145729-74938`、品項 `美式咖啡 12oz`、促銷 `咖啡滿百 9 折`、應收 `$170` 與找零 `$0`。
  - 付款完成後 `現金` 付款方式按鈕 disabled、收款金額 input disabled、主按鈕顯示 `付款已完成` 並 disabled。
  - 付款完成後顯示 `返回收銀台` 與 `查看訂單列表` 作為明確下一步。

## Git 提交記錄 - POS Checkout Completion Action Polish

- 時間：2026-05-17 14:58:06 CST
- Hash：`10585e3`
- 訊息：`fix(pos): lock checkout controls after payment`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 19 insertions(+), 5 deletions(-)

## POS Order Number Search Polish

- 時間：2026-05-17 15:11:43 CST。
- 目標：修正訂單列表「依訂單編號搜尋」欄位只有 UI、沒有實際篩選行為的問題，避免驗收時看起來可用但輸入無效果。

## Progress - POS Order Number Search Polish

- 前端：
  - `OrderListPage` 新增 `orderNoKeyword` state。
  - 新增 `displayedOrders`，依目前載入頁面的 `orderNo` 做關鍵字篩選。
  - 桌面表格、手機卡片與本頁統計卡改用 `displayedOrders`。
  - 搜尋無結果時顯示 `找不到符合條件的訂單` 與調整關鍵字提示。
- 測試：
  - 擴充 `OrderListPage.test.tsx`，新增第二筆訂單資料。
  - 覆蓋輸入訂單尾碼後只顯示符合訂單；輸入無匹配關鍵字後顯示空狀態。

## Verification - POS Order Number Search Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，1 file / 2 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 24 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/orders`。
  - 列表載入多筆訂單，最新可見訂單包含 `000000-20260517145729-74938`。
  - 搜尋 `74938` 後，列表只保留該訂單。
  - 搜尋 `NO-MATCH-ORDER` 後，顯示 `找不到符合條件的訂單` 與 `請調整訂單編號關鍵字或清除搜尋。`。

## Git 提交記錄 - POS Order Number Search Polish

- 時間：2026-05-17 15:15:49 CST
- Hash：`7bb6119`
- 訊息：`fix(pos): enable order number search`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 69 insertions(+), 13 deletions(-)

## POS New Order Action Polish

- 時間：2026-05-17 18:48:07 CST。
- 目標：修正訂單列表右上角 `新增訂單` 按鈕只有視覺狀態、沒有實際導頁行為的問題，讓收銀員可從訂單列表直接開始下一筆訂單。

## Progress - POS New Order Action Polish

- 前端：
  - `OrderListPage` 的 `新增訂單` 按鈕新增 `onClick`。
  - 點擊後固定導向 `/pos/register`。
- 測試：
  - 擴充 `OrderListPage.test.tsx`，mock `useNavigate()` 並驗證點擊 `新增訂單` 會呼叫 `navigate('/pos/register')`。

## Verification - POS New Order Action Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，1 file / 3 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 25 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/orders`。
  - 點擊 `新增訂單`。
  - 頁面導向 `http://127.0.0.1:38182/pos/register`，並顯示 `收銀台`、`目前訂單` 與 `立即結帳`。

## Git 提交記錄 - POS New Order Action Polish

- 時間：2026-05-17 18:52:03 CST
- Hash：`d8ecddf`
- 訊息：`fix(pos): wire new order action`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 21 insertions(+)

## Flutter Android Toolchain Setup

- 時間：2026-05-17 19:29:43 CST。
- 目標：依使用者要求先處理 POS 手機端 Android-first Flutter 開發環境，確認本機需要安裝的工具並完成可建置、可啟動的 smoke verification。

## Planning - Flutter Android Toolchain Setup

- 使用 subagent 分別盤點 POS 專案 Flutter app 現況與本機 Android/Flutter toolchain。
- 依官方 Flutter / Android 文件確認 macOS Android 開發必要項目。
- 安裝 Flutter SDK、Android Studio、Android SDK command-line tools。
- 安裝 Android SDK platform、build-tools、platform-tools、emulator、ARM64 system image。
- 建立 Android tablet AVD，並用 Flutter sample app 驗證 build / install / launch。

## Project Structure - Flutter Android Toolchain Setup

```text
POS/
├── backend/                         # Spring Boot 多模組後端
├── frontend-web/                    # React/Vite POS Web 前端，目前承接 POS 主流程
├── frontend-app/                    # Flutter POS 終端規劃目錄，目前尚未建立
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數
├── 需求/                            # Flutter POS、離線同步、終端安全與驗收規格
├── docs/                            # 專案地圖與交接文件
├── devlog/                          # 每日開發日誌與 troubleshooting
├── scripts/                         # 初始化與輔助腳本
├── reference/模塊化組件/             # AI 參考用模組化組件匯出資料
└── tmp/                             # 本地驗證截圖與臨時產物，不進版控
```

## Progress - Flutter Android Toolchain Setup

- 專案盤點：
  - 確認目前 POS repo 尚無 `frontend-app/`、`pubspec.yaml`、Flutter `android/` 或 `lib/` 結構。
  - 文件顯示 Flutter POS 終端原規劃於 Phase 3，使用者本輪已明確要求先啟動 Android-first。
- 本機安裝：
  - 安裝 `flutter` cask，Flutter SDK 位於 `/opt/homebrew/share/flutter`。
  - 安裝 Android Studio `2025.3.4.7` 至 `/Applications/Android Studio.app`。
  - 安裝 `android-commandlinetools`，提供 `sdkmanager` / `avdmanager`。
  - Android SDK 設定於 `/Users/wei/Library/Android/sdk`。
  - 安裝 `platform-tools`、`emulator`、`platforms;android-36`、`build-tools;36.0.0`、`system-images;android-36;google_apis;arm64-v8a`。
  - Gradle smoke build 自動補齊 NDK `28.2.13676358`、Build Tools `35.0.0`、CMake `3.22.1`。
  - 設定 `flutter config --android-sdk /Users/wei/Library/Android/sdk`。
  - 更新 `/Users/wei/.zshrc`，加入 `ANDROID_HOME`、`ANDROID_SDK_ROOT`、`platform-tools`、`emulator`、`cmdline-tools/latest/bin` PATH。
- Emulator：
  - 建立 AVD `pos_android_tablet`，device 使用 `pixel_tablet`。

## Verification - Flutter Android Toolchain Setup

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Flutter / Android：
  - `flutter doctor -v`：Flutter、Android toolchain、Chrome、Network resources 通過；僅剩 iOS/macOS 的完整 Xcode 與 CocoaPods 缺口，Android-first 不阻塞。
  - `flutter doctor --android-licenses`：All SDK package licenses accepted。
  - `which adb emulator sdkmanager flutter dart`：皆可解析到本機安裝路徑。
  - `sdkmanager --list_installed`：確認 Android SDK 36、Build Tools 36、Platform Tools、Emulator、ARM64 Google APIs system image 已安裝。
  - `flutter emulators`：可列出 `pos_android_tablet`。
- Smoke test：
  - 在 `/tmp/pos_flutter_smoke` 建立 Flutter Android sample app，未污染 POS repo。
  - `flutter build apk --debug`：通過，產出 `build/app/outputs/flutter-apk/app-debug.apk`。
  - 啟動 `pos_android_tablet` emulator，`flutter devices` 可偵測 `emulator-5554`。
  - `flutter run -d emulator-5554`：成功 build、install、launch sample app，DevTools URL 可建立。
  - 測試後已停止 `flutter run` 與 emulator，避免背景佔用資源。

## Local Secret Cleanup

- 時間：2026-05-17 19:44:00 CST。
- 目標：處理本機 shell 設定中曾出現明文 OpenAI API key 的資安問題，移除明文環境變數並清理本機歷史快照。

## Progress - Local Secret Cleanup

- 掃描範圍：
  - POS repo 工作樹與已追蹤檔案。
  - 常見 shell 設定與 history：`.zshrc`、`.zprofile`、`.zshenv`、`.bashrc`、`.bash_profile`、`.zsh_history`、`.bash_history`。
  - Codex 本機 shell snapshots 與 session logs。
  - `.config` 與桌面專案資料夾的 OpenAI key 形態字串。
- 清理：
  - 從 `/Users/wei/.zshrc` 移除明文 `OPENAI_API_KEY` export。
  - 執行 `launchctl unsetenv OPENAI_API_KEY`，避免 launchd 層級持續暴露舊值。
  - 對 `.codex/shell_snapshots` 與 `.codex/sessions` 中的 API-key 型字串做 `[REDACTED_OPENAI_KEY]` 遮罩。
  - 未修改官方技能範例文件與 Codex `auth.json`，避免破壞工具自身登入狀態。

## Verification - Local Secret Cleanup

- POS repo：
  - `rg -l 'OPENAI_API_KEY|sk-*' /Users/wei/Desktop/code/POS`：未發現明文 OpenAI key。
  - `git grep -l 'OPENAI_API_KEY|sk-*'`：未發現已追蹤檔案含明文 OpenAI key。
- Shell / history：
  - `.zshrc` 已不包含 `OPENAI_API_KEY`。
  - 常見 shell 設定與 history 未再發現 OpenAI API key 形態字串。
- Codex local logs：
  - `.codex/shell_snapshots` 與 `.codex/sessions` 未再發現 OpenAI API key 形態字串。
- 後續必要動作：
  - 由於 key 曾寫入本機設定與歷史快照，仍需到 OpenAI API keys 頁面撤銷舊 key 並建立新 key。
  - 新 key 應存放於 secret manager 或本機 Keychain，不再寫入 shell rc、repo、devlog 或 session 可讀文字。

## Flutter App Kickoff and Web Barcode Polish

- 時間：2026-05-17 20:00:56 CST。
- 目標：開始同步開發 App 端與 Web 端；App 先建立 Android-first Flutter POS 終端骨架，Web 同步打磨收銀台掃描條碼按鈕。

## Planning - Flutter App Kickoff and Web Barcode Polish

- 主線：建立 `frontend-app/` Flutter Android app，先做可測、可建置、可在 emulator 啟動的 POS 終端首屏。
- 旁線：使用 subagent 只讀盤點 Web POS 可打磨點，優先處理收銀現場最容易誤用的死按鈕。
- 驗證：Flutter 跑 `analyze`、`test`、`build apk`、emulator 啟動；Web 跑 TypeScript、單檔測試、lint、全測試、build、Docker frontend rebuild 與瀏覽器實測。

## Project Structure - Flutter App Kickoff and Web Barcode Polish

```text
POS/
├── backend/                         # Spring Boot 多模組後端
├── frontend-web/                    # React/Vite POS Web 前端與收銀台打磨
├── frontend-app/                    # Flutter Android-first POS 終端
│   ├── android/                     # Android 平台工程
│   ├── lib/app/                     # MaterialApp 與 app shell
│   ├── lib/core/                    # 商品/購物車模型與 demo catalog
│   ├── lib/features/pos_terminal/   # 平板收銀主畫面
│   └── test/                        # Flutter widget tests
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數
├── 需求/                            # 需求、規格、進度與驗收文件
├── docs/                            # 專案地圖與交接文件
├── devlog/                          # 每日開發日誌與 troubleshooting
└── scripts/                         # 初始化與輔助腳本
```

## Progress - Flutter App Kickoff and Web Barcode Polish

- App：
  - 新增 `frontend-app/` Flutter 專案，Android package 使用 `com.xinyi.pos.xinyi_pos_app`。
  - 將預設 counter app 改為 Xinyi POS Android tablet 首屏。
  - 新增深色 POS theme、側邊 rail、線上/待同步狀態、分類 chip、商品 grid、購物車與現金結帳 CTA。
  - 新增 `Product`、`CartItem`、`demoCatalog`，先用 demo catalog 保持離線友善展示，後續再接 API / SQLite / sync。
  - 新增 Flutter widget tests，覆蓋首屏元素與加入商品後總額/按鈕狀態。
  - 修正 Android emulator 首幀零尺寸導致 `GridView` assert 的問題，加入尺寸防護。
- Web：
  - `RegisterPage` 掃描條碼按鈕新增可操作行為。
  - 點擊掃描按鈕會聚焦搜尋欄、選取既有文字並顯示「掃描模式已啟動」提示。
  - 條碼掃描器可沿用鍵盤輸入模式，輸入條碼後按 Enter 會走既有 keyword 搜尋。
  - 新增 `RegisterPage.test.tsx` 覆蓋掃描模式提示、搜尋欄 focus 與條碼 Enter 後 API keyword payload。
- 文件：
  - 更新 `README.md`、`docs/project-map.md` 與 `需求/開發進度對照.md`，把 Flutter Android app 從未開始調整為 Android-first 骨架已建立。

## Verification - Flutter App Kickoff and Web Barcode Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Flutter：
  - `dart format lib test`：通過。
  - `flutter analyze`：通過。
  - `flutter test`：通過，2 tests。
  - `flutter build apk --debug`：通過。
  - `flutter run -d emulator-5554`：成功安裝並啟動於 `pos_android_tablet`；修正後未再出現 layout exception。
- Web：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/RegisterPage.test.tsx`：通過，1 file / 1 test。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，13 files / 26 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功重建並啟動 `pos-frontend`。
- Browser：
  - 開啟 `http://127.0.0.1:38182/pos/register`。
  - 點擊掃描條碼按鈕後，畫面顯示「掃描模式已啟動，請掃描條碼或輸入條碼後按 Enter。」。
  - 瀏覽器 console error logs：空。
  - browser automation 的文字輸入受虛擬剪貼簿限制，本輪條碼 Enter payload 由 RTL 測試覆蓋。

## Git 提交記錄 - Flutter App Kickoff and Web Barcode Polish

- 時間：2026-05-17 20:04:30 CST
- Hash：`c650893`
- 訊息：`feat(pos-app): start android terminal shell`
- 變更統計：39 files changed, 1790 insertions(+), 10 deletions(-)
