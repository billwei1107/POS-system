# 2026-05-17 Devlog

## POS Checkout Receipt Snapshot Polish

- 時間：2026-05-17 14:38:35 CST。
- 目標：打磨 POS 結帳完成後的收銀員確認體驗，避免付款完成後因購物車清空導致畫面摘要消失或現金找零顯示錯誤。

## Planning

- 檢查收銀、會員、促銷與結帳近期改動後的邊界體驗。
- 修正付款完成後仍需呈現剛完成訂單摘要的 UI 狀態。
- 補測付款完成後購物車已清空但畫面仍保留商品、會員、促銷與找零資訊。
- 重新建置 frontend container，並使用瀏覽器跑實際付款完成流程。

## Project Structure

```text
POS/
├── backend/                         # Spring Boot 多模組後端
├── frontend-web/                    # React/Vite POS Web 前端
│   ├── src/features/pos-orders/     # 收銀台、結帳、訂單、退款與掛單功能
│   └── test/pos-orders/             # POS 訂單與結帳回歸測試
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數
├── 需求/                            # 需求、規格、進度與驗收文件
├── docs/                            # 專案地圖與交接文件
├── devlog/                          # 每日開發日誌與 troubleshooting
├── scripts/                         # 初始化與輔助腳本
├── reference/模塊化組件/             # AI 參考用模組化組件匯出資料
└── tmp/                             # 本地驗證截圖與臨時產物，不進版控
```

## Progress

- 前端：
  - `CheckoutPage` 新增付款完成快照 `CheckoutReceiptSnapshot`。
  - 建立訂單與完成付款成功後，先保存目前品項、金額、會員、折扣來源與促銷資訊，再清空購物車。
  - 訂單摘要、會員 chip、促銷 chip、金額彙總與現金收款區改讀取完成快照，避免付款完成畫面顯示「購物車是空的」。
  - 現金找零改用摘要快照總額計算，避免清空購物車後把應收視為 `$0`，導致找零錯顯為整筆收款額。
- 測試：
  - 擴充 `CheckoutPage.test.tsx`，確認付款完成後仍顯示品項與正確找零，同時購物車 state 已清空。
  - 擴充促銷付款測試，確認付款完成後仍顯示會員資訊與促銷 chip。

## Verification - POS Checkout Receipt Snapshot Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/CheckoutPage.test.tsx`：通過，1 file / 3 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 23 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/register`，加入 `美式咖啡 12oz`。
  - 進入 `http://127.0.0.1:38182/pos/checkout`，現金付款完成。
  - 付款完成畫面顯示訂單號 `000000-20260517144557-06854`、`付款完成`、`美式咖啡 12oz`、`數量：1`、應收 `$95`。
  - 付款完成畫面未顯示 `購物車是空的`。
  - 現金收款區找零顯示 `$0`，確認不再因購物車清空而錯顯 `$95`。
  - in-app browser 截圖管線仍回 `Page.captureScreenshot` timeout；本輪以 DOM snapshot 與實際互動結果完成驗證。

## Git 提交記錄

- 時間：2026-05-17 14:48:02 CST
- Hash：`0f86d69`
- 訊息：`fix(pos): preserve checkout receipt after payment`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 60 insertions(+), 18 deletions(-)

## POS Checkout Completion Action Polish

- 時間：2026-05-17 14:54:33 CST。
- 目標：延續付款完成摘要修正，讓付款完成後的操作狀態更明確，避免收銀員誤以為仍可變更付款方式或收款金額。

## Progress - POS Checkout Completion Action Polish

- 前端：
  - `CheckoutPage` 新增 `paymentCompleted` 狀態衍生。
  - 付款完成後鎖定付款方式按鈕、現金收款輸入與加鈔快捷鍵。
  - 主付款按鈕在完成後改顯示 `付款已完成` 並保持 disabled。
  - 返回按鈕在完成後改顯示 `返回收銀台`，並固定導回 `/pos/register`。
- 測試：
  - 擴充 `CheckoutPage.test.tsx`，覆蓋付款完成後主按鈕 disabled、顯示 `返回收銀台`，且收款金額欄位 disabled。

## Verification - POS Checkout Completion Action Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/CheckoutPage.test.tsx`：通過，1 file / 3 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 23 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/register`，加入 `美式咖啡 12oz` 兩杯，自動套用 `咖啡滿百 9 折`。
  - 進入結帳並完成現金付款。
  - 付款完成畫面顯示訂單號 `000000-20260517145729-74938`、品項 `美式咖啡 12oz`、促銷 `咖啡滿百 9 折`、應收 `$170` 與找零 `$0`。
  - 付款完成後 `現金` 付款方式按鈕 disabled、收款金額 input disabled、主按鈕顯示 `付款已完成` 並 disabled。
  - 付款完成後顯示 `返回收銀台` 與 `查看訂單列表` 作為明確下一步。

## Git 提交記錄 - POS Checkout Completion Action Polish

- 時間：2026-05-17 14:58:06 CST
- Hash：`10585e3`
- 訊息：`fix(pos): lock checkout controls after payment`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 19 insertions(+), 5 deletions(-)

## POS Order Number Search Polish

- 時間：2026-05-17 15:11:43 CST。
- 目標：修正訂單列表「依訂單編號搜尋」欄位只有 UI、沒有實際篩選行為的問題，避免驗收時看起來可用但輸入無效果。

## Progress - POS Order Number Search Polish

- 前端：
  - `OrderListPage` 新增 `orderNoKeyword` state。
  - 新增 `displayedOrders`，依目前載入頁面的 `orderNo` 做關鍵字篩選。
  - 桌面表格、手機卡片與本頁統計卡改用 `displayedOrders`。
  - 搜尋無結果時顯示 `找不到符合條件的訂單` 與調整關鍵字提示。
- 測試：
  - 擴充 `OrderListPage.test.tsx`，新增第二筆訂單資料。
  - 覆蓋輸入訂單尾碼後只顯示符合訂單；輸入無匹配關鍵字後顯示空狀態。

## Verification - POS Order Number Search Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，1 file / 2 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 24 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/orders`。
  - 列表載入多筆訂單，最新可見訂單包含 `000000-20260517145729-74938`。
  - 搜尋 `74938` 後，列表只保留該訂單。
  - 搜尋 `NO-MATCH-ORDER` 後，顯示 `找不到符合條件的訂單` 與 `請調整訂單編號關鍵字或清除搜尋。`。

## Git 提交記錄 - POS Order Number Search Polish

- 時間：2026-05-17 15:15:49 CST
- Hash：`7bb6119`
- 訊息：`fix(pos): enable order number search`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 69 insertions(+), 13 deletions(-)

## POS New Order Action Polish

- 時間：2026-05-17 18:48:07 CST。
- 目標：修正訂單列表右上角 `新增訂單` 按鈕只有視覺狀態、沒有實際導頁行為的問題，讓收銀員可從訂單列表直接開始下一筆訂單。

## Progress - POS New Order Action Polish

- 前端：
  - `OrderListPage` 的 `新增訂單` 按鈕新增 `onClick`。
  - 點擊後固定導向 `/pos/register`。
- 測試：
  - 擴充 `OrderListPage.test.tsx`，mock `useNavigate()` 並驗證點擊 `新增訂單` 會呼叫 `navigate('/pos/register')`。

## Verification - POS New Order Action Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，1 file / 3 tests。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，12 files / 25 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：POS `38180`、`38182` 由 OrbStack / POS containers 使用。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/orders`。
  - 點擊 `新增訂單`。
  - 頁面導向 `http://127.0.0.1:38182/pos/register`，並顯示 `收銀台`、`目前訂單` 與 `立即結帳`。

## Git 提交記錄 - POS New Order Action Polish

- 時間：2026-05-17 18:52:03 CST
- Hash：`d8ecddf`
- 訊息：`fix(pos): wire new order action`
- 推送：本輪一併推送至 `origin/feature`
- 變更統計：2 files changed, 21 insertions(+)

## Flutter Android Toolchain Setup

- 時間：2026-05-17 19:29:43 CST。
- 目標：依使用者要求先處理 POS 手機端 Android-first Flutter 開發環境，確認本機需要安裝的工具並完成可建置、可啟動的 smoke verification。

## Planning - Flutter Android Toolchain Setup

- 使用 subagent 分別盤點 POS 專案 Flutter app 現況與本機 Android/Flutter toolchain。
- 依官方 Flutter / Android 文件確認 macOS Android 開發必要項目。
- 安裝 Flutter SDK、Android Studio、Android SDK command-line tools。
- 安裝 Android SDK platform、build-tools、platform-tools、emulator、ARM64 system image。
- 建立 Android tablet AVD，並用 Flutter sample app 驗證 build / install / launch。

## Project Structure - Flutter Android Toolchain Setup

```text
POS/
├── backend/                         # Spring Boot 多模組後端
├── frontend-web/                    # React/Vite POS Web 前端，目前承接 POS 主流程
├── frontend-app/                    # Flutter POS 終端規劃目錄，目前尚未建立
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數
├── 需求/                            # Flutter POS、離線同步、終端安全與驗收規格
├── docs/                            # 專案地圖與交接文件
├── devlog/                          # 每日開發日誌與 troubleshooting
├── scripts/                         # 初始化與輔助腳本
├── reference/模塊化組件/             # AI 參考用模組化組件匯出資料
└── tmp/                             # 本地驗證截圖與臨時產物，不進版控
```

## Progress - Flutter Android Toolchain Setup

- 專案盤點：
  - 確認目前 POS repo 尚無 `frontend-app/`、`pubspec.yaml`、Flutter `android/` 或 `lib/` 結構。
  - 文件顯示 Flutter POS 終端原規劃於 Phase 3，使用者本輪已明確要求先啟動 Android-first。
- 本機安裝：
  - 安裝 `flutter` cask，Flutter SDK 位於 `/opt/homebrew/share/flutter`。
  - 安裝 Android Studio `2025.3.4.7` 至 `/Applications/Android Studio.app`。
  - 安裝 `android-commandlinetools`，提供 `sdkmanager` / `avdmanager`。
  - Android SDK 設定於 `/Users/wei/Library/Android/sdk`。
  - 安裝 `platform-tools`、`emulator`、`platforms;android-36`、`build-tools;36.0.0`、`system-images;android-36;google_apis;arm64-v8a`。
  - Gradle smoke build 自動補齊 NDK `28.2.13676358`、Build Tools `35.0.0`、CMake `3.22.1`。
  - 設定 `flutter config --android-sdk /Users/wei/Library/Android/sdk`。
  - 更新 `/Users/wei/.zshrc`，加入 `ANDROID_HOME`、`ANDROID_SDK_ROOT`、`platform-tools`、`emulator`、`cmdline-tools/latest/bin` PATH。
- Emulator：
  - 建立 AVD `pos_android_tablet`，device 使用 `pixel_tablet`。

## Verification - Flutter Android Toolchain Setup

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Flutter / Android：
  - `flutter doctor -v`：Flutter、Android toolchain、Chrome、Network resources 通過；僅剩 iOS/macOS 的完整 Xcode 與 CocoaPods 缺口，Android-first 不阻塞。
  - `flutter doctor --android-licenses`：All SDK package licenses accepted。
  - `which adb emulator sdkmanager flutter dart`：皆可解析到本機安裝路徑。
  - `sdkmanager --list_installed`：確認 Android SDK 36、Build Tools 36、Platform Tools、Emulator、ARM64 Google APIs system image 已安裝。
  - `flutter emulators`：可列出 `pos_android_tablet`。
- Smoke test：
  - 在 `/tmp/pos_flutter_smoke` 建立 Flutter Android sample app，未污染 POS repo。
  - `flutter build apk --debug`：通過，產出 `build/app/outputs/flutter-apk/app-debug.apk`。
  - 啟動 `pos_android_tablet` emulator，`flutter devices` 可偵測 `emulator-5554`。
  - `flutter run -d emulator-5554`：成功 build、install、launch sample app，DevTools URL 可建立。
  - 測試後已停止 `flutter run` 與 emulator，避免背景佔用資源。

## Local Secret Cleanup

- 時間：2026-05-17 19:44:00 CST。
- 目標：處理本機 shell 設定中曾出現明文 OpenAI API key 的資安問題，移除明文環境變數並清理本機歷史快照。

## Progress - Local Secret Cleanup

- 掃描範圍：
  - POS repo 工作樹與已追蹤檔案。
  - 常見 shell 設定與 history：`.zshrc`、`.zprofile`、`.zshenv`、`.bashrc`、`.bash_profile`、`.zsh_history`、`.bash_history`。
  - Codex 本機 shell snapshots 與 session logs。
  - `.config` 與桌面專案資料夾的 OpenAI key 形態字串。
- 清理：
  - 從 `/Users/wei/.zshrc` 移除明文 `OPENAI_API_KEY` export。
  - 執行 `launchctl unsetenv OPENAI_API_KEY`，避免 launchd 層級持續暴露舊值。
  - 對 `.codex/shell_snapshots` 與 `.codex/sessions` 中的 API-key 型字串做 `[REDACTED_OPENAI_KEY]` 遮罩。
  - 未修改官方技能範例文件與 Codex `auth.json`，避免破壞工具自身登入狀態。

## Verification - Local Secret Cleanup

- POS repo：
  - `rg -l 'OPENAI_API_KEY|sk-*' /Users/wei/Desktop/code/POS`：未發現明文 OpenAI key。
  - `git grep -l 'OPENAI_API_KEY|sk-*'`：未發現已追蹤檔案含明文 OpenAI key。
- Shell / history：
  - `.zshrc` 已不包含 `OPENAI_API_KEY`。
  - 常見 shell 設定與 history 未再發現 OpenAI API key 形態字串。
- Codex local logs：
  - `.codex/shell_snapshots` 與 `.codex/sessions` 未再發現 OpenAI API key 形態字串。
- 後續必要動作：
  - 由於 key 曾寫入本機設定與歷史快照，仍需到 OpenAI API keys 頁面撤銷舊 key 並建立新 key。
  - 新 key 應存放於 secret manager 或本機 Keychain，不再寫入 shell rc、repo、devlog 或 session 可讀文字。

## Flutter App Kickoff and Web Barcode Polish

- 時間：2026-05-17 20:00:56 CST。
- 目標：開始同步開發 App 端與 Web 端；App 先建立 Android-first Flutter POS 終端骨架，Web 同步打磨收銀台掃描條碼按鈕。

## Planning - Flutter App Kickoff and Web Barcode Polish

- 主線：建立 `frontend-app/` Flutter Android app，先做可測、可建置、可在 emulator 啟動的 POS 終端首屏。
- 旁線：使用 subagent 只讀盤點 Web POS 可打磨點，優先處理收銀現場最容易誤用的死按鈕。
- 驗證：Flutter 跑 `analyze`、`test`、`build apk`、emulator 啟動；Web 跑 TypeScript、單檔測試、lint、全測試、build、Docker frontend rebuild 與瀏覽器實測。

## Project Structure - Flutter App Kickoff and Web Barcode Polish

```text
POS/
├── backend/                         # Spring Boot 多模組後端
├── frontend-web/                    # React/Vite POS Web 前端與收銀台打磨
├── frontend-app/                    # Flutter Android-first POS 終端
│   ├── android/                     # Android 平台工程
│   ├── lib/app/                     # MaterialApp 與 app shell
│   ├── lib/core/                    # 商品/購物車模型與 demo catalog
│   ├── lib/features/pos_terminal/   # 平板收銀主畫面
│   └── test/                        # Flutter widget tests
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數
├── 需求/                            # 需求、規格、進度與驗收文件
├── docs/                            # 專案地圖與交接文件
├── devlog/                          # 每日開發日誌與 troubleshooting
└── scripts/                         # 初始化與輔助腳本
```

## Progress - Flutter App Kickoff and Web Barcode Polish

- App：
  - 新增 `frontend-app/` Flutter 專案，Android package 使用 `com.xinyi.pos.xinyi_pos_app`。
  - 將預設 counter app 改為 Xinyi POS Android tablet 首屏。
  - 新增深色 POS theme、側邊 rail、線上/待同步狀態、分類 chip、商品 grid、購物車與現金結帳 CTA。
  - 新增 `Product`、`CartItem`、`demoCatalog`，先用 demo catalog 保持離線友善展示，後續再接 API / SQLite / sync。
  - 新增 Flutter widget tests，覆蓋首屏元素與加入商品後總額/按鈕狀態。
  - 修正 Android emulator 首幀零尺寸導致 `GridView` assert 的問題，加入尺寸防護。
- Web：
  - `RegisterPage` 掃描條碼按鈕新增可操作行為。
  - 點擊掃描按鈕會聚焦搜尋欄、選取既有文字並顯示「掃描模式已啟動」提示。
  - 條碼掃描器可沿用鍵盤輸入模式，輸入條碼後按 Enter 會走既有 keyword 搜尋。
  - 新增 `RegisterPage.test.tsx` 覆蓋掃描模式提示、搜尋欄 focus 與條碼 Enter 後 API keyword payload。
- 文件：
  - 更新 `README.md`、`docs/project-map.md` 與 `需求/開發進度對照.md`，把 Flutter Android app 從未開始調整為 Android-first 骨架已建立。

## Verification - Flutter App Kickoff and Web Barcode Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Flutter：
  - `dart format lib test`：通過。
  - `flutter analyze`：通過。
  - `flutter test`：通過，2 tests。
  - `flutter build apk --debug`：通過。
  - `flutter run -d emulator-5554`：成功安裝並啟動於 `pos_android_tablet`；修正後未再出現 layout exception。
- Web：
  - `npx tsc -b`：通過。
  - `npm test -- --run test/pos-orders/RegisterPage.test.tsx`：通過，1 file / 1 test。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，13 files / 26 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功重建並啟動 `pos-frontend`。
- Browser：
  - 開啟 `http://127.0.0.1:38182/pos/register`。
  - 點擊掃描條碼按鈕後，畫面顯示「掃描模式已啟動，請掃描條碼或輸入條碼後按 Enter。」。
  - 瀏覽器 console error logs：空。
  - browser automation 的文字輸入受虛擬剪貼簿限制，本輪條碼 Enter payload 由 RTL 測試覆蓋。

## Git 提交記錄 - Flutter App Kickoff and Web Barcode Polish

- 時間：2026-05-17 20:04:30 CST
- Hash：`c650893`
- 訊息：`feat(pos-app): start android terminal shell`
- 變更統計：39 files changed, 1790 insertions(+), 10 deletions(-)

## Web POS Session Guard and Android Cart State Polish

- 時間：2026-05-17 20:20:12 CST。
- 目標：依使用者同意啟用雙線 subagent 開發；Web 線處理 POS session 遺失時的導回登入問題，Android 線打磨 Flutter POS 購物車狀態與測試。

## Planning - Web POS Session Guard and Android Cart State Polish

- Web worker：限縮在 `frontend-web/`，優先處理隔一段時間後 POS session 遺失卻停在主畫面並顯示商品獲取失敗的問題。
- Android worker：限縮在 `frontend-app/`，優先把購物車金額與異動邏輯從 Widget state 抽出，讓後續串 API / offline queue 前先有穩定測試基礎。
- 主控：複核 diff、跑 Web/Flutter 測試與 build、重建 Docker frontend，並用瀏覽器實測 POS 登入與 session guard。

## Project Structure - Web POS Session Guard and Android Cart State Polish

```text
POS/
├── backend/                         # Spring Boot 多模組後端，本輪未修改
├── frontend-web/                    # React/Vite POS Web 前端
│   ├── src/features/pos-auth/       # POS PIN 登入與 session route guard
│   ├── src/features/pos-orders/     # POS session context、收銀台與訂單功能
│   └── test/                        # Web POS 回歸測試
├── frontend-app/                    # Flutter Android-first POS 終端
│   ├── lib/features/pos_terminal/   # 平板收銀畫面與 cart state
│   └── test/                        # Flutter unit/widget tests
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數
├── devlog/                          # 每日開發日誌與 troubleshooting
└── 需求/                            # 需求、規格、進度與驗收文件
```

## Progress - Web POS Session Guard and Android Cart State Polish

- Web：
  - 新增 `PosSessionRoute`，進入 `/pos/*` 時除了 auth token，也要求 `pos-session` 具備 `storeId`、`terminalId`、`employeeId`。
  - session 遺失或不完整時呼叫 `logout()` 清掉殘留 auth / POS session，並導回 `/pos/login?redirect=...`。
  - `App.tsx` 將 POS layout 包進 `ProtectedRoute` 與 `PosSessionRoute`，避免過期或殘缺 session 停在收銀台顯示商品載入失敗。
  - `posSession.ts` 新增 `hasActivePosSession()`，讓 route guard 與測試共用 session 有效性判斷。
- Android：
  - 新增 `PosCartState`，集中管理加入商品、同商品累加、清空購物車、小計、滿 200 九折折扣與總計。
  - `PosTerminalScreen` 改讀寫 `PosCartState`，並補上穩定 widget keys，降低測試對重複文字的依賴。
  - 新增 `pos_cart_state_test.dart`，補強購物車空狀態、重複商品、折扣四捨五入與 immutable clear 行為。
  - 擴充 Flutter widget test，覆蓋重複加入商品後折扣顯示與清空後結帳按鈕 disabled。

## Verification - Web POS Session Guard and Android Cart State Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/pos-auth/PosSessionRoute.test.tsx test/pos-orders/posSession.test.ts test/shared/ProtectedRoute.test.tsx test/shared/axiosInstance.test.ts`：通過，4 files / 7 tests。
  - `npm run lint`：通過。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
  - `npm test`：通過，14 files / 29 tests。
- Flutter：
  - `dart format --set-exit-if-changed lib/features/pos_terminal/state/pos_cart_state.dart lib/features/pos_terminal/screens/pos_terminal_screen.dart test/pos_cart_state_test.dart test/pos_terminal_screen_test.dart`：通過，0 changed。
  - `flutter test test/pos_cart_state_test.dart test/pos_terminal_screen_test.dart`：通過，7 tests。
  - `flutter analyze`：通過。
  - `flutter build apk --debug`：通過，產出 `build/app/outputs/flutter-apk/app-debug.apk`。
  - `flutter test`：通過，7 tests。
- Docker / Browser：
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：成功重建並啟動 `pos-frontend`。
  - 開啟 `http://localhost:38182/pos/login`，使用 PIN `1234` 登入成功導向 `/pos/register`。
  - 收銀台顯示商品清單、`目前訂單`、`立即結帳`，未出現商品獲取失敗。
  - 點擊 `鎖定終端` 後導向 `http://localhost:38182/pos/login?redirect=%2Fpos%2Fregister`。
  - 未登入直接開 `http://localhost:38182/pos/register` 會導回 `http://localhost:38182/pos/login?redirect=%2Fpos%2Fregister`。
  - in-app browser 截圖管線仍回 `Page.captureScreenshot` timeout；本輪以 URL、DOM snapshot 與實際互動結果完成瀏覽器驗證。

## Notes - Web POS Session Guard and Android Cart State Polish

- 目前 POS Docker 暫時跑在 `38180` / `38182`；需求文件原始 `38080` / `38082` 被 `financial-accounting` containers 佔用。
- Android `PosCartState` 的滿 200 九折仍是 demo 規則；後續串 backend 後應改由 promotion/order quote 主導。

## Git 提交記錄 - Web POS Session Guard and Android Cart State Polish

- 時間：2026-05-17 20:35:00 CST
- Hash：`5bbcb8b`
- 訊息：`fix(pos): guard stale terminal sessions`
- 變更統計：10 files changed, 409 insertions(+), 36 deletions(-)

## Member Promotion Regression and App Repository Foundation

- 時間：2026-05-17 20:39:48 CST。
- 目標：延續 Web 與 Android 雙線開發；Web 鎖住「會員綁定後套用折扣/促銷不可刷掉會員」的回歸情境，Android 建立未來串後端商品 API 的 repository 與 API config 基礎。

## Planning - Member Promotion Regression and App Repository Foundation

- Web：檢查 `cartStore`、`Cart` 與 `CheckoutPage` 的會員、手動折扣、促銷折扣資料流，若邏輯已正確則補貼近 UI 操作的回歸測試。
- Android：不啟動 emulator、不打真 API，先建立 `ApiConfig` 與 `ProductRepository` 合約，讓商品來源從 demo catalog 抽象成可替換資料來源。
- 主控：整合兩個 worker 的改動，跑 Web 全測 / lint / build、Flutter format / test / analyze / debug APK build，並用瀏覽器做 POS Web smoke。

## Project Structure - Member Promotion Regression and App Repository Foundation

```text
POS/
├── frontend-web/
│   └── test/pos-orders/CartPromotion.test.tsx      # 會員 + 促銷折扣回歸測試
├── frontend-app/
│   ├── lib/core/config/api_config.dart             # App API endpoint config
│   ├── lib/core/repositories/                      # ProductRepository 合約與 demo 實作
│   ├── lib/features/pos_terminal/screens/          # POS terminal 改由 repository 載入商品
│   └── test/                                       # API config、repository、terminal widget tests
├── devlog/
└── 需求/
```

## Progress - Member Promotion Regression and App Repository Foundation

- Web：
  - 確認產品邏輯已在手動折扣、促銷折扣與 checkout 建單時保留 `selectedMember` / `memberId`。
  - `CartPromotion.test.tsx` 新增 UI 回歸：會員已綁定且目前為會員折扣時，套用優惠碼後 `discountSource` 切為 `promotion`，但 `selectedMember` 仍保留，會員按鈕仍顯示 `GOLD 金卡會員`。
- Android：
  - 新增 `ApiConfig`，支援 `POS_API_BASE_URL` dart-define，並提供 PIN login / products endpoint helper。
  - 新增 `ProductRepository` 介面與 `DemoProductRepository`，先以離線 demo catalog 實作 repository contract。
  - `PosTerminalScreen` 改由 repository 非同步載入商品目錄，並保留空商品狀態顯示。
  - 新增 `api_config_test.dart` 與 `demo_product_repository_test.dart`。
  - 調整 terminal widget test，等待非同步商品載入完成後再斷言 UI。

## Verification - Member Promotion Regression and App Repository Foundation

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test`：通過，14 files / 30 tests。
  - `npm run lint`：通過。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Flutter：
  - `dart format --set-exit-if-changed lib test`：通過，0 changed。
  - `flutter test`：通過，11 tests。
  - `flutter analyze`：通過。
  - `flutter build apk --debug`：通過，產出 `build/app/outputs/flutter-apk/app-debug.apk`。
- Browser：
  - 開啟 `http://localhost:38182/pos/register`。
  - 未登入狀態正確導回 `http://localhost:38182/pos/login?redirect=%2Fpos%2Fregister`。
  - DOM snapshot 確認登入頁顯示 `Please enter PIN`。

## Notes - Member Promotion Regression and App Repository Foundation

- App `ApiConfig` 預設 `http://10.0.2.2:38080`，符合 Android emulator 對主機服務的預設連線方式；目前本機因 `financial-accounting` 佔用標準 port，POS Docker 暫跑 `38180` / `38182`，真機或臨時 port 測試需用 `--dart-define=POS_API_BASE_URL=...` 指定。
- Android 商品 repository 目前只建立接點，尚未接真實 HTTP API、PIN session 或離線 sync queue。

## Git 提交記錄 - Member Promotion Regression and App Repository Foundation

- 時間：2026-05-17 20:41:00 CST
- Hash：`91e35d0`
- 訊息：`feat(pos-app): add product repository foundation`
- 變更統計：9 files changed, 316 insertions(+), 7 deletions(-)

## Checkout Member Payload and App PIN Session Foundation

- 時間：2026-05-17 20:47:32 CST。
- 目標：繼續 Web / Android 雙線打磨；Web 鎖住促銷折扣結帳時 `memberId` 不可遺失，Android 建立 PIN login / POS session 的可測基礎，並對齊後端實際 PIN endpoint。

## Planning - Checkout Member Payload and App PIN Session Foundation

- Web：檢查 `CheckoutPage` 建單 payload，在 `selectedMember + promotion` 同時存在時直接斷言 `memberId`、`discountSource`、`promotionRuleId`、`promotionCode` 與 `discountLabel`。
- Android：新增 PIN login request、POS session model、auth repository contract、demo auth repository 與 session state；本輪仍不打真 API、不啟動 emulator。
- 主控：確認後端 PIN endpoint 為 `/api/v1/pos/auth/pin-login`，修正 App `ApiConfig` 與測試，避免下一輪接 API 時 endpoint 錯誤。

## Project Structure - Checkout Member Payload and App PIN Session Foundation

```text
POS/
├── frontend-web/
│   └── test/pos-orders/CheckoutPage.test.tsx       # 會員 + 促銷結帳 payload 回歸測試
├── frontend-app/
│   ├── lib/core/config/api_config.dart             # PIN / products endpoint config
│   ├── lib/core/models/                            # PIN request 與 POS session model
│   ├── lib/core/repositories/                      # AuthRepository 與 demo auth
│   ├── lib/features/auth/state/                    # 可測 session state
│   └── test/                                       # auth/session/config tests
├── backend/module-auth/                            # 後端 POS PIN endpoint 對齊來源，本輪未修改
└── devlog/
```

## Progress - Checkout Member Payload and App PIN Session Foundation

- Web：
  - `CheckoutPage.test.tsx` 將促銷測試改為直接擷取 `orderApi.create` payload。
  - 驗證促銷折扣結帳時仍送出 `memberId: member-gold`，且促銷 metadata 與折扣欄位正確。
- Android：
  - `ApiConfig.pinLoginEndpoint` 修正為 `/api/v1/pos/auth/pin-login`，與後端 `PosAuthController`、Web `pinLoginApi` 對齊。
  - 新增 `PinLoginRequest`，符合後端 contract：`pin` 搭配 `terminalId` 或 `terminalCode`，不再帶後端不接受的 `storeId`。
  - 新增 `PosSession` model。
  - 新增 `AuthRepository` 與 `DemoAuthRepository`，先提供不打 API 的固定 demo session。
  - 新增 `PosSessionState`，集中管理登入、登出與 session active 檢查。
  - 新增 auth/session 相關單元測試。

## Verification - Checkout Member Payload and App PIN Session Foundation

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test`：通過，14 files / 30 tests。
  - `npm run lint`：通過。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Flutter：
  - `dart format --set-exit-if-changed lib test`：初次指出 4 個檔案需格式化，已執行格式化並重跑驗證。
  - `flutter test`：通過，20 tests。
  - `flutter analyze`：通過。
  - `flutter build apk --debug`：通過，產出 `build/app/outputs/flutter-apk/app-debug.apk`。
- Browser：
  - 開啟 `http://localhost:38182/pos/register`。
  - 未登入狀態正確導回 `http://localhost:38182/pos/login?redirect=%2Fpos%2Fregister`。
  - DOM snapshot 確認登入頁顯示 `Please enter PIN`。

## Notes - Checkout Member Payload and App PIN Session Foundation

- `DemoAuthRepository` 僅作下一輪接真實 API 的 contract/state 基礎；尚未處理 secure storage、refresh token、HTTP DTO mapping 或錯誤碼翻譯。
- App PIN request 已與後端 `PinLoginRequest` 對齊，後續 HTTP repository 可直接送 `{ pin, terminalCode }` 或 `{ pin, terminalId }`。

## Git 提交記錄 - Checkout Member Payload and App PIN Session Foundation

- 時間：2026-05-17 20:48:00 CST
- Hash：`51103c9`
- 訊息：`feat(pos-app): add pin session foundation`
- 變更統計：11 files changed, 378 insertions(+), 12 deletions(-)

## POS User Manual

- 時間：2026-05-17 20:56:20 CST。
- 目標：依使用者要求撰寫一份面向實際操作人員的 POS 使用說明書，說明如何使用目前系統。

## Planning - POS User Manual

- 使用 `docs/user-manual.md` 作為正式使用說明書路徑，符合專案文件命名規範。
- 說明書以 Web POS 主流程為主，App 端狀態作為附錄。
- README 補上使用說明書入口，方便之後開啟專案時找到文件。

## Project Structure - POS User Manual

```text
POS/
├── README.md                         # 補充使用說明書入口
├── docs/
│   ├── production-handoff.md          # 導入交接
│   ├── project-map.md                 # 專案地圖
│   └── user-manual.md                 # 使用者操作說明書，本輪新增
└── devlog/
    └── 2026-05-17-devlog.md
```

## Progress - POS User Manual

- 新增 `docs/user-manual.md`。
- 覆蓋內容：
  - 系統入口與目前本機 port。
  - Demo PIN 登入資訊。
  - 登入、鎖定終端與 session 過期導回。
  - 收銀台加入商品、調整購物車。
  - 會員綁定、會員折扣、手動折扣與促銷折扣。
  - 掛單、取單、現金結帳與付款完成摘要。
  - 訂單列表、退款、商品、庫存、發票、會員、班次與對帳。
  - Android App 目前完成狀態與待開發項目。
  - 常見問題、建議驗收流程與正式營運前待補強項目。
- 更新 `README.md`，新增 `docs/user-manual.md` 入口。

## Verification - POS User Manual

- `git diff --check`：通過。
- 人工檢閱 `docs/user-manual.md` 前半段與 README diff，確認文件可讀、路徑正確。

## Git 提交記錄 - POS User Manual

- 時間：2026-05-17 20:58:00 CST
- Hash：`699a164`
- 訊息：`docs(pos): add user manual`
- 變更統計：3 files changed, 368 insertions(+)

## Flutter App PIN Login Shell Polish

- 時間：2026-05-17 21:06:07 CST。
- 目標：繼續打磨 App 端，讓 Android Flutter app 先具備可操作的 PIN 登入、session 切換與鎖定回登入流程。

## Planning - Flutter App PIN Login Shell Polish

- App root 不再直接進入收銀台，先顯示 PIN 登入畫面。
- PIN 登入先沿用 `DemoAuthRepository`，保持後續可替換為真實 HTTP repository。
- 登入成功後將 `PosSession` 傳入收銀台 header，顯示目前 cashier。
- 收銀台新增鎖定終端按鈕，登出後回到 PIN 登入。
- 補 widget tests，覆蓋成功登入、錯誤 PIN、未滿四碼不可送出、登入後收銀流程與鎖定回登入。

## Project Structure - Flutter App PIN Login Shell Polish

```text
POS/
├── frontend-app/
│   ├── lib/app/xinyi_pos_app.dart                         # App root session shell
│   ├── lib/features/auth/screens/pin_login_screen.dart     # PIN 登入畫面，本輪新增
│   ├── lib/features/auth/state/pos_session_state.dart      # 新增 signedIn factory
│   ├── lib/features/pos_terminal/screens/pos_terminal_screen.dart
│   │                                                       # session header 與鎖定終端
│   └── test/
│       ├── pin_login_screen_test.dart                      # PIN UI 測試，本輪新增
│       └── pos_terminal_screen_test.dart                   # 改為先登入再測收銀
├── devlog/2026-05-17-devlog.md
└── devlog/troubleshooting.md
```

## Progress - Flutter App PIN Login Shell Polish

- 新增 `PinLoginScreen`：
  - 支援 4-6 碼 PIN、刪除鍵、送出鍵、錯誤訊息與 loading 狀態。
  - 使用 `PinLoginRequest(pin, terminalCode)` 呼叫 `AuthRepository`。
  - demo PIN 登入成功後回傳 `PosSession`。
- `XinyiPosApp` 改為 stateful session shell：
  - 未登入時顯示 PIN 登入。
  - 已登入時顯示 `PosTerminalScreen`。
  - 登出時清除 session 並回到 PIN 登入。
- `PosTerminalScreen`：
  - 接收 `PosSession?` 與 `onLogout`。
  - header 顯示 cashier name。
  - 新增 header / side rail 鎖定終端按鈕。
  - 窄版 header 改為上下排列並允許狀態列水平捲動。
- 測試過程中發現 PIN 畫面在 800x600 測試 viewport 溢出，已修正並記錄於 `devlog/troubleshooting.md` 與 Obsidian raw errors。

## Verification - Flutter App PIN Login Shell Polish

- `dart format lib test`：通過。
- `flutter test`：通過，24 tests。
- `flutter analyze`：通過。
- `flutter build apk --debug`：通過，產出 `build/app/outputs/flutter-apk/app-debug.apk`。
- 本輪未啟動 Android emulator，避免增加系統記憶體負擔；先以 widget tests、analyze 與 debug APK build 驗證。

## Notes - Flutter App PIN Login Shell Polish

- 目前 App 端仍使用 demo auth / demo products；下一步應新增 HTTP auth repository、secure token storage 與 session expiry 檢查。
- 目前鎖定終端只清除本機 session state；正式接 API 後應補後端 logout / revoke 流程。
