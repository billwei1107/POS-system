# 2026-05-21 Devlog

## Login Brand Template Cleanup

- 時間：2026-05-21 10:26:21 CST。
- 目標：修正後台共用登入頁 `/login` 仍顯示模板品牌 `HIVE.ERP` 的問題，避免 POS 專案畫面混入母體模板文案。

## Planning - Login Brand Template Cleanup

- 釐清實際路由：使用者截圖為 `http://localhost:38182/login?redirect=%2Fadmin%2Fdashboard`，屬於後台共用登入頁，不是 POS PIN 登入頁 `/pos/login`。
- 檢查是否為容器或端口跑錯：
  - `pos-frontend` 正常綁定 `38182`。
  - `financial-accounting-frontend` 綁定 `38082`，未覆蓋 POS 前端。
- 修正策略：
  - 移除登入頁殘留的模板品牌 `HIVE.ERP`、`企業模塊化組件系統` 與 `Enterprise Inc.`。
  - 將登入頁品牌與表單文案抽到共用常數，降低未來改版漏改風險。
  - 新增登入頁品牌測試，避免模板文案回歸。

## Project Structure - Login Brand Template Cleanup

```text
POS/
├── frontend-web/
│   ├── src/
│   │   ├── features/
│   │   │   ├── auth/
│   │   │   │   ├── components/
│   │   │   │   │   └── LoginForm.tsx
│   │   │   │   └── pages/
│   │   │   │       └── LoginPage.tsx
│   │   │   ├── pos-auth/
│   │   │   ├── pos-orders/
│   │   │   ├── pos-products/
│   │   │   └── admin/
│   │   ├── layouts/
│   │   └── shared/
│   │       ├── config/
│   │       │   └── appBrand.ts
│   │       ├── store/
│   │       ├── theme/
│   │       └── api/
│   └── test/
│       └── auth/
│           └── LoginPage.test.tsx
├── backend/
├── docker/
├── env/
├── devlog/
│   ├── 2026-05-21-devlog.md
│   └── troubleshooting.md
└── 需求/
```

## Progress - Login Brand Template Cleanup

- `LoginPage`：
  - 後台共用登入頁品牌改為 `Titanium POS`。
  - 副標改為 `咖啡門市收銀與後台管理系統`。
  - copyright 改為 `© 2026 Titanium POS. All rights reserved.`。
  - h1 加上 `aria-label="Titanium POS"`，避免分段上色造成 accessible name 變成 `TitaniumPOS`。
- `LoginForm`：
  - 表單 label、submit label 與登入按鈕品牌色改由共用常數提供。
- `appBrand.ts`：
  - 新增 POS 品牌與登入表單文案常數。
- `LoginPage.test.tsx`：
  - 新增測試確認 `/login` 顯示 `Titanium POS` 且不再出現 `HIVE.ERP` 與 `企業模塊化組件系統`。

## Verification - Login Brand Template Cleanup

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/auth/LoginPage.test.tsx`：通過，1 test。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，18 files / 42 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - `lsof -nP -iTCP:38080 -sTCP:LISTEN`：由 `financial-accounting-backend` 經 OrbStack 占用。
  - `lsof -nP -iTCP:38082 -sTCP:LISTEN`：由 `financial-accounting-frontend` 經 OrbStack 占用。
  - POS 仍使用 `38180 / 38182`。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml build frontend`：通過。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過，`pos-frontend` 已重啟。
- Browser：
  - 開啟 `http://localhost:38182/login?redirect=%2Fadmin%2Fdashboard`。
  - DOM 驗證 `heading` 為 `Titanium POS`。
  - DOM 驗證 `HIVE` / `ERP` 不存在。
  - DOM 驗證副標 `咖啡門市收銀與後台管理系統` 存在。

## Git 提交記錄

- 時間：2026-05-21 10:34:44 CST
- Hash：`a69a77c`
- 訊息：`fix(auth): replace template login branding`
- 變更統計：4 files changed, 84 insertions(+), 9 deletions(-)

## Admin Sidebar Toggle Icon Polish

- 時間：2026-05-21 10:47:57 CST。
- 目標：將後台側邊欄收合按鈕圖示由左右箭頭改成三條橫線選單圖示，讓操作語意更接近「導覽選單」。

## Progress - Admin Sidebar Toggle Icon Polish

- `AdminLayout`：
  - 移除側邊欄 toggle 使用的 `ChevronLeftIcon` / `ChevronRightIcon`。
  - 改用 `MenuIcon` 作為收合與展開狀態共用圖示。
  - 保留 `aria-label="收合側邊欄"` / `aria-label="展開側邊欄"`，讓按鈕語意不受純圖示更換影響。
  - 新增穩定的 `data-testid="admin-sidebar-menu-toggle-icon"`，讓測試與瀏覽器驗證可明確辨識目標 icon。
- `AdminLayout.test.tsx`：
  - 更新側邊欄收合測試，確認收合前、收合後與展開後皆使用三條橫線 menu toggle icon。

## Verification - Admin Sidebar Toggle Icon Polish

- `npm test -- --run test/layouts/AdminLayout.test.tsx`：通過，2 tests。
- `npx tsc -b`：通過。
- `npm run lint`：通過。
- `npm test -- --run`：通過，18 files / 42 tests。
- `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - `lsof -nP -iTCP:38080 -sTCP:LISTEN`：由 `financial-accounting-backend` 經 OrbStack 占用。
  - `lsof -nP -iTCP:38082 -sTCP:LISTEN`：由 `financial-accounting-frontend` 經 OrbStack 占用。
  - POS 仍使用 `38180 / 38182`。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml build frontend`：通過。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --no-deps frontend`：通過，`pos-frontend` 已重啟。
- Browser：
  - 開啟 `http://localhost:38182/admin/pos/products?fresh=menu-icon`。
  - 側邊欄 toggle button 存在，label 為 `收合側邊欄`。
  - button 內含 `data-testid="admin-sidebar-menu-toggle-icon"`。
  - icon path 為 `M3 18h18v-2H3zm0-5h18v-2H3zm0-7v2h18V6z`，確認為三條橫線。
  - button 內未含左右箭頭 path。

## Git 提交記錄 - Admin Sidebar Toggle Icon Polish

- 時間：2026-05-21 10:49:05 CST
- Hash：`5a45988`
- 訊息：`style(admin): use menu icon for sidebar toggle`
- 變更統計：2 files changed, 6 insertions(+), 3 deletions(-)
