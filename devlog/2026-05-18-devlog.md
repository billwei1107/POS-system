# 2026-05-18 Devlog

## Web and App Polish From Improvement Discussion

- 時間：2026-05-18 11:55:53 CST。
- 目標：依據 2026-05-17 專案改進討論，先修正 Web 訂單列表 UI 與操作員區塊，並同步打磨 Flutter App 平板收銀台版型。

## Planning - Web and App Polish From Improvement Discussion

- Web 訂單列表：
  - 桌面表格欄位維持單行，不讓訂單編號、付款狀態、建立時間與操作欄斷行。
  - 表格支援水平捲動，長資料不壓縮到換行。
  - 訂單編號欄固定在左側，方便水平瀏覽時辨識資料列。
  - 點擊整列可直接開啟訂單詳情。
- Web 操作員區塊：
  - 調整桌面狀態列右上操作員資訊，避免文字和 UI 擠在一起。
- Flutter App：
  - 收銀台 header 將 cashier 從副標題拆成獨立操作員 badge。
  - 商品卡文字防止長字溢出。
  - compact 版購物車改為滿寬。
- 文件：
  - 保存影片整理出的專案改進會議筆記。

## Project Structure - Web and App Polish From Improvement Discussion

```text
POS/
├── docs/
│   └── meeting-notes/
│       └── 2026-05-17-pos-improvement-discussion.md
├── frontend-web/
│   ├── src/layouts/PosLayout.tsx
│   ├── src/features/pos-orders/pages/OrderListPage.tsx
│   └── test/pos-orders/OrderListPage.test.tsx
├── frontend-app/
│   ├── lib/features/pos_terminal/screens/pos_terminal_screen.dart
│   └── test/pos_terminal_screen_test.dart
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - Web and App Polish From Improvement Discussion

- 新增 `docs/meeting-notes/2026-05-17-pos-improvement-discussion.md`，整理影片中的改進方向與優先待辦。
- `OrderListPage`：
  - 新增桌面表格水平捲動面與 `minWidth: 1280px`。
  - 訂單編號欄改為 sticky left。
  - 桌面表格 header 與 body cell 設為 `whiteSpace: nowrap`。
  - 桌面表格 row 新增點擊與鍵盤 Enter / Space 開詳情。
  - 操作按鈕阻止 row click 冒泡，避免點退款、付款記錄時誤開詳情。
- `OrderListPage.test.tsx`：
  - 補 row click 開詳情測試。
  - 補桌面表格水平捲動與單行樣式測試。
- `PosLayout`：
  - 桌面狀態列加入 `minWidth` / `maxWidth` / ellipsis。
  - 操作員卡片固定高度與收斂圓角，避免右上角文字擠壓。
- `PosTerminalScreen`：
  - cashier 顯示改為獨立 `_OperatorBadge`。
  - 商品卡分類與品名加入 ellipsis / max lines。
  - compact 版 `_CartPanel` 改為滿寬。

## Verification - Web and App Polish From Improvement Discussion

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，5 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，14 files / 32 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
- Browser：登入 `http://localhost:38182/pos/orders`，確認 table 可水平捲動、訂單編號 sticky/nowrap、row click 可開訂單詳情。
- Browser 二次確認：row 保持 table row 語意（未覆寫為 button role），仍可透過點擊資料列開啟詳情。
- Flutter：
  - `dart format lib test`：通過。
  - `flutter test`：通過，24 tests。
  - `flutter analyze`：通過。
  - `flutter build apk --debug`：通過，產出 `build/app/outputs/flutter-apk/app-debug.apk`。

## Notes - Web and App Polish From Improvement Discussion

- 38080 / 38082 目前由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。
- 本輪未啟動 Android emulator，避免額外提高記憶體負擔；App 先以 widget tests、analyze 與 debug APK build 驗證。
- 後續可繼續處理商品 / 材料 / 庫存關聯與 SKU 規則文件。

## Git 提交記錄 - Web and App Polish From Improvement Discussion

- 時間：2026-05-18 11:58:26 CST
- Hash：`a088fe3`
- 訊息：`feat(pos): polish order table and app terminal`
- 變更統計：7 files changed, 463 insertions(+), 45 deletions(-)

## Web and App Touch Control Polish

- 時間：2026-05-18 12:27:09 CST。
- 目標：延續專案改進討論方向，補強收銀台在觸控螢幕與 App 平板端的操作效率，讓搜尋、購物車數量與移除操作更直覺。

## Planning - Web and App Touch Control Polish

- Web 收銀台：
  - 搜尋列新增明確的觸控搜尋按鈕，避免平板或觸控螢幕只能依賴鍵盤 Enter。
  - 掃描條碼按鈕改為更大的按鈕面積，提升觸控可點擊性。
  - 小螢幕和平板頂部購物車按鈕顯示商品數量 badge。
- Flutter App：
  - 購物車明細列加入增加、減少與移除操作。
  - 數量減至 0 時自動移除商品。
  - 補狀態層與 widget test，避免 App 端購物車操作回歸。

## Project Structure - Web and App Touch Control Polish

```text
POS/
├── frontend-web/
│   ├── src/layouts/PosLayout.tsx
│   ├── src/features/pos-orders/pages/RegisterPage.tsx
│   └── test/pos-orders/RegisterPage.test.tsx
├── frontend-app/
│   ├── lib/features/pos_terminal/screens/pos_terminal_screen.dart
│   ├── lib/features/pos_terminal/state/pos_cart_state.dart
│   ├── test/pos_cart_state_test.dart
│   └── test/pos_terminal_screen_test.dart
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - Web and App Touch Control Polish

- `RegisterPage`：
  - 搜尋列新增 `搜尋` button，點擊後送出目前輸入的商品 / SKU / 條碼關鍵字。
  - 掃描條碼由 icon-only button 改為較大的 outlined button，保留 `aria-label="掃描條碼"`。
- `PosLayout`：
  - 行動與平板上方購物車按鈕加入商品數量 badge。
  - 避免 MUI `Badge` icon 與 material badge 元件命名衝突，改用 `BadgeIcon` / `MuiBadge`。
- `RegisterPage.test.tsx`：
  - 新增觸控搜尋按鈕送出關鍵字測試。
- `PosCartState`：
  - 新增 `increaseProduct`、`decreaseProduct`、`removeProduct` 與共用數量更新邏輯。
  - 商品數量扣到 0 時會移除明細，未知商品 id 則維持原狀態。
- `PosTerminalScreen`：
  - 購物車明細列新增加號、減號與刪除 icon button。
  - 商品名稱加上 ellipsis，避免長品名壓縮價格與操作鈕。
- Flutter tests：
  - 新增購物車狀態調整與觸控加減 / 移除 widget 測試。

## Verification - Web and App Touch Control Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/pos-orders/RegisterPage.test.tsx && npx tsc -b`：通過。
  - `npm run lint && npm test -- --run && npm run build`：通過，14 files / 33 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：登入 `http://localhost:38182/pos/register`，確認 `搜尋` button 可送出 `DEMO-AMERICANO`，點選商品後購物車出現美式咖啡與 `立即結帳 $95`。
- Flutter：
  - `dart format lib test`：通過。
  - `flutter test test/pos_cart_state_test.dart test/pos_terminal_screen_test.dart`：通過，10 tests。
  - `flutter test && flutter analyze && flutter build apk --debug`：通過，26 tests、analyze no issues、debug APK build 成功。

## Notes - Web and App Touch Control Polish

- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。
- Browser 自動輸入時遇到虛擬剪貼簿限制，已改用逐鍵輸入驗證搜尋流程。
- 本輪未啟動 Android emulator，避免再次拉高本機記憶體；App 端先以 widget tests、analyze 與 debug APK build 驗證。

## Git 提交記錄 - Web and App Touch Control Polish

- 時間：2026-05-18 12:30:35 CST
- Hash：`3be8a74`
- 訊息：`feat(pos): improve register touch controls`
- 變更統計：8 files changed, 306 insertions(+), 16 deletions(-)

## POS Foreground and Back Office Split

- 時間：2026-05-18 12:38:40 CST。
- 目標：依據前台 / 後台分工討論，讓 POS 前台聚焦收銀與訂單操作，把商品、庫存、會員、發票、班次、對帳與促銷等管理工作統一導向管理後台。

## Planning - POS Foreground and Back Office Split

- POS 前台：
  - 側邊欄只保留收銀台與訂單群組。
  - 保留退款處理入口，作為門市處理已完成訂單的操作流程。
  - 底部新增管理後台入口，讓店長或管理者可切換至 back office。
- 管理後台：
  - 商品、分類、會員、促銷、庫存、進貨、盤點、發票、班次、Z 報表、對帳、支付與金流設定走 `/admin/...`。
  - 舊 `/pos/...` 管理網址不再要求 POS PIN，直接轉址到對應 `/admin/...`。
- 頁面內導向：
  - 管理端訂單列表的退款按鈕留在 `/admin/pos/refunds`。
  - 庫存總覽與盤點頁內部跳轉改用 `/admin/inventory/...`。

## Project Structure - POS Foreground and Back Office Split

```text
POS/
├── frontend-web/
│   ├── src/App.tsx
│   ├── src/layouts/PosLayout.tsx
│   ├── src/features/pos-orders/pages/OrderListPage.tsx
│   ├── src/features/pos-inventory/pages/StockOverviewPage.tsx
│   ├── src/features/pos-inventory/pages/StockTakePage.tsx
│   ├── src/features/pos-products/pages/ProductListPage.tsx
│   ├── src/features/pos-products/pages/CategoryListPage.tsx
│   ├── test/layouts/PosLayout.test.tsx
│   └── test/pos-orders/OrderListPage.test.tsx
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - POS Foreground and Back Office Split

- `PosLayout`：
  - 移除 POS 前台側邊欄中的商品、庫存、發票、會員、班次與對帳項目。
  - 手機 / 平板 dock 改為依前台選單顯示，現在只顯示收銀台與訂單。
  - 新增 `管理後台` 按鈕，導向 `/admin/dashboard`。
- `App`：
  - 移除 `/pos` 巢狀路由中的管理頁。
  - 新增舊 `/pos/products`、`/pos/inventory`、`/pos/members` 等管理網址轉址到 `/admin/...`。
  - 盤點明細舊網址保留 id 並轉到 `/admin/inventory/stock-takes/:stockTakeId`。
- `OrderListPage`：
  - 依目前路徑決定退款導向；管理端使用 `/admin/pos/refunds`，前台使用 `/pos/refunds`。
- `StockOverviewPage` / `StockTakePage`：
  - 內部進貨、盤點與盤點明細導向改為 `/admin/inventory/...`。
- `ProductListPage` / `CategoryListPage`：
  - 未登入提示的登入後回跳路徑改為管理端網址。
- Tests：
  - `PosLayout.test.tsx` 補前台導航不顯示管理模組與管理後台入口測試。
  - `OrderListPage.test.tsx` 補管理端退款導向測試。

## Verification - POS Foreground and Back Office Split

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/layouts/PosLayout.test.tsx test/pos-orders/OrderListPage.test.tsx && npx tsc -b`：通過，2 files / 8 tests。
  - `npm run lint && npm test -- --run && npm run build`：通過，14 files / 35 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：登入 `http://localhost:38182/pos/register`，確認 POS 前台只顯示收銀台、訂單與管理後台入口。
  - Browser：開啟 `http://localhost:38182/pos/products`，確認轉址到 `http://localhost:38182/admin/pos/products` 並顯示商品管理頁。

## Notes - POS Foreground and Back Office Split

- POS 前台仍保留購物車內的會員套用按鈕，這屬於收銀流程中的會員查找 / 套用，不等同完整會員管理。
- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。

## Git 提交記錄 - POS Foreground and Back Office Split

- 時間：2026-05-18 12:39:33 CST
- Hash：`e7de6d5`
- 訊息：`refactor(pos): split foreground and admin routes`
- 變更統計：10 files changed, 165 insertions(+), 57 deletions(-)

## Admin Dashboard Boundary Polish

- 時間：2026-05-18 12:57:42 CST。
- 目標：延續前台 / 後台分流，讓管理首頁本身也明確區分門市前台入口與後台管理入口，避免使用者進入後台後仍把收銀操作和管理功能混在一起。

## Planning - Admin Dashboard Boundary Polish

- 後台首頁：
  - POS 管理摘要補齊會員與促銷，符合目前管理端實際入口數量。
  - 將前台收銀、門市訂單與退款處理獨立成「門市前台」區塊。
  - 將商品、庫存、會員、員工、權限、流程與人事入口保留在「後台管理」區塊。
- 測試：
  - 補管理首頁測試，確認前台入口走 `/pos/...`，管理入口走 `/admin/...`。

## Project Structure - Admin Dashboard Boundary Polish

```text
POS/
├── frontend-web/
│   ├── src/features/admin/pages/AdminDashboardPage.tsx
│   └── test/admin/AdminDashboardPage.test.tsx
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - Admin Dashboard Boundary Polish

- `AdminDashboardPage`：
  - `POS 管理` 摘要由 4 項調整為 6 項，補上會員與促銷。
  - 原本混合的 `常用入口` 拆成 `門市前台` 與 `後台管理`。
  - `門市前台` 只保留 `進入收銀台`、`門市訂單`、`退款處理`。
  - `後台管理` 保留商品、庫存、盤點、訂單、會員、對帳、員工、部門、權限、稽核、流程與請假入口。
- `AdminDashboardPage.test.tsx`：
  - 新增入口分區測試，檢查前台連結為 `/pos/...`、後台連結為 `/admin/...`。

## Verification - Admin Dashboard Boundary Polish

- Web：
  - `npm test -- --run test/admin/AdminDashboardPage.test.tsx && npx tsc -b`：通過，1 test。
  - `npm run lint && npm test -- --run && npm run build`：通過，15 files / 36 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：開啟 `http://localhost:38182/admin/dashboard`，確認首頁顯示 `門市前台` 與 `後台管理` 兩個分區，入口路徑符合前台 / 後台分流。

## Notes - Admin Dashboard Boundary Polish

- 本輪只調整 Web 管理端首頁，Flutter App 未變更。
- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。

## Git 提交記錄 - Admin Dashboard Boundary Polish

- 時間：2026-05-18 13:01:23 CST
- Hash：`ae99a65`
- 訊息：`refactor(admin): clarify dashboard entry boundaries`
- 變更統計：3 files changed, 151 insertions(+), 30 deletions(-)

## POS Foreground Admin Entry

- 時間：2026-05-18 16:12:02 CST。
- 目標：依使用者回饋，在 POS 前台明顯位置加入進入後台按鈕，避免管理入口只藏在側邊欄底部。

## Planning - POS Foreground Admin Entry

- 桌面前台：
  - 在頂部狀態列加入 `進入後台` 按鈕，和線上狀態、操作員資訊並列。
  - 收斂狀態列間距與店名寬度，避免新增按鈕後擠壓標題或換行。
- 手機 / 平板前台：
  - 在頂部列加入同一個 `進入後台` 動作，手機尺寸保留 icon 並用 aria-label 維持可及性。
- 測試：
  - 補 `PosLayout` 測試，確認頂部 `進入後台` 可導向 `/admin/dashboard`。

## Project Structure - POS Foreground Admin Entry

```text
POS/
├── frontend-web/
│   ├── src/layouts/PosLayout.tsx
│   └── test/layouts/PosLayout.test.tsx
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - POS Foreground Admin Entry

- `PosLayout`：
  - 新增共用 `handleOpenAdmin`，統一導向 `/admin/dashboard`。
  - 桌面頂部狀態列新增 `進入後台` 按鈕。
  - 行動 / 平板頂部列新增 `進入後台` 按鈕，手機尺寸縮成 icon 為主。
  - 保留側邊欄底部 `管理後台` 作為第二入口。
  - 調整桌面狀態列店名、班次、線上狀態、按鈕與操作員卡片寬度，避免 1280px 寬度下互相擠壓。
- `PosLayout.test.tsx`：
  - 新增前台頂部 `進入後台` 按鈕導向測試。

## Verification - POS Foreground Admin Entry

- Web：
  - `npm test -- --run test/layouts/PosLayout.test.tsx && npx tsc -b`：通過，3 tests。
  - `npm run lint && npm test -- --run && npm run build`：通過，15 files / 37 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：開啟 `http://localhost:38182/pos/register`，確認前台頂部顯示 `進入後台`，點擊後可導向 `http://localhost:38182/admin/dashboard`。

## Notes - POS Foreground Admin Entry

- 本輪只調整 Web 前台入口，Flutter App 未變更。
- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。

## Git 提交記錄 - POS Foreground Admin Entry

- 時間：2026-05-18 16:19:22 CST
- Hash：`7073ad3`
- 訊息：`feat(pos): add foreground admin entry`
- 變更統計：3 files changed, 99 insertions(+), 6 deletions(-)

## POS Top Bar Spacing Polish

- 時間：2026-05-18 16:23:25 CST。
- 目標：依使用者截圖回饋，改善 POS 前台右上角 `線上`、`進入後台`、`操作員` 區塊過度貼近的視覺問題。

## Planning - POS Top Bar Spacing Polish

- 桌面頂部狀態列：
  - 拉大右側狀態、後台入口與操作員卡片之間的水平間距。
  - 操作員卡片左側加入淡分隔線，讓身份資訊和前方操作按鈕形成獨立區塊。
  - 收斂狀態列左右 padding 與店名最大寬度，避免內容在收銀台右側購物車存在時互相擠壓。

## Project Structure - POS Top Bar Spacing Polish

```text
POS/
├── frontend-web/
│   └── src/layouts/PosLayout.tsx
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - POS Top Bar Spacing Polish

- `PosLayout`：
  - 桌面狀態列 `gap` 與 `px` 調整，右側區塊分布更鬆。
  - `線上` 狀態膠囊增加固定高度與 padding。
  - `進入後台` 按鈕增加高度與水平 padding。
  - 操作員卡片增加左側 margin、分隔線與更清楚的內距。

## Verification - POS Top Bar Spacing Polish

- Web：
  - `npm test -- --run test/layouts/PosLayout.test.tsx && npx tsc -b`：通過，3 tests。
  - `npm run lint && npm test -- --run && npm run build`：通過，15 files / 37 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：開啟 `http://localhost:38182/pos/register`，確認右上角狀態、後台入口與操作員資訊已拉開。

## Notes - POS Top Bar Spacing Polish

- 本輪只調整 Web 前台桌面頂部狀態列間距，Flutter App 未變更。
- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。

## Git 提交記錄 - POS Top Bar Spacing Polish

- 時間：2026-05-18 16:27:54 CST
- Hash：`c310245`
- 訊息：`style(pos): space foreground top bar actions`
- 變更統計：2 files changed, 74 insertions(+), 6 deletions(-)

## POS Page Action Vertical Spacing

- 時間：2026-05-18 16:34:16 CST。
- 目標：依使用者補充說明，修正操作員卡片與下方頁面右側快捷操作（例如 `格狀`）之間的垂直間距太小問題。

## Planning - POS Page Action Vertical Spacing

- 收銀台：
  - 右側快捷操作列不再貼齊頁面標題區上緣，改與標題區中線對齊。
- 訂單頁：
  - 右側 `新增訂單` 也改與標題區中線對齊，避免和頂部操作員卡片看起來太近。

## Project Structure - POS Page Action Vertical Spacing

```text
POS/
├── frontend-web/
│   ├── src/features/pos-orders/pages/RegisterPage.tsx
│   └── src/features/pos-orders/pages/OrderListPage.tsx
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - POS Page Action Vertical Spacing

- `RegisterPage`：
  - 收銀台快捷按鈕列新增桌面 `alignSelf: center`，讓 `折扣`、`急單`、`格狀` 整排往下對齊標題區中線。
- `OrderListPage`：
  - 頁首容器桌面 `alignItems` 從 `flex-start` 改為 `center`，讓 `新增訂單` 不再貼近頂部狀態列。

## Verification - POS Page Action Vertical Spacing

- Web：
  - `npm test -- --run test/pos-orders/RegisterPage.test.tsx test/pos-orders/OrderListPage.test.tsx && npx tsc -b`：通過，2 files / 8 tests。
  - `npm run lint && npm test -- --run && npm run build`：通過，15 files / 37 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：開啟 `http://localhost:38182/pos/register`，確認 `格狀` 快捷按鈕列已與操作員卡片拉開。

## Notes - POS Page Action Vertical Spacing

- 本輪只調整 Web POS 前台頁首快捷操作的垂直對齊，Flutter App 未變更。
- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。

## Git 提交記錄 - POS Page Action Vertical Spacing

- 時間：2026-05-18 16:40:26 CST
- Hash：`b016e70`
- 訊息：`style(pos): align foreground page actions`
- 變更統計：3 files changed, 45 insertions(+), 2 deletions(-)

## POS Operator Top Spacing Polish

- 時間：2026-05-18 16:57:33 CST。
- 目標：修正 Web POS 前台桌面頂部操作員卡片太靠近螢幕上緣的視覺問題。

## Planning - POS Operator Top Spacing Polish

- 桌面狀態列：
  - 將固定高度從原本 `64px` 抽成常數並提高，讓 `線上`、`進入後台`、`操作員` 卡片在狀態列內垂直置中。
  - 同步更新主內容高度計算，避免頁面內容因狀態列增高而超出 viewport。

## Project Structure - POS Operator Top Spacing Polish

```text
POS/
├── frontend-web/
│   └── src/layouts/PosLayout.tsx
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - POS Operator Top Spacing Polish

- `PosLayout`：
  - 新增 `DESKTOP_STATUS_BAR_HEIGHT = 96`。
  - 桌面狀態列改用該常數作為高度。
  - 桌面內容區高度改用同一常數計算，維持 layout 穩定。

## Verification - POS Operator Top Spacing Polish

- Web：
  - `npm test -- --run test/layouts/PosLayout.test.tsx && npx tsc -b`：通過，3 tests。
  - `npm run lint && npm test -- --run && npm run build`：通過，15 files / 37 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：開啟 `http://localhost:38182/pos/register`，操作員卡片上緣約 `20px`，相較前一版約 `12px` 更不貼近螢幕上緣。

## Notes - POS Operator Top Spacing Polish

- 本輪只調整 Web POS 前台桌面頂部狀態列高度，Flutter App 未變更。
- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。

## Git 提交記錄 - POS Operator Top Spacing Polish

- 時間：2026-05-18 16:58:14 CST
- Hash：`166aedb`
- 訊息：`style(pos): lower foreground operator bar`
- 變更統計：2 files changed, 44 insertions(+), 2 deletions(-)

## Web Light Mode Toggle

- 時間：2026-05-18 17:31:07 CST。
- 目標：新增 Web 端淺色模式，並讓使用者可以在深色與淺色模式間切換。

## Planning - Web Light Mode Toggle

- 主題系統：
  - 將固定深色 `posTheme` 改為可依 `dark/light` 建立的 `createPosTheme(mode)`。
  - 新增全域 `ThemeModeProvider`，並以 `localStorage` 保存使用者選擇。
- 切換入口：
  - 新增共用 `ThemeModeToggle` 圖示按鈕。
  - 將切換鈕放在 POS 前台頂部列、後台 AppBar 與 POS PIN 登入頁。
- 版面調整：
  - 將 POS 前台、後台、POS 登入頁中明顯的深色硬編碼改為依 theme mode 調整。

## Project Structure - Web Light Mode Toggle

```text
POS/
├── frontend-web/
│   ├── src/App.tsx
│   ├── src/features/pos-auth/pages/PosLoginPage.tsx
│   ├── src/layouts/AdminLayout.tsx
│   ├── src/layouts/PosLayout.tsx
│   ├── src/shared/components/
│   │   ├── ThemeModeToggle.tsx
│   │   └── index.ts
│   ├── src/shared/theme/
│   │   ├── ThemeModeProvider.tsx
│   │   ├── index.ts
│   │   └── themeModeContext.ts
│   └── test/shared/ThemeModeProvider.test.tsx
└── devlog/
    └── 2026-05-18-devlog.md
```

## Progress - Web Light Mode Toggle

- Theme：
  - 新增 `AppThemeMode`、`THEME_MODE_STORAGE_KEY`、`createPosTheme(mode)`。
  - 補齊淺色模式背景、文字、卡片陰影、Drawer 邊線與 TableCell 色彩。
- State：
  - 新增 `ThemeModeProvider` 與 `themeModeContext`，支援切換與持久化。
- UI：
  - 新增 `ThemeModeToggle`，依目前模式顯示月亮或太陽圖示。
  - POS 前台、後台、POS PIN 登入頁均加入切換鈕。
  - 後台側欄、AppBar、POS 前台狀態列與登入鍵盤改為會跟隨深淺色模式。
- Test：
  - 新增 `ThemeModeProvider.test.tsx` 驗證切換與 stored preference 載入。

## Verification - Web Light Mode Toggle

- Web：
  - `npm test -- --run test/shared/ThemeModeProvider.test.tsx test/layouts/PosLayout.test.tsx && npx tsc -b`：通過，2 files / 5 tests。
  - `npm run lint && npm test -- --run && npm run build`：通過，16 files / 39 tests；保留既有 Vite chunk size warning。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
  - Browser：開啟 `http://localhost:38182/pos/register`，點擊切換鈕後 DOM 從 `切換淺色模式` 變為 `切換深色模式`。
  - Headless Chrome：以 light mode 與 demo POS session 開啟 `http://localhost:38182/pos/register`，確認 `切換深色模式` 存在並輸出截圖 `/tmp/pos-light-mode-register.png`。

## Notes - Web Light Mode Toggle

- Headless Chrome 截圖使用測試用 demo session 與假 JWT，故商品 API 會顯示載入提示；本輪驗證重點為 theme mode 切換與版面套色。
- 38080 / 38082 目前仍由 `financial-accounting` 容器占用；POS 既有容器使用 `38180 / 38182`。
