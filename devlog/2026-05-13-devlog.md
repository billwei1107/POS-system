# 2026-05-13 Devlog

## Planning

- 修正 POS 盤點單完成後庫存沒有同步更新的問題。
- 確認後端完成盤點流程與前端「輸入實盤數後直接完成」流程都能正確套用實盤數。

## Project Structure

- `backend/module-pos-inventory/`
  - POS 庫存、庫存異動、進貨驗收與盤點單後端模組。
- `backend/module-pos-inventory/src/main/java/com/enterprise/inventory/service/StockTakeService.java`
  - 盤點單建立、登記實盤數與完成盤點後校正庫存的服務。
- `backend/module-pos-inventory/src/test/java/com/enterprise/inventory/StockTakeServiceTest.java`
  - 盤點完成同步庫存與異動紀錄測試。
- `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
  - POS 盤點單頁面，負責盤點單列表、實盤輸入、登記與完成盤點。
- `devlog/troubleshooting.md`
  - POS 開發問題追蹤紀錄。

## Progress

- `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
  - 完成盤點前會先檢查畫面上尚未登記的實盤輸入。
  - 使用者可直接輸入實盤數後按「完成盤點」，系統會先送出 pending count，再完成盤點。
  - 實盤輸入框新增 `data-stock-count-key`，完成盤點時直接讀取畫面 DOM 的即時值，避免觸控鍵盤或快速點擊時 state 尚未同步造成漏送。
  - 完成中會停用完成、取消與單品登記按鈕，避免重複送出。
- `backend/module-pos-inventory/src/test/java/com/enterprise/inventory/StockTakeServiceTest.java`
  - 新增完成盤點後庫存數量等於實盤數、且建立 `ADJUSTMENT` 庫存異動的測試。

## Verification

- `mvn -pl module-pos-inventory -am test -Dtest=StockControllerTest,StockTakeControllerTest,StockTakeServiceTest -Dsurefire.failIfNoSpecifiedTests=false`：通過。
- `npm run lint`（frontend-web）：通過。
- `npx tsc -b`（frontend-web）：通過。
- `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
- `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，frontend 容器重建並啟動。
- Chrome CDP 瀏覽器互動驗證：
  - 在 `/pos/inventory/stock-takes` 新增盤點單。
  - 僅在第一個實盤欄位輸入 `42`，不按「登記」，直接按「完成盤點」。
  - 完成後盤點單狀態為 `COMPLETED`。
  - 該品項 `countedQty=42`、`difference=-58`。
  - 該品項門店庫存由 `100` 同步更新為 `42`。
  - 截圖輸出：`/tmp/pos-stocktake-complete-sync-final.png`。

## 端口規範補充

- 時間：2026-05-13 01:56:51 CST。
- 使用者要求：localhost 端口被佔用時，仍應優先使用專案原本端口；每次設定或啟動端口前必須先檢查是否被佔用。
- 已補充文件：
  - `AGENTS.md`：新增「端口與服務啟動規則」。
  - `ai-project-start.md`：標準啟動順序加入端口檢查。
  - `需求/系統規格表.md`：本地服務端口與檢查指令補齊。
  - `需求/功能檢驗流程書.md`：測試流程新增 `3.0 端口佔用檢查`。
- 已寫入 ai-kb 記憶：`feedback-port-check-before-service-start`。
- 當下端口檢查結果：`38080`、`38082` 由 `financial-accounting-backend/frontend` 佔用；已停止本輪臨時後端 `38081` 與臨時瀏覽器測試程序。

## 臨時替代端口瀏覽器驗證

- 使用者同意在原本端口被佔用時改用其他端口。
- 啟動前已檢查 `38180`、`38182`、`38183`、`9223` 均未被佔用。
- 臨時端口：
  - Backend：`38180`
  - Frontend 測試頁：`38182`
  - Chrome CDP：`9223`
- 驗證方式：
  - 後端以最新 `backend/app/target/app-1.0.0-SNAPSHOT.jar` 啟動，`/actuator/health` 回 `UP`。
  - 前端以 `frontend-web/dist` 靜態服務啟動，並將同源 `/api` 代理到 `38180`。
  - 瀏覽器登入後開啟 `/pos/inventory/stock-takes`。
- 驗證結果：
  - 盤點單列表可正常載入，未出現「載入盤點單失敗」。
  - 已完成 / 已取消盤點單點擊後，明細展開在該張卡片下方，仍停留列表頁。
  - 點擊「建立盤點單」後會跳轉到 `/pos/inventory/stock-takes/{id}` 的獨立「盤點作業」頁。
  - 獨立盤點頁有 `返回盤點列表`、`完成盤點` 與 4 個實盤輸入欄。
  - 測試建立的進行中盤點單已取消並返回列表。
  - 截圖輸出：`/tmp/pos-stocktake-alt-port-list-detail.png`。

## OrbStack 容器驗證

- 時間：2026-05-13 02:25:28 CST。
- 使用者提醒：本地可試用環境應優先使用 OrbStack / Docker Compose 容器，而不是本機 jar + 靜態服務。
- 已用 Docker Compose 臨時 override 啟動 POS 容器：
  - `pos-backend`：`38180 -> 8080`
  - `pos-frontend`：`38182 -> 80`
  - `pos-postgres`、`pos-redis`：沿用既有 OrbStack 容器。
- 原本 `38080/38082` 仍由 `financial-accounting-backend/frontend` 佔用，未停止無關容器。
- 驗證：
  - `curl http://127.0.0.1:38180/actuator/health`：回 `{"status":"UP"}`。
  - `curl http://127.0.0.1:38182/`：可取得前端 HTML。
  - `POST http://127.0.0.1:38182/api/v1/pos/auth/pin-login`：回 200，確認 Nginx `/api` 代理至 backend 正常。
  - 瀏覽器開啟 `http://127.0.0.1:38182/pos/inventory/stock-takes`：盤點單頁可載入，未出現「載入盤點單失敗」。

## 盤點單列表分頁與時間搜尋

- 時間：2026-05-13 02:45:30 CST。
- 目標：盤點單列表不得一次列出全部資料；每頁最多 5 筆，並可依盤點時間搜尋。
- 變更：
  - `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
    - 新增盤點單時間搜尋欄，支援 `2026/05/13`、`05/13`、`01:26`、`20260513` 等時間字串。
    - 新增列表分頁，每頁最多顯示 5 張盤點單。
    - 搜尋或翻頁時自動收合已展開詳情，避免詳情卡片與新列表狀態錯位。
    - 列表上方顯示目前筆數範圍與總筆數，底部顯示頁碼控制。
- 驗證：
  - `npx tsc -b`（frontend-web）：通過。
  - `npm run lint`（frontend-web）：通過。
  - `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
  - OrbStack/Docker 重建 `pos-frontend` 容器成功，沿用 `38182 -> 80`。
  - Chrome CDP 瀏覽器實測：
    - 初始列表顯示 `目前顯示 1-5 筆，共 10 筆`，卡片數 5。
    - 搜尋 `02:24` 後顯示 `目前顯示 1-1 筆，共 1 筆`。
    - 清除搜尋後翻至第 2 頁，仍維持 5 筆上限。
    - 截圖輸出：`/tmp/pos-stocktake-pagination-search.png`。

## 盤點單時間篩選改為選取器

- 時間：2026-05-13 03:07:09 CST。
- 使用者要求：盤點時間不要用打字搜尋，應改為選取；時間需用可滾動選單選取。
- 變更：
  - `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
    - 移除自由輸入的盤點時間搜尋欄。
    - 改為「盤點日期」與「盤點時間」兩個選取器。
    - 日期與時間選項直接由既有盤點單產生，避免輸入格式錯誤。
    - 時間選單限制高度並支援滾動，觸控裝置可直接滑動選取。
    - 保留每頁最多 5 筆、篩選後自動收合詳情與重設頁碼。
- 驗證：
  - `npx tsc -b`（frontend-web）：通過。
  - `npm run lint`（frontend-web）：通過。
  - `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
  - OrbStack/Docker 重建 `pos-frontend` 容器成功，沿用 `38182 -> 80`。
  - Chrome CDP 瀏覽器實測：
    - 畫面出現「盤點日期」與「盤點時間」選取器。
    - 原本 `依盤點時間搜尋，例如...` 自由輸入欄已不存在。
    - 時間選單有 11 個選項，選單最大高度 `360px`，可滾動。
    - 選取 `02:24` 後列表顯示 `目前顯示 1-1 筆，共 1 筆`。
    - 截圖輸出：`/tmp/pos-stocktake-picker-filter.png`。

## 盤點單日期時間挑選器改為月曆與滾輪

- 時間：2026-05-13 03:20:16 CST。
- 使用者要求：日期需像日曆列出當月所有日期並可點擊；時間需拆成小時與分鐘兩欄，透過滾動方式選取。
- 變更：
  - `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
    - 日期篩選改為當月月曆方格，顯示星期列與當月所有日期。
    - 月曆可切換上一個月 / 下一個月，點擊日期後立即套用篩選。
    - 時間篩選拆成「小時」與「分鐘」兩個可滾動欄位。
    - 小時提供 `00-23` 與 `全部`，分鐘提供 `00-59` 與 `全部`。
    - 移除下拉選單，保留每頁最多 5 筆與篩選後重設頁碼。
- 驗證：
  - `npx tsc -b`（frontend-web）：通過。
  - `npm run lint`（frontend-web）：通過。
  - `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
  - OrbStack/Docker 重建 `pos-frontend` 容器成功，沿用 `38182 -> 80`。
  - Chrome CDP 瀏覽器實測：
    - 畫面存在月曆日期方格與 `2026 年 5 月` 月份標題。
    - 不存在下拉選單 `combobox` 與自由輸入搜尋提示。
    - 小時選項共 25 個（含 `全部`），分鐘選項共 61 個（含 `全部`）。
    - 分鐘欄可滾動。
    - 點選 `2026/05/13`、小時 `02`、分鐘 `24` 後，列表顯示 `目前顯示 1-1 筆，共 1 筆`。
    - 截圖輸出：`/tmp/pos-stocktake-calendar-time-picker.png`。

## 盤點單日期時間篩選 UIUX 精簡

- 時間：2026-05-13 03:42:04 CST。
- 使用者回饋：雖然需要適合觸控，但篩選區過大且缺乏美感，應參考成熟系統做得更精緻。
- 參考方向：
  - Apple HIG：空間受限時使用 compact picker，點開後以 popover/modal 編輯。
  - Material Design：行動端日期與時間 picker 適合以 dialog 呈現。
  - SAP Fiori：日期用 calendar、時間用 rotating wheel，但控制應適應不同裝置。
- 變更：
  - `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
    - 將原本鋪滿頁面的月曆與時間滾輪收成精簡篩選列。
    - 點擊「選擇日期時間」才開啟 compact dialog。
    - Dialog 寬度固定為 660px 以內，日期與時間控制縮小並維持可觸控。
    - 選取狀態改為低飽和紫色，移除過度搶眼的橘色選取樣式。
    - 列表重新成為頁面主體，初始即可看見多筆盤點單。
- 驗證：
  - `npx tsc -b`（frontend-web）：通過。
  - `npm run lint`（frontend-web）：通過。
  - `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
  - OrbStack/Docker 重建 `pos-frontend` 容器成功，沿用 `38182 -> 80`。
  - Chrome CDP 瀏覽器實測：
    - 篩選列高度約 `73.56px`，頁面不再直接鋪出月曆與時間滾輪。
    - 列表初始顯示 5 筆。
    - Dialog 寬度 `660px`、高度約 `488px`。
    - Dialog 內含月曆、時間欄與 2 個可滾動欄位。
    - 截圖輸出：`/tmp/pos-stocktake-compact-dialog-purple.png`、`/tmp/pos-stocktake-compact-page-purple.png`。

## 盤點單挑選器年月選取與對比修正

- 時間：2026-05-13 04:07:46 CST。
- 使用者回饋：
  - 年份與月份也要可直接選取。
  - 年月旁邊的上一月 / 下一月箭頭不要有框框。
  - 底部目前篩選時間不可顯示成 `--:--`。
  - 深色背景搭配紫色文字可讀性太差，後續 UIUX 設計需避免相近低對比配色。
- 變更：
  - `frontend-web/src/features/pos-inventory/pages/StockTakePage.tsx`
    - 月曆標題改為年份與月份兩個可選取欄位。
    - 上一月 / 下一月改用無框 `IconButton`。
    - 篩選摘要改為 `全部時間`、`HH:全部分鐘`、`全部小時:mm` 或 `HH:mm`，避免 `--:--`。
    - 日期、時間選取態與套用按鈕改為橘底白字，清除篩選改為高對比白字邊框。
    - Dialog 背景與日期 / 時間區塊邊界改為較清楚的深灰階層。
  - `~/ai-kb/memory/feedback/feedback-ui-contrast-dark-theme.md`
    - 新增長期記憶：深色 UI 避免黑 / 深灰底搭低對比紫色文字或主要操作文字。
- 驗證：
  - `npx tsc -b`（frontend-web）：通過。
  - `npm run lint`（frontend-web）：通過。
  - `npm run build`（frontend-web）：通過；僅有 Vite chunk size warning。
  - OrbStack/Docker 重建 `pos-frontend` 容器成功，沿用 `38182 -> 80`。
  - Chrome CDP 瀏覽器實測：
    - Dialog 尺寸約 `720px x 487px`。
    - 年份與月份各有 `combobox`，目前顯示 `2026 年`、`5 月`。
    - 上一月 / 下一月按鈕邊框為 `0px none`。
    - 底部摘要顯示 `全部日期 · 全部時間`。
    - 選取態與套用按鈕為 `rgb(255, 122, 26)` 橘底白字。
    - 截圖輸出：`/tmp/pos-stocktake-picker-year-month-contrast.png`。
