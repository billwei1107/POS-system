# 2026-05-22 Devlog

## POS Foreground Sidebar Collapse

- 時間：2026-05-22 00:55:20 CST。
- 目標：讓收銀台前台桌面側邊欄支援收合與展開，並使用與後台一致的三條橫線圖標。

## Planning - POS Foreground Sidebar Collapse

- 保留手機版既有 Drawer 與底部導覽，不改動行動端導覽邏輯。
- 桌面前台新增 `收合側邊欄` / `展開側邊欄` 操作，收合後保留圖標入口、tooltip 與可及性名稱。
- 收合寬度設定為 `88px`，展開寬度維持 `240px`。
- 以單元測試與瀏覽器 DOM 幾何量測驗證收合寬度與入口可用性。

## Project Structure - POS Foreground Sidebar Collapse

```text
POS/
├── frontend-web/
│   ├── src/
│   │   ├── layouts/
│   │   │   ├── AdminLayout.tsx
│   │   │   └── PosLayout.tsx
│   │   ├── features/
│   │   │   ├── pos-auth/
│   │   │   ├── pos-orders/
│   │   │   ├── pos-products/
│   │   │   └── admin/
│   │   └── shared/
│   │       ├── components/
│   │       ├── config/
│   │       ├── store/
│   │       └── theme/
│   └── test/
│       └── layouts/
│           └── PosLayout.test.tsx
├── frontend-app/
├── backend/
├── docker/
│   └── local/
├── env/
├── devlog/
│   ├── 2026-05-22-devlog.md
│   └── troubleshooting.md
├── docs/
├── scripts/
└── 需求/
```

## Progress - POS Foreground Sidebar Collapse

- `PosLayout`：
  - 新增桌面前台側邊欄收合狀態。
  - 新增三條橫線 toggle icon，保留 `收合側邊欄` / `展開側邊欄` 可及性名稱。
  - 收合狀態隱藏品牌與文字，只保留 icon-only 導覽與 tooltip。
  - 桌面側邊欄改為一般 `aside` flex 容器，手機版仍使用原本 temporary Drawer。
  - 為桌面 shell 與內容容器同步設定 `width / minWidth / maxWidth`，避免 flex item 被內容最小寬度撐回展開狀態。
- `PosLayout.test.tsx`：
  - 新增桌面前台側邊欄收合與展開測試。
  - 驗證收合後仍能取得 `收銀台` 與 `管理後台` 操作入口。

## Verification - POS Foreground Sidebar Collapse

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/layouts/PosLayout.test.tsx`：通過，4 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，18 files / 44 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - `lsof -nP -iTCP:38080 -sTCP:LISTEN`：由 `financial-accounting-backend` 經 OrbStack 占用。
  - `lsof -nP -iTCP:38082 -sTCP:LISTEN`：由 `financial-accounting-frontend` 經 OrbStack 占用。
  - POS 仍使用 `38180 / 38182`。
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
- Browser：
  - 開啟 `http://localhost:38182/pos/register?fresh=pos-sidebar-min-width`。
  - 展開狀態：`data-collapsed="false"`，shell width `240px`，sidebar width `240px`。
  - 點擊 `收合側邊欄` 後：`data-collapsed="true"`，shell width `88px`，sidebar width `88px`。
  - 收合狀態確認仍有 `展開側邊欄`、`收銀台`、`管理後台` 操作入口。
  - 再點擊 `展開側邊欄` 後：回到 `240px`，品牌文字重新顯示。

## Git 提交記錄

- 時間：2026-05-22 00:56:59 CST
- Hash：`a0566ff`
- 訊息：`feat(pos): add collapsible foreground sidebar`
- 變更統計：2 files changed, 224 insertions(+), 99 deletions(-)

## POS Foreground Support Placeholder Removal

- 時間：2026-05-22 10:24:50 CST。
- 目標：移除收銀台前台側邊欄底部未綁定功能的 `支援` 佔位入口，降低操作員誤點與介面雜訊。

## Planning - POS Foreground Support Placeholder Removal

- 僅移除前台 POS 側邊欄的 `支援` 項目。
- 保留底部真正可用的 `管理後台` 與 `鎖定終端`。
- 補測試確認前台導覽不再出現 `支援`，並確認收合後也不會回歸。

## Progress - POS Foreground Support Placeholder Removal

- `PosLayout`：
  - 移除側邊欄底部 `支援` ListItem。
  - 收合狀態同步不再顯示支援 icon-only 入口。
- `PosLayout.test.tsx`：
  - 補上前台導覽不應顯示 `支援` 的斷言。
  - 補上收合狀態不應顯示 `支援` 的斷言。

## Verification - POS Foreground Support Placeholder Removal

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/layouts/PosLayout.test.tsx`：通過，4 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，18 files / 44 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - `lsof -nP -iTCP:38080 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-backend` 占用。
  - `lsof -nP -iTCP:38082 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-frontend` 占用。
  - POS 仍使用 `38180 / 38182`。
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
- Browser：
  - 開啟 `http://localhost:38182/pos/register?fresh=remove-support`。
  - 展開狀態：`支援` 不存在，`管理後台` 與 `鎖定終端` 存在。
  - 收合狀態：width `88px`，`支援` 不存在，`管理後台`、`鎖定終端` 與 `展開側邊欄` 存在。

## Git 提交記錄 - POS Foreground Support Placeholder Removal

- 時間：2026-05-22 10:27:43 CST
- Hash：`c8c022b`
- 訊息：`refactor(pos): remove unused support sidebar item`
- 變更統計：2 files changed, 2 insertions(+), 16 deletions(-)

## POS Cart Quick Amount Placeholder Removal

- 時間：2026-05-22 10:34:04 CST。
- 目標：移除 `立即結帳` 下方未綁定行為的 `$10 / $20 / $50` 金額按鈕，避免操作員誤以為可以快速輸入付款金額。

## Planning - POS Cart Quick Amount Placeholder Removal

- 確認 `$10 / $20 / $50` 只存在於 `Cart.tsx` 的靜態按鈕，沒有 `onClick`、狀態更新或付款流程串接。
- 保留真正的 `立即結帳` CTA 與總額顯示。
- 補測試確保未使用的快速金額按鈕不會回歸。

## Progress - POS Cart Quick Amount Placeholder Removal

- `Cart.tsx`：
  - 移除 `立即結帳` 下方 `$10 / $20 / $50` 的 outlined button group。
- `CartPromotion.test.tsx`：
  - 新增測試確認購物車保留 `立即結帳`，但不再顯示 `$10`、`$20`、`$50`。

## Verification - POS Cart Quick Amount Placeholder Removal

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/pos-orders/CartPromotion.test.tsx`：通過，3 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，18 files / 45 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - `lsof -nP -iTCP:38080 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-backend` 占用。
  - `lsof -nP -iTCP:38082 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-frontend` 占用。
  - POS 仍使用 `38180 / 38182`。
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
- Browser：
  - 開啟 `http://localhost:38182/pos/register?fresh=remove-quick-amounts`。
  - DOM 驗證 `立即結帳` 存在。
  - DOM 驗證 `$10`、`$20`、`$50` 不存在。

## Git 提交記錄 - POS Cart Quick Amount Placeholder Removal

- 時間：2026-05-22 10:34:44 CST
- Hash：`92cf332`
- 訊息：`refactor(pos): remove unused quick amount buttons`
- 變更統計：2 files changed, 9 insertions(+), 6 deletions(-)

## POS Register Header Quick Action Placeholder Removal

- 時間：2026-05-22 10:41:56 CST。
- 目標：移除收銀台標題列未綁定行為的 `折扣`、`急單`、`格狀` 三個快捷按鈕，避免操作員點擊後沒有回饋。

## Planning - POS Register Header Quick Action Placeholder Removal

- 確認三個按鈕只來自 `QUICK_ACTIONS` 靜態常數，沒有 `onClick`、狀態切換或 API 行為。
- 僅移除標題列的假快捷按鈕。
- 保留右側購物車內真正可用的 `折扣` 按鈕，因為它會開啟折扣 Dialog。
- 補測試避免標題列假快捷按鈕回歸。

## Progress - POS Register Header Quick Action Placeholder Removal

- `RegisterPage.tsx`：
  - 移除 `QUICK_ACTIONS` 常數。
  - 移除標題列右側三個 outlined 快捷按鈕。
  - 移除未使用的 `Bolt`、`GridView`、`LocalOffer` icon import。
- `RegisterPage.test.tsx`：
  - 新增測試確認收銀台頁面不再渲染未使用的 `折扣`、`急單`、`格狀` 標題列快捷按鈕。

## Verification - POS Register Header Quick Action Placeholder Removal

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Web：
  - `npm test -- --run test/pos-orders/RegisterPage.test.tsx`：通過，3 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，18 files / 46 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - `lsof -nP -iTCP:38080 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-backend` 占用。
  - `lsof -nP -iTCP:38082 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-frontend` 占用。
  - POS 仍使用 `38180 / 38182`。
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建並啟動。
- Browser：
  - 開啟 `http://localhost:38182/pos/register?fresh=remove-header-actions`。
  - DOM 驗證標題列不再顯示 `急單` 與 `格狀`。
  - DOM 位置驗證唯一剩餘的 `折扣` 按鈕位於右側購物車區，非標題列假快捷按鈕。

## Git 提交記錄 - POS Register Header Quick Action Placeholder Removal

- 時間：2026-05-22 10:42:41 CST
- Hash：`fea066c`
- 訊息：`refactor(pos): remove unused register quick actions`
- 變更統計：2 files changed, 11 insertions(+), 25 deletions(-)

## Account Management Polish

- 時間：2026-05-22 11:38:55 CST。
- 目標：確認母體 `module-auth` 帳號管理規劃與現況，並打磨後台帳號管理頁與後端鎖定狀態篩選。

## Planning - Account Management Polish

- 沿用既有 `module-auth` 後端帳號、角色與權限設計，不重做架構。
- 後端修正帳號列表 `LOCKED` 篩選語意，讓登入失敗鎖定的帳號可被管理後台查出。
- 前端將 `UserTable` 從深色硬編碼改成跟隨 MUI 深/淺色主題。
- 增加建立帳號與重設密碼必要欄位禁用狀態，停用/解除鎖定前增加確認流程。
- 補帳號管理表格測試，覆蓋鎖定狀態呈現、建立帳號欄位驗證與解除鎖定確認。

## Project Structure - Account Management Polish

```text
POS/
├── backend/
│   ├── module-auth/
│   │   ├── src/main/java/com/enterprise/auth/
│   │   │   ├── controller/UserController.java
│   │   │   ├── dto/
│   │   │   └── service/impl/UserServiceImpl.java
│   │   └── src/test/java/com/enterprise/auth/service/impl/UserServiceImplTest.java
│   └── module-common/
├── frontend-web/
│   ├── src/features/auth/
│   │   ├── api/authApi.ts
│   │   ├── components/UserTable.tsx
│   │   ├── pages/UserListPage.tsx
│   │   └── types/index.ts
│   ├── src/layouts/AdminLayout.tsx
│   └── test/auth/UserTable.test.tsx
├── docker/local/
├── env/local/
├── devlog/
└── 需求/
```

## Progress - Account Management Polish

- 規劃確認：
  - `需求/開發流程規劃.md` 已將 `module-auth` 標為完整 + POS 擴展，並列出 `User/Role/Permission` 與 `UserService/RoleService/PermissionService`。
  - `frontend-web/src/features/auth/` 已存在 `UserTable`、`RolePermissionTree`、`authApi` 與頁面路由。
  - 後台入口 `/admin/access/users` 已由 `AdminLayout` 與 `App.tsx` 接線。
- 後端：
  - `UserServiceImpl.statusFilter` 對 `LOCKED` 改用 `lockedUntil > now` 判定。
  - `ACTIVE` 篩選排除仍在鎖定期間的帳號，避免鎖定帳號被誤判為可用啟用帳號。
  - `UserServiceImplTest` 新增 `LOCKED` 篩選測試。
  - 已檢查母體 `/Users/wei/Desktop/code/模塊化組件/`；母體 `module-auth` 尚未包含 POS 目前的 managed user search / role summary API，無法做小範圍同步，避免整檔覆蓋帶入大量無關差異。
- 前端：
  - `UserListPage` 改用 `text.primary/text.secondary`。
  - `UserTable` 改用主題色、`alpha()` 與 `background.paper`，支援深色/淺色模式。
  - 依 `lockedUntil` 顯示實際鎖定狀態，並顯示失敗登入次數。
  - 建立帳號與重設密碼的提交按鈕在必要欄位空白時禁用。
  - 停用帳號或解除鎖定前顯示確認對話框。
  - `User` 型別補齊 `failedAttempts`、`lockedUntil`、`mfaEnabled`、`updatedAt`。

## Verification - Account Management Polish

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Backend：
  - `mvn -pl module-auth -am test -Dtest=UserServiceImplTest -Dsurefire.failIfNoSpecifiedTests=false`：通過，11 tests。
- Web：
  - `npm test -- --run test/auth/UserTable.test.tsx`：通過，3 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，19 files / 49 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - `lsof -nP -iTCP:38080 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-backend` 占用。
  - `lsof -nP -iTCP:38082 -sTCP:LISTEN`：由 OrbStack / `financial-accounting-frontend` 占用。
  - POS 使用 `38180 / 38182`。
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-backend` 與 `pos-frontend` 已重建並啟動。
  - `curl http://localhost:38180/actuator/health`：`{"status":"UP"}`。
- Browser：
  - 使用臨時 `/tmp/pos-browser-check` 的 `playwright-core` 控制本機 Chrome，不修改專案依賴。
  - 開啟 `http://localhost:38182/admin/access/users`。
  - 淺色模式：頁面顯示 `帳號管理`、`admin`、`新增帳號`，無 console error，截圖 `/tmp/pos-account-users-light.png`。
  - 深色模式：頁面顯示 `帳號管理`、`admin`，body 背景 `rgb(26, 28, 35)`，無 console error，截圖 `/tmp/pos-account-users-dark-real.png`。

## Git 提交記錄 - Account Management Polish

- 時間：2026-05-22 11:40:20 CST
- Hash：`a788561`
- 訊息：`feat(auth): polish account management`
- 變更統計：8 files changed, 431 insertions(+), 91 deletions(-)

## Planning - Account Management Permission Guard

- 目標：修正 POS 店長/收銀員身分進入 `帳號管理` 後 API 被 403 擋下，畫面卻顯示「載入角色資料失敗」與空表格的混淆狀態。
- 判斷：
  - `cashier` 目前具備 `CASHIER, STORE_MANAGER`，不具備 `system:user:manage`。
  - `admin` 具備 `SUPER_ADMIN`，後端對 `SUPER_ADMIN` 採用敏感管理功能 bypass。
  - 帳號管理頁包含建立帳號、重設密碼與角色指派，應要求 `SUPER_ADMIN` 重新登入，不應讓 POS session 留在半可用頁面。

## Progress - Account Management Permission Guard

- 後端：
  - `LoginResponse` 補回 `role` 欄位。
  - `AuthServiceImpl.login/refreshToken` 回傳主角色，讓前端登入後可保存 `SUPER_ADMIN` / `STORE_MANAGER` 等角色。
  - `AuthServiceImplTest` 補驗證登入回傳角色。
- 前端：
  - `authStore` 新增 `getTokenRole()`，可從既有 JWT payload 讀取角色，支援舊 session 判斷。
  - `ProtectedRoute` 新增 `allowedRoles`，`/admin/access/roles` 與 `/admin/access/users` 限定 `SUPER_ADMIN`。
  - `LoginPage` 對 `reason=admin-permission` 顯示「此功能需要系統管理員帳號」提示，並只清除非 `SUPER_ADMIN` 的舊 POS session，避免 admin 登入成功後被同一個 reason 清掉。
  - `AdminLayout` 依目前角色隱藏 `角色權限` 與 `帳號管理` 入口，減少無權限入口誤觸。
  - 補 `ProtectedRoute`、`LoginPage`、`authStore` 測試。

## Verification - Account Management Permission Guard

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- Backend：
  - `mvn -pl module-auth -am test -Dtest=AuthServiceImplTest,UserServiceImplTest,RbacControllerSecurityTest -Dsurefire.failIfNoSpecifiedTests=false`：通過，17 tests。
- Web：
  - `npm test -- --run test/shared/ProtectedRoute.test.tsx test/auth/LoginPage.test.tsx test/shared/authStore.test.ts`：通過，8 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，19 files / 52 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - 38080 / 38082 仍由 `financial-accounting` 佔用，POS 使用 `38180 / 38182`。
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建啟動。
- Browser：
  - 使用本機 Chrome + Playwright-core，開啟 `http://localhost:38182/admin/access/users`。
  - 真實 `cashier / 123456` session 進入帳號管理：導向 `/login?redirect=%2Fadmin%2Faccess%2Fusers&reason=admin-permission`，顯示系統管理員提示，POS session 被清除。
  - 再用 `admin / 123456` 登入：導向 `/admin/access/users`，`/api/v1/roles` 與 `/api/v1/users` 均 200，頁面顯示 `admin` 與 `cashier`，不再顯示「載入角色資料失敗」，console 無錯誤。

## Git 提交記錄 - Account Management Permission Guard

- 時間：2026-05-22 12:12:10 CST
- Hash：`5f8bfb6`
- 訊息：`fix(auth): guard account management access`
- 變更統計：15 files changed, 239 insertions(+), 17 deletions(-)

## Planning - Login Dark Mode Contrast

- 目標：修正後台登入頁在深色模式下，白色登入卡片內的輸入框文字、標籤與頁尾文字過淡，導致使用者看不清楚。
- 範圍：
  - 只調整登入頁與登入表單的局部色彩，不改動全域主題。
  - 保持卡片為淺色承載面，確保深色模式背景下仍有清楚層次。

## Progress - Login Dark Mode Contrast

- `LoginPage`：
  - 登入卡片補固定文字色 `#1e293b`。
  - 副標題與頁尾改用適合淺色卡片的 slate 色階，避免繼承深色主題的低對比文字。
- `LoginForm`：
  - 新增登入頁專用 `TextField` 樣式。
  - 固定輸入文字、placeholder/label、外框與 focus 狀態色，避免深色模式下 MUI input 套用白色文字。

## Verification - Login Dark Mode Contrast

- Web：
  - `npm test -- --run test/auth/LoginPage.test.tsx`：通過，2 tests。
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run`：通過，19 files / 52 tests。
  - `npm run build`：通過，保留既有 Vite chunk size warning。
- Docker：
  - 38080 / 38082 仍由 `financial-accounting` 佔用，POS 使用 `38180 / 38182`。
  - `docker compose -f docker/local/docker-compose.yml up -d --build frontend`：通過，`pos-frontend` 已重建啟動。
- Browser：
  - 開啟 `http://localhost:38182/login?redirect=/admin/access/users&reason=admin-permission`。
  - 實測登入卡片背景 `rgba(255, 255, 255, 0.95)`，卡片文字 `rgb(30, 41, 59)`。
  - 輸入框 label 為 `rgb(100, 116, 139)`，輸入文字與 WebKit fill 為 `rgb(30, 41, 59)`。
  - 頁尾文字可讀，登入頁截圖已確認無白字消失問題。
