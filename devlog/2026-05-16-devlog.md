# 2026-05-16 Devlog

## POS Order Discount Audit Detail

- 時間：2026-05-16 16:16:13 CST。
- 目標：延續 Phase 3-2 promotion 促銷，讓訂單列表不只顯示折扣摘要，也能從訂單詳情追溯促銷折扣來源、規則識別與品項快照，補上營運查單時最需要的基礎稽核入口。

## Planning

- 在 `OrderListPage` 新增每筆訂單的「詳情」操作。
- 詳情 Dialog 顯示訂單狀態、類型、時間、門店、終端、操作員、會員 ID。
- 詳情 Dialog 顯示折扣來源、折扣標籤、促銷規則 ID、優惠碼與金額拆分。
- 詳情 Dialog 顯示品項名稱、SKU、單價、數量、明細折扣與小計。
- 補 RTL 測試，避免促銷折扣稽核欄位在後續 UI 調整時被移除。

## Project Structure

```text
POS/
├── backend/                         # Spring Boot 多模組後端
├── frontend-web/                    # React/Vite POS Web 前端
│   ├── src/features/pos-orders/     # 訂單、收銀、結帳與掛單前端功能
│   └── test/pos-orders/             # POS 訂單與收銀前端回歸測試
├── docker/local/                    # 本地 Docker Compose 與 Nginx 設定
├── env/local/                       # 本地環境變數範本與實際 env_file
├── 需求/                            # 需求、規格、進度與驗收文件
├── docs/                            # 專案地圖與交接文件
├── devlog/                          # 每日開發日誌與 troubleshooting
├── scripts/                         # 初始化與輔助腳本
├── reference/模塊化組件/             # AI 參考用模組化組件匯出資料
└── tmp/                             # 本地驗證截圖與臨時產物，不進版控
```

## Progress

- 前端：
  - `OrderListPage` 新增 `detailTarget` state 與「詳情」按鈕。
  - 新增訂單詳情 Dialog，顯示訂單基本資訊、折扣稽核、金額拆分與品項快照。
  - 折扣來源以 `手動折扣`、`會員折扣`、`促銷折扣` 顯示，無資料時顯示 `-`，避免營運查單看到空白欄位。
- 測試：
  - 新增 `OrderListPage.test.tsx`，mock 訂單列表、付款狀態與發票狀態 API。
  - 覆蓋開啟詳情 Dialog 後能看到 `促銷折扣`、折扣標籤、促銷規則 ID、優惠碼與品項 SKU。
- 文件：
  - 更新 `需求/開發進度對照.md`，將 Phase 3-2 促銷狀態補上「訂單詳情稽核檢視」。
  - 更新 `.gitignore`，排除本地瀏覽器驗證截圖與臨時產物 `tmp/`。

## Verification - POS Order Discount Audit Detail

- 分支與同步：
  - `git status --short --branch`：確認位於 `feature`。
  - `git pull origin feature`：Already up to date。
- 前端測試：
  - `npx tsc -b`：通過。
  - `npm run lint`：通過。
  - `npm test -- --run test/pos-orders/OrderListPage.test.tsx`：通過，1 file / 1 test。
  - `npm test -- --run`：通過，10 files / 18 tests。
  - `npm run build`：通過，僅保留既有 Vite chunk size warning。
- Docker / OrbStack：
  - 啟動前檢查端口：`38080`、`38082` 由 financial-accounting 使用；POS 使用 `38180`、`38182`。
  - `docker compose --env-file env/local/.env -f docker/local/docker-compose.yml up -d --build frontend`：成功，重建並啟動 `pos-frontend`；compose 同步檢查 backend image cache。
- 瀏覽器驗證：
  - 開啟 `http://127.0.0.1:38182/pos/orders`。
  - 訂單列表第一列 `000000-20260515221933-98365` 顯示 `咖啡滿百 9 折 -$15`。
  - 點擊同列「詳情」後，Dialog 顯示 `折扣稽核`、`促銷折扣`、`咖啡滿百 9 折`、`00000000-0000-0000-0000-000000001001`、`燕麥拿鐵 12oz` 與 `DEMO-OAT-LATTE-12OZ`。
  - 截圖已暫存於 `tmp/order-discount-audit-detail.png` 作為本地視覺驗證紀錄。
